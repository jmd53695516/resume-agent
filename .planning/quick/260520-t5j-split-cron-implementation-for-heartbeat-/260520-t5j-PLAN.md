---
phase: quick-260520-t5j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompt-cache.ts
  - src/app/api/cron/heartbeat/route.ts
  - src/app/api/cron/prewarm-cache/route.ts
  - tests/cron/prewarm-cache.test.ts
  - .planning/incidents/2026-05-20-heartbeat-overfire.md
  - .planning/reports/MILESTONE_SUMMARY-v1.0.md
autonomous: true
requirements:
  - quick-260520-t5j
must_haves:
  truths:
    - "warmPromptCache() is defined in src/lib/prompt-cache.ts and imported by both cron routes"
    - "POST /api/cron/prewarm-cache validates Bearer auth, calls warmPromptCache(), returns 200 on both Anthropic success and Anthropic failure"
    - "POST /api/cron/prewarm-cache writes heartbeat:anthropic Redis key with ex=120 on Anthropic success"
    - "POST /api/cron/prewarm-cache logs event:'cron_run', cron_name:'prewarm-cache' on every successful run"
    - "Existing /api/cron/heartbeat behavior is unchanged when HEARTBEAT_LLM_PREWARM=true (warmPromptCache still runs)"
    - "Existing /api/cron/heartbeat behavior is unchanged when HEARTBEAT_LLM_PREWARM=false (warmPromptCache skipped, deps-only)"
    - "npx tsc --noEmit exits 0"
    - "npm run build exits 0"
    - "Existing tests/cron/heartbeat.test.ts still passes (warmPromptCache moved to shared module, mock surface preserved)"
    - "New tests/cron/prewarm-cache.test.ts passes with auth/success/failure/redis cases"
    - "Incident doc Addendum 'Implementation status' line flipped to SHIPPED with commit hash"
    - "MILESTONE_SUMMARY-v1.0.md line 125 describes two-cron operational reality"
  artifacts:
    - path: "src/lib/prompt-cache.ts"
      provides: "warmPromptCache() shared between heartbeat and prewarm-cache routes"
      exports: ["warmPromptCache"]
    - path: "src/app/api/cron/prewarm-cache/route.ts"
      provides: "Cache-warmth half of the split-cron pattern"
      exports: ["POST", "runtime", "maxDuration"]
    - path: "src/app/api/cron/heartbeat/route.ts"
      provides: "Deps-only ping when HEARTBEAT_LLM_PREWARM=false; imports warmPromptCache from shared lib"
      contains: "import { warmPromptCache } from '@/lib/prompt-cache'"
    - path: "tests/cron/prewarm-cache.test.ts"
      provides: "Auth + success + failure + redis-write coverage for new route"
      contains: "describe('POST /api/cron/prewarm-cache'"
  key_links:
    - from: "src/app/api/cron/heartbeat/route.ts"
      to: "src/lib/prompt-cache.ts"
      via: "named import"
      pattern: "from ['\"]@/lib/prompt-cache['\"]"
    - from: "src/app/api/cron/prewarm-cache/route.ts"
      to: "src/lib/prompt-cache.ts"
      via: "named import"
      pattern: "from ['\"]@/lib/prompt-cache['\"]"
    - from: "src/app/api/cron/prewarm-cache/route.ts"
      to: "src/lib/cron-auth.ts"
      via: "validateCronAuth"
      pattern: "validateCronAuth\\(req\\)"
    - from: "src/lib/prompt-cache.ts"
      to: "redis"
      via: "heartbeat:anthropic write"
      pattern: "heartbeat:anthropic"
---

<objective>
Implement the split-cron pattern decided in quick task 260520-paz: extract `warmPromptCache()` into a shared lib, add a new `/api/cron/prewarm-cache` route that ONLY runs the Sonnet prompt-cache prewarm, and leave the existing `/api/cron/heartbeat` route's logic unchanged (its `HEARTBEAT_LLM_PREWARM=false` gate already exists). After ship + Joe's operational steps in Vercel + cron-job.org, the heartbeat fires every 1 min during business hours (cheap deps-only, keeps banner green) and prewarm-cache fires every 5 min (Sonnet cache_read only, keeps Anthropic ephemeral cache warm).

Purpose: Resolve the banner-vs-cost tension documented in `.planning/incidents/2026-05-20-heartbeat-overfire.md` Addendum. Today the cron is at `*/5` (banner regression) or `*/1` (over-fire $5.57/biz-day). Post-ship steady state is ~$1.06/biz-day with banner green.

Output: 1 new shared helper, 1 new route, 1 new test file, top-comment + doc reconciliations on three existing files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/incidents/2026-05-20-heartbeat-overfire.md
@src/app/api/cron/heartbeat/route.ts
@src/lib/cron-auth.ts
@src/lib/anthropic.ts
@src/lib/env.ts
@src/lib/redis.ts
@src/lib/logger.ts
@src/lib/system-prompt.ts
@src/lib/health.ts
@tests/cron/heartbeat.test.ts

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From src/lib/cron-auth.ts:
- `export function validateCronAuth(req: Request): boolean` — returns true iff method=POST AND Authorization='Bearer <CRON_SECRET>'. Caller returns 401 on false.

From src/lib/anthropic.ts:
- `export const MODELS = { MAIN: 'claude-sonnet-4-6', CLASSIFIER: 'claude-haiku-4-5' } as const`

From src/lib/env.ts:
- `env.ANTHROPIC_API_KEY: string` — required
- `env.CRON_SECRET: string` — required, min 32 chars
- `env.HEARTBEAT_LLM_PREWARM?: string` — optional, defaults to 'true'; `.toLowerCase() !== 'false'` is the truthy check used at heartbeat/route.ts:117

From src/lib/redis.ts:
- `export const redis: Redis` — Upstash Redis instance; key write pattern: `await redis.set('heartbeat:anthropic', Date.now(), { ex: 120 })`

From src/lib/logger.ts:
- `export function log(payload: Record<string, unknown>, level?: 'debug'|'info'|'warn'|'error'): void`

From src/lib/system-prompt.ts:
- `export function buildSystemPrompt(): string` — MUST be used (not an inline copy) so byte-identical prefix produces a cache hit. Pitfall 5 / D-E determinism contract.

From src/lib/health.ts:
- `HEARTBEAT_OK_S = 60` — banner ok window. Heartbeat key TTL = 120s. Drives the 1-min cron cadence requirement.

From src/app/api/cron/heartbeat/route.ts (current warmPromptCache shape — port verbatim into src/lib/prompt-cache.ts):
- Lazy Anthropic client via `getAnthropic()` accessor (lines 43-49). Class-mock pattern in tests/cron/heartbeat.test.ts:39-43 depends on this.
- `warmPromptCache(): Promise<{ cache_read_tokens: number; cost_cents: number; ok: boolean }>` (lines 60-95)
- Body: `messages.create({ model: MODELS.MAIN, max_tokens: 1, system: [{ type: 'text', text: buildSystemPrompt(), cache_control: { type: 'ephemeral' } }], messages: [{ role: 'user', content: 'ping' }] })`
- Cost: `Math.round((cache_read / 1_000_000) * 30)` — Sonnet 4.6 cache_read rate is $0.30/MTok
- On success: write `redis.set('heartbeat:anthropic', Date.now(), { ex: 120 })`
- On error: log `{ event: 'heartbeat_anthropic_failed', error_message: (err as Error).message }` at 'warn' level, return `{ cache_read_tokens: 0, cost_cents: 0, ok: false }`
</interfaces>

Key constraints (locked — do not re-debate):
- Two-route pattern. Existing heartbeat route's `HEARTBEAT_LLM_PREWARM` gate STAYS (lines 116-120). No deletion or rename of the env var.
- The classifier live call at heartbeat/route.ts:130-146 (Haiku banner refresh) STAYS in the heartbeat route — NOT a prewarm concern.
- `warmPromptCache()` signature is unchanged so heartbeat/route.ts:119 call site needs only an import swap.
- `validateCronAuth` is reused as-is. `runtime='nodejs'` + lazy Anthropic client pattern matches the existing route.
- Per project memory feedback_local_vs_vercel_build.md: run BOTH `npx tsc --noEmit` AND `npm run build` before declaring done. Vitest alone misses Next-build-time errors.
- Per CLAUDE.md: no emoji anywhere in output (code comments, test descriptions, SUMMARY).
- DO NOT touch operational state (no Vercel env edits, no cron-job.org edits). Operational steps go in SUMMARY.md for Joe to execute post-ship.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract warmPromptCache into src/lib/prompt-cache.ts and rewire heartbeat route</name>
  <files>src/lib/prompt-cache.ts, src/app/api/cron/heartbeat/route.ts</files>
  <action>
    Create `src/lib/prompt-cache.ts` exporting `warmPromptCache()` with the SAME signature and body as the current implementation at heartbeat/route.ts:60-95. Preserve the lazy Anthropic client pattern: define a module-scoped `let _anthropic: Anthropic | null = null` and a private `getAnthropic()` accessor that instantiates `new Anthropic({ apiKey: env.ANTHROPIC_API_KEY })` on first call. Imports needed: `Anthropic` from '@anthropic-ai/sdk', `MODELS` from '@/lib/anthropic', `env` from '@/lib/env', `redis` from '@/lib/redis', `buildSystemPrompt` from '@/lib/system-prompt', `log` from '@/lib/logger'.

    Add a top-of-file comment explaining: (1) this is the shared cache-warmth helper for the split-cron pattern; (2) consumed by both /api/cron/heartbeat (when HEARTBEAT_LLM_PREWARM=true) and /api/cron/prewarm-cache; (3) MUST use buildSystemPrompt() — never an inline copy — for byte-identical prefix match (Pitfall 5 / Phase 1 D-E determinism); (4) cite `.planning/incidents/2026-05-20-heartbeat-overfire.md`.

    In `src/app/api/cron/heartbeat/route.ts`: (a) replace the inline `warmPromptCache` function body (lines 60-95), the `getAnthropic`/`_anthropic` lazy client (lines 43-49), and the unused `Anthropic`/`MODELS`/`buildSystemPrompt` imports with a single `import { warmPromptCache } from '@/lib/prompt-cache'`. Verify by tracing: after the swap, `Anthropic`, `MODELS`, and `buildSystemPrompt` should NO LONGER be referenced in heartbeat/route.ts and their imports should be removed. (b) Update the top-of-file comment to describe the new operational reality: this route is the deps-only ping when HEARTBEAT_LLM_PREWARM=false (the production setting after this ships), the prompt-cache prewarm now lives at /api/cron/prewarm-cache, expected schedule `*/1 9-17 * * 1-5` TZ America/New_York, and reference `.planning/incidents/2026-05-20-heartbeat-overfire.md` Addendum for the split-cron rationale. Leave the existing references to the prior `*/5` reconciliation as historical context.

    Do NOT change the classifier live call at lines 130-146, the heartbeat:exa write block, the heartbeat:anthropic write block (lines 171-182 — runs when prewarm disabled), the anthropicStatus logic (lines 196-198), or either log() call (event:'heartbeat' and event:'cron_run'). Those all stay byte-identical.

    Test mock-surface guidance (preferred path — try this FIRST, change nothing else):
    1. After the import swap, run `npx vitest run tests/cron/heartbeat.test.ts`.
    2. EXPECTED: it passes with no test-file edits. The existing `vi.mock('@anthropic-ai/sdk')` class-mock at line 39 is hoisted and applies globally per test file, so the lazy `new Anthropic(...)` inside the new `src/lib/prompt-cache.ts` resolves to the same mocked constructor. The existing `vi.mock('@/lib/system-prompt')` and `vi.mock('@/lib/anthropic')` mocks are also globally hoisted and continue to satisfy prompt-cache.ts's transitive imports.
    3. Only if step 2 fails: read the vitest error output, identify the SPECIFIC mock that is not being applied to the transitive import (e.g. "redis.set is not a function" → the mock for that module is missing), and add ONLY the minimum mock needed. Document the diagnosed gap in the commit message. Adding `vi.mock('@/lib/prompt-cache', ...)` is a last resort — it converts the heartbeat test from a behavior test into a stub test and should only be used if no transitive mock can be made to work.
  </action>
  <verify>
    <automated>cd "C:/Users/joedo/Documents/Python Scripts/Agent For Interviews" &amp;&amp; npx vitest run tests/cron/heartbeat.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/prompt-cache.ts exists and exports warmPromptCache. heartbeat/route.ts no longer defines warmPromptCache, no longer imports Anthropic or buildSystemPrompt or MODELS directly, and imports warmPromptCache from '@/lib/prompt-cache'. heartbeat/route.ts top comment describes the new deps-only role + cites the incident doc + states the new */1 schedule. tests/cron/heartbeat.test.ts still passes. `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create POST /api/cron/prewarm-cache route</name>
  <files>src/app/api/cron/prewarm-cache/route.ts</files>
  <action>
    Create the new route file. Export `runtime = 'nodejs'`, `maxDuration = 60`, and an async `POST(req: Request): Promise<Response>` handler.

    Handler body:
    1. `if (!validateCronAuth(req)) return Response.json({ error: 'unauthorized' }, { status: 401 });`
    2. `const started = Date.now();`
    3. `const prewarm = await warmPromptCache();` — warmPromptCache handles its own try/catch internally and returns `{ cache_read_tokens, cost_cents, ok }`. Do NOT add an outer try/catch — graceful failure is already baked in (returns ok:false on Anthropic error).
    4. Emit two log lines matching the heartbeat route's shape:
       - `log({ event: 'prewarm_cache', anthropic_cache_read_tokens: prewarm.cache_read_tokens, cost_cents: prewarm.cost_cents, ok: prewarm.ok, duration_ms: Date.now() - started });`
       - `log({ event: 'cron_run', cron_name: 'prewarm-cache', duration_ms: Date.now() - started, status: prewarm.ok ? 'ok' : 'degraded', items_processed: 1 });`
    5. Return `Response.json({ ok: prewarm.ok })` with status 200 in BOTH success and failure cases. Rationale: returning non-2xx would trigger cron-job.org retries — we don't want retries on a transient Anthropic blip because the next scheduled fire (5 min later) is cheaper than a retry storm.

    Imports: `validateCronAuth` from '@/lib/cron-auth', `warmPromptCache` from '@/lib/prompt-cache', `log` from '@/lib/logger'.

    Top-of-file comment must document:
    - Role: cache-warmth half of the split-cron pattern decided in `.planning/incidents/2026-05-20-heartbeat-overfire.md` Addendum.
    - Expected cron-job.org schedule: `*/5 9-17 * * 1-5` TZ America/New_York. Cadence is constrained by Anthropic's ~5-min ephemeral prompt-cache TTL — firing more often burns cache_read tokens with zero coverage benefit (the over-fire failure mode from the incident).
    - Companion route: `/api/cron/heartbeat` runs every 1 min for dep pings + classifier banner with HEARTBEAT_LLM_PREWARM=false.
    - Cost: ~$1.00/biz-day at the expected schedule (Sonnet cache_read ~30k tokens/fire * 108 fires/biz-day).
    - Auth: same Bearer CRON_SECRET pattern as the heartbeat route.
    - Graceful-failure contract: returns HTTP 200 even on Anthropic error so cron-job.org does not retry.
  </action>
  <verify>
    <automated>cd "C:/Users/joedo/Documents/Python Scripts/Agent For Interviews" &amp;&amp; node -e "require('fs').accessSync('src/app/api/cron/prewarm-cache/route.ts'); const src = require('fs').readFileSync('src/app/api/cron/prewarm-cache/route.ts','utf8'); if(!/export\s+async\s+function\s+POST/.test(src))process.exit(1); if(!/validateCronAuth/.test(src))process.exit(2); if(!/warmPromptCache/.test(src))process.exit(3); if(!/runtime\s*=\s*['\"]nodejs['\"]/.test(src))process.exit(4); console.log('ok');"</automated>
  </verify>
  <done>src/app/api/cron/prewarm-cache/route.ts exists, exports POST + runtime='nodejs' + maxDuration=60, validates Bearer auth, calls warmPromptCache (no outer try/catch — relies on warmPromptCache's internal try/catch), returns 200 in both success and failure cases, emits prewarm_cache + cron_run log events, and the top comment explains the schedule + companion route + cost + graceful-failure contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/cron/prewarm-cache.test.ts</name>
  <files>tests/cron/prewarm-cache.test.ts</files>
  <action>
    Create the test file mirroring tests/cron/heartbeat.test.ts mocking style. Use `vi.hoisted()` for the shared mocks container, class-mock @anthropic-ai/sdk (lazy `new Anthropic(...)` in prompt-cache.ts depends on this), mock @/lib/env to expose CRON_SECRET + ANTHROPIC_API_KEY, mock @/lib/redis with `{ set: vi.fn() }`, mock @/lib/system-prompt with `{ buildSystemPrompt: vi.fn(() => 'CACHED-SYSTEM-PROMPT') }`, mock @/lib/anthropic with `{ MODELS: { MAIN: 'claude-sonnet-4-6', CLASSIFIER: 'claude-haiku-4-5' } }`, mock @/lib/logger with `{ log: vi.fn() }`.

    Import `POST` from '@/app/api/cron/prewarm-cache/route'. `makeReq` helper mirrors heartbeat.test.ts (constructs a Request with optional auth + method).

    Required test cases:

    1. `returns 401 with no auth header` — assert status 401 and messagesCreate NOT called.

    2. `returns 401 with GET method even with correct Bearer token` — assert status 401.

    3. `returns 200 + writes heartbeat:anthropic Redis key on Anthropic success` — mock `messagesCreate.mockResolvedValue({ usage: { cache_read_input_tokens: 30000, cache_creation_input_tokens: 0, input_tokens: 100, output_tokens: 1 } })`. Assert:
       - status 200, response body `{ ok: true }`
       - messagesCreate called with `expect.objectContaining({ model: 'claude-sonnet-4-6', max_tokens: 1, system: [expect.objectContaining({ type: 'text', text: 'CACHED-SYSTEM-PROMPT', cache_control: { type: 'ephemeral' } })], messages: [{ role: 'user', content: 'ping' }] })`
       - `redisSet` called with `('heartbeat:anthropic', expect.any(Number), { ex: 120 })`
       - log called with `expect.objectContaining({ event: 'prewarm_cache', anthropic_cache_read_tokens: 30000, cost_cents: 1, ok: true })` (Math.round((30000/1_000_000)*30) = 1)
       - log called with `expect.objectContaining({ event: 'cron_run', cron_name: 'prewarm-cache', status: 'ok' })`

    4. `returns 200 with ok:false on Anthropic failure (graceful — no retry trigger)` — `messagesCreate.mockRejectedValue(new Error('rate limited'))`. Assert:
       - status 200 (NOT 5xx — graceful-failure contract; cron-job.org must not retry)
       - response body `{ ok: false }`
       - log called with `expect.objectContaining({ event: 'heartbeat_anthropic_failed' }), 'warn'` (this comes from warmPromptCache's internal catch — preserved from the original implementation)
       - redisSet NOT called with `'heartbeat:anthropic'` (the warmPromptCache redis.set happens before the inner catch only on the success path)
       - log called with `expect.objectContaining({ event: 'cron_run', cron_name: 'prewarm-cache', status: 'degraded' })`

    5. `does NOT make the Haiku classifier live call (that stays in /api/cron/heartbeat)` — assert no mock for classifier is invoked. Since this test file does not mock @/lib/classifier, the import would fail at module load if the route accidentally imported the classifier — making this an implicit gate. Add an explicit assertion comment: `// Regression gate: if a future edit accidentally adds the classifier call to this route, this file won't even compile because @/lib/classifier isn't mocked here.`

    `beforeEach` resets all mocks. Use `mockResolvedValue('OK')` on redisSet by default. CRON_SECRET = 34-char string built like in heartbeat.test.ts (`['x','y','z'].join('').repeat(11) + 'a'`). ANTHROPIC_API_KEY = `'sk-ant-' + 'x'.repeat(20)`.
  </action>
  <verify>
    <automated>cd "C:/Users/joedo/Documents/Python Scripts/Agent For Interviews" &amp;&amp; npx vitest run tests/cron/prewarm-cache.test.ts</automated>
  </verify>
  <done>Requires Tasks 1 and 2 complete. tests/cron/prewarm-cache.test.ts exists, all 5 test cases pass, no flakes. Mocking style matches tests/cron/heartbeat.test.ts (vi.hoisted + class-mock @anthropic-ai/sdk).</done>
</task>

<task type="auto">
  <name>Task 4: Full build + type verification + doc reconciliation</name>
  <files>.planning/incidents/2026-05-20-heartbeat-overfire.md, .planning/reports/MILESTONE_SUMMARY-v1.0.md</files>
  <action>
    Step A — Full verification gate (run BOTH commands; per project memory feedback_local_vs_vercel_build.md, vitest does NOT catch Next build-time errors):
    1. `npx tsc --noEmit` — must exit 0
    2. `npm run build` — must exit 0
    3. `npx vitest run tests/cron/` — both heartbeat.test.ts and prewarm-cache.test.ts pass

    If any of (1)(2)(3) fail, fix the root cause and re-run all three. Do not declare done until all green.

    Step B — Update `.planning/incidents/2026-05-20-heartbeat-overfire.md` Addendum. Find the line `Implementation status at time of writing: **NOT YET SHIPPED**.` and replace it with `Implementation status: **SHIPPED in commit <hash> on 2026-05-21** — split-cron pattern live; cron-job.org operational steps tracked in `.planning/quick/260520-t5j-split-cron-implementation-for-heartbeat-/SUMMARY.md`.` Leave `<hash>` as the literal string `<hash>` — the actual commit hash gets filled in by the git workflow after this commit lands.

    Step C — Update `.planning/reports/MILESTONE_SUMMARY-v1.0.md` line 125 (the cron-job.org schedules bullet). Replace the existing heartbeat sentence with a description of the two-cron operational reality:
    - `/api/cron/heartbeat` at `*/1 9-17 * * 1-5` TZ America/New_York with HEARTBEAT_LLM_PREWARM=false (deps-only, ~$0.06/biz-day, keeps banner green)
    - `/api/cron/prewarm-cache` at `*/5 9-17 * * 1-5` TZ America/New_York (Sonnet cache_read only, ~$1.00/biz-day, keeps Anthropic ephemeral cache warm)
    - Total ~$1.06/biz-day, reconciled 2026-05-21 from the 2026-05-20 banner-vs-cost tension
    - Reference `.planning/incidents/2026-05-20-heartbeat-overfire.md` Addendum

    Keep the archive + weekly-eval deferred sentences as-is.

    Step D — Write the SUMMARY.md for this quick task at `.planning/quick/260520-t5j-split-cron-implementation-for-heartbeat-/SUMMARY.md` documenting Joe's operational steps post-ship (these are for Joe to execute manually — DO NOT execute them):
    1. Vercel dashboard: add `HEARTBEAT_LLM_PREWARM=false` to Production environment (already exists as an env var; set its value to literal string `false`). Trigger redeploy or wait for next push.
    2. cron-job.org existing heartbeat job: change schedule from current `*/5 9-17 * * 1-5` TZ America/New_York back to `*/1 9-17 * * 1-5` TZ America/New_York.
    3. cron-job.org NEW job: create `prewarm-cache` POSTing to `https://joe-dollinger-chat.com/api/cron/prewarm-cache` with header `Authorization: Bearer <CRON_SECRET>` (same secret as heartbeat — copy from Vercel env). Schedule `*/5 9-17 * * 1-5` TZ America/New_York.
    4. Verify post-deploy: hit /admin/health, confirm StatusBanner shows green during business hours (within 1 min of the heartbeat fire).
    5. Verify cost: 24h later, confirm Anthropic dashboard shows ~$1.06/biz-day total (down from the over-fire $5.57 or banner-regression $1.08).

    Also capture in SUMMARY.md: expected steady-state cost numbers, file list created/modified, and a one-line backreference to the incident doc.
  </action>
  <verify>
    <automated>cd "C:/Users/joedo/Documents/Python Scripts/Agent For Interviews" &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npx vitest run tests/cron/heartbeat.test.ts tests/cron/prewarm-cache.test.ts</automated>
  </verify>
  <done>npx tsc --noEmit exits 0. npm run build exits 0. Both cron test files pass. Incident doc Addendum 'Implementation status' line flipped to SHIPPED (with `<hash>` placeholder). MILESTONE_SUMMARY-v1.0.md line 125 describes both crons with cost numbers. SUMMARY.md exists with Joe's 5 operational steps + steady-state cost expectation + file list.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm run build` exits 0
3. `npx vitest run tests/cron/heartbeat.test.ts` — all existing cases pass (no regression from the warmPromptCache extraction)
4. `npx vitest run tests/cron/prewarm-cache.test.ts` — all 5 new cases pass
5. File existence: src/lib/prompt-cache.ts, src/app/api/cron/prewarm-cache/route.ts, tests/cron/prewarm-cache.test.ts
6. Grep verification: `heartbeat/route.ts` does NOT contain `function warmPromptCache` (it now imports it). `heartbeat/route.ts` does NOT import from `@anthropic-ai/sdk`, `@/lib/anthropic` (MODELS), or `@/lib/system-prompt` directly (those moved with warmPromptCache to prompt-cache.ts).
7. Doc verification: incident doc Addendum no longer contains 'NOT YET SHIPPED'. MILESTONE_SUMMARY-v1.0.md line ~125 contains both 'prewarm-cache' and '*/1 9-17' substrings.
</verification>

<success_criteria>
- All 4 tasks complete with green verify commands
- All must_haves truths observable and artifacts present
- Joe has a clear SUMMARY.md describing the 5 operational steps required post-deploy (Vercel env + 2 cron-job.org changes + verification)
- No operational state changed by Claude (env vars untouched, cron-job.org untouched)
- Steady-state cost after Joe's operational steps: ~$1.06/biz-day (down from $5.57 over-fire baseline)
- Banner stays green during business hours because the deps-only heartbeat fires every 1 minute
- Anthropic prompt cache stays warm because prewarm-cache fires every 5 minutes
</success_criteria>

<output>
Create `.planning/quick/260520-t5j-split-cron-implementation-for-heartbeat-/SUMMARY.md` when done (Task 4 Step D). Capture commit hash for the incident doc backfill.
</output>
