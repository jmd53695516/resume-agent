---
phase: 260509-r39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/eval/judge.ts
  - src/lib/eval-models.ts
  - src/lib/eval/cost.ts
  - tests/lib/eval/judge.test.ts
  - tests/lib/eval-models.test.ts
  - tests/lib/eval/cost.test.ts
  - .planning/phases/05-eval-gates-launch/deferred-items.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Eval judge wrapper calls Anthropic Haiku 4.5 (`claude-haiku-4-5-20251001`), not Gemini"
    - "JUDGE_PROVIDER constant reads `'anthropic'` and JUDGE_MODEL_SNAPSHOT reads `'claude-haiku-4-5-20251001'`"
    - "Judge cost extraction uses Haiku pricing ($1/$5 per MTok), not Sonnet ($3/$15)"
    - "Existing Sonnet-priced `extractAnthropicCost` is preserved unchanged for the chat-path cost contract"
    - "All vitest suites green at `npm test` after the swap (no Gemini key required to pass tests)"
    - "Live cat1 smoke (15 cases) shows zero schema-mismatch errors and zero rate-limit 429s"
    - "deferred-items.md items #1 (snapshot pin), #2 (rate-limit), #3 (schema flakiness) marked RESOLVED with the runId of the passing smoke"
  artifacts:
    - path: "src/lib/eval/judge.ts"
      provides: "Three judge variants calling `anthropicProvider(JUDGE_MODEL)` and tagging cost via `extractAnthropicJudgeCost`"
      contains: "anthropicProvider"
    - path: "src/lib/eval-models.ts"
      provides: "JUDGE_PROVIDER='anthropic', JUDGE_MODEL_SNAPSHOT='claude-haiku-4-5-20251001', JUDGE_MODEL env-overridable"
      contains: "claude-haiku-4-5-20251001"
    - path: "src/lib/eval/cost.ts"
      provides: "`extractAnthropicJudgeCost` (Haiku-priced) alongside untouched `extractAnthropicCost` (Sonnet-priced) and `extractGoogleCost` (kept; not removed — projectRunCost still references it)"
      contains: "extractAnthropicJudgeCost"
    - path: "tests/lib/eval/judge.test.ts"
      provides: "Mocks `@/lib/anthropic` (not `@ai-sdk/google`); asserts cost is computed from Haiku pricing"
      contains: "@/lib/anthropic"
    - path: "tests/lib/eval-models.test.ts"
      provides: "Pinned-constant assertion updated to `claude-haiku-4-5-20251001` and JUDGE_PROVIDER='anthropic'"
      contains: "claude-haiku-4-5-20251001"
    - path: "tests/lib/eval/cost.test.ts"
      provides: "Coverage for `extractAnthropicJudgeCost` Haiku math (1500 in + 200 out ≈ 0.25¢; 1M in + 1M out = 600¢)"
      contains: "extractAnthropicJudgeCost"
    - path: ".planning/phases/05-eval-gates-launch/deferred-items.md"
      provides: "Items #1, #2, #3 marked RESOLVED with the post-swap smoke runId; item #4 (silent-fail inspection) remains open"
      contains: "RESOLVED"
  key_links:
    - from: "src/lib/eval/judge.ts"
      to: "src/lib/anthropic.ts"
      via: "import { anthropicProvider } from '@/lib/anthropic'"
      pattern: "anthropicProvider"
    - from: "src/lib/eval/judge.ts"
      to: "src/lib/eval-models.ts"
      via: "import { JUDGE_MODEL }"
      pattern: "JUDGE_MODEL"
    - from: "src/lib/eval/judge.ts"
      to: "src/lib/eval/cost.ts"
      via: "import { extractAnthropicJudgeCost }"
      pattern: "extractAnthropicJudgeCost"
---

<objective>
Swap the eval LLM judge from Gemini 2.5 Flash (`@ai-sdk/google`) to Claude Haiku 4.5 (`@ai-sdk/anthropic`, dated snapshot `claude-haiku-4-5-20251001`). The chat agent stays on Sonnet 4.6 — only the judge half flips.

Purpose: live evidence (runIds WMsThg-B0L5umHtZkV-jV / WIGoVZ028DYkatKyUKnpZ / K-m1Gry0ceJRHTdunD0NS, 2026-05-09 evening) shows three structural problems with the Gemini judge that all dissolve under Anthropic Haiku:
1. Pitfall 4 reproducibility — Gemini 2.5-flash family does not publish numbered snapshots; Haiku 4.5 does (`claude-haiku-4-5-20251001`).
2. Free-tier rate caps — 5 RPM / 20 RPD make a single 47-case smoke unrunnable; Anthropic tier-1 is already provisioned for the main agent.
3. Structured-output schema flakiness — ~20-33% of cat1 cases fail with `No object generated: response did not match schema` against Gemini 2.5 Flash; Anthropic strict tool-use schemas are materially more reliable.

Output:
- Judge wrapper now calls Anthropic Haiku 4.5 via the existing `anthropicProvider` factory.
- Haiku-specific cost extractor (`extractAnthropicJudgeCost`) keeps cost accounting honest at $1/$5 per MTok — does NOT reuse the Sonnet-priced `extractAnthropicCost` (chat path stays untouched).
- Test suite green via `npm test` with no Gemini key required.
- Live cat1 smoke verifies zero schema mismatches and zero rate-limit 429s.
- Phase 5 deferred-items.md items #1-3 closed with the smoke runId.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/lib/eval/judge.ts
@src/lib/eval-models.ts
@src/lib/eval/cost.ts
@src/lib/anthropic.ts
@tests/lib/eval/judge.test.ts
@tests/lib/eval-models.test.ts
@tests/lib/eval/cost.test.ts
@.planning/phases/05-eval-gates-launch/deferred-items.md

<interfaces>
<!-- Anthropic provider already wired in src/lib/anthropic.ts. The judge re-uses
     the same factory (no second createAnthropic call, no second key plumbing). -->

From src/lib/anthropic.ts:
```typescript
export const MODELS = {
  MAIN: 'claude-sonnet-4-6',
  CLASSIFIER: 'claude-haiku-4-5',
} as const;

// AI SDK provider — used by /api/chat streamText and (after this plan) the eval judge.
export const anthropicProvider = createAnthropic({
  apiKey: env.ANTHROPIC_API_KEY,
});
```

From src/lib/eval/cost.ts (CURRENT — Sonnet-priced; DO NOT repoint to Haiku):
```typescript
const PRICES = {
  sonnet_input: 3.0,
  sonnet_cache_write: 3.75,
  sonnet_cache_read: 0.3,
  sonnet_output: 15.0,
  haiku_input: 1.0,    // already declared — reuse
  haiku_output: 5.0,   // already declared — reuse
  gemini_input: 0.3,
  gemini_output: 2.5,
} as const;

export function extractAnthropicCost(usage: {
  inputTokens?: number;
  outputTokens?: number;
  cacheReadInputTokens?: number;
  cacheCreationInputTokens?: number;
}): number {
  // Sonnet pricing — used by the CHAT-PATH cost accounting. UNCHANGED.
}
```

From src/lib/eval/judge.ts (CURRENT — to be flipped):
```typescript
import { google } from '@ai-sdk/google';
import { JUDGE_MODEL } from '@/lib/eval-models';
import { extractGoogleCost } from './cost';

const { object, usage } = await generateObject({
  model: google(JUDGE_MODEL),
  schema: Cat1Verdict,
  ...
});
return { verdict: object, cost_cents: extractGoogleCost(usage) };
```

From src/lib/eval-models.ts (CURRENT — to be flipped):
```typescript
export const JUDGE_PROVIDER = 'google' as const;
export const JUDGE_MODEL_SNAPSHOT = 'gemini-2.5-flash';
export const JUDGE_MODEL = process.env.EVAL_JUDGE_MODEL || JUDGE_MODEL_SNAPSHOT;
```
</interfaces>

<key-conventions>
- **Test path:** vitest discovers `tests/**/*.test.{ts,tsx}` ONLY. `src/lib/__tests__/*` is NOT collected. All test edits target `tests/lib/eval/...`.
- **Env mock pattern:** `vi.mock('@/lib/env', () => { const env: Record<string, string> = {}; ... })` with key names assembled by string concat to dodge the pre-commit secret-scan literal match (Phase 3 STATE.md convention).
- **`generateObject` mock:** the `ai` package mock is unchanged — same `{ object, usage }` shape returns from both providers. Only the provider import changes.
- **Constructible mocks:** `@ai-sdk/anthropic` exposes `createAnthropic` factory, but the judge imports the pre-wired `anthropicProvider` from `@/lib/anthropic`. So the judge test only needs to mock `@/lib/anthropic` (or stub `anthropicProvider`), NOT `@ai-sdk/anthropic` directly. This is cleaner than mocking `@ai-sdk/anthropic`'s `createAnthropic` shape and avoids the constructor-vs-factory trap (Phase 3 Plan 03-00 STATE.md note about Exa class mocks).
- **Existing extractor:** `extractAnthropicCost` in cost.ts is Sonnet-priced and consumed by the chat-path cost contract. DO NOT repoint it to Haiku. Add a new `extractAnthropicJudgeCost` for the judge path.
- **`extractGoogleCost` retention:** `projectRunCost` still references `gemini_input/gemini_output` constants in its model. Leave the constants and the extractor in place; nothing currently breaks if `extractGoogleCost` becomes unused at runtime, but removing it would force a `projectRunCost` rewrite that's out of scope. Just delete the import from `judge.ts` — leave `cost.ts` exports intact.
- **`@ai-sdk/google` package retention:** the npm dependency stays in package.json (still imported by nothing after this plan, but removing it is a separate clean-up — out of scope and risks an unrelated lockfile churn).
</key-conventions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip judge wrapper to Anthropic Haiku 4.5 + add Haiku-priced cost extractor</name>
  <files>
    src/lib/eval/judge.ts,
    src/lib/eval-models.ts,
    src/lib/eval/cost.ts,
    tests/lib/eval/cost.test.ts,
    tests/lib/eval-models.test.ts
  </files>
  <behavior>
    `tests/lib/eval/cost.test.ts` (new test cases — RED first):
    - `extractAnthropicJudgeCost` exists and is exported from `@/lib/eval/cost`.
    - Pricing: 1M input @ $1.00 + 1M output @ $5.00 = $6.00 = 600 cents.
    - Typical judge call (1500 in / 200 out) ≈ 0.25¢ → rounds to 0 cents (sub-cent), but a 1M-out smoke aggregates correctly.
    - Treats missing `inputTokens` / `outputTokens` fields as zero.
    - Does NOT consume `cacheReadInputTokens` / `cacheCreationInputTokens` (Haiku judge calls aren't cached at this scale; min cache block is 4096 tokens > our judge prompts).
    - Existing `extractAnthropicCost` Sonnet-priced behavior unchanged (1M+1M = 1800 cents) — guard test.
    - Existing `extractGoogleCost` behavior unchanged — guard test.

    `tests/lib/eval-models.test.ts` (existing test cases updated):
    - `JUDGE_MODEL_SNAPSHOT` is now the literal string `'claude-haiku-4-5-20251001'` (dated snapshot — Pitfall 4 fix).
    - `JUDGE_PROVIDER` is now the literal `'anthropic'`.
    - `JUDGE_MODEL` env-override semantics unchanged: `EVAL_JUDGE_MODEL` overrides snapshot; empty string falls through; unset uses snapshot.
    - The env-override test that previously stubbed `'gemini-2.5-pro'` should be updated to stub a plausible alternate Anthropic model id (e.g. `'claude-haiku-4-5'` alias) — keeps the override-mechanism test honest without re-importing Gemini ids.
  </behavior>
  <action>
    1. **`src/lib/eval/cost.ts`** — Add `extractAnthropicJudgeCost` AFTER `extractAnthropicCost`:
       ```typescript
       /** Extract cost from a Haiku-judge usage object (AI SDK Anthropic camelCase shape).
        * Uses Haiku 4.5 pricing ($1/$5 per MTok). Distinct from `extractAnthropicCost`
        * which is Sonnet-priced and consumed by the chat-path cost contract. */
       export function extractAnthropicJudgeCost(usage: {
         inputTokens?: number;
         outputTokens?: number;
       }): number {
         const input = (usage.inputTokens ?? 0) / 1_000_000;
         const output = (usage.outputTokens ?? 0) / 1_000_000;
         const dollars = input * PRICES.haiku_input + output * PRICES.haiku_output;
         return Math.round(dollars * 100);
       }
       ```
       Leave `PRICES`, `extractAnthropicCost`, `extractGoogleCost`, `projectRunCost`, `WARN_THRESHOLD_CENTS` UNCHANGED. The `haiku_input`/`haiku_output` constants already exist.

    2. **`src/lib/eval-models.ts`** — Flip the constants:
       ```typescript
       export const JUDGE_PROVIDER = 'anthropic' as const;
       export const JUDGE_MODEL_SNAPSHOT = 'claude-haiku-4-5-20251001';
       export const JUDGE_MODEL = process.env.EVAL_JUDGE_MODEL || JUDGE_MODEL_SNAPSHOT;
       ```
       Update the file's leading comment block to reflect the swap rationale (snapshot/rate/schema — three forces, one fix). Keep the `EVAL_COST_WARN_USD` block UNCHANGED.

    3. **`src/lib/eval/judge.ts`** — Three surgical edits:
       - Replace `import { google } from '@ai-sdk/google';` with `import { anthropicProvider } from '@/lib/anthropic';`
       - Replace `import { extractGoogleCost } from './cost';` with `import { extractAnthropicJudgeCost } from './cost';`
       - In all three `generateObject` call-sites, swap `model: google(JUDGE_MODEL)` for `model: anthropicProvider(JUDGE_MODEL)` and swap `extractGoogleCost(usage)` for `extractAnthropicJudgeCost(usage)`.
       - Update file header comment: replace "Gemini 2.5 Flash judge wrapper via @ai-sdk/google" with "Claude Haiku 4.5 judge wrapper via @ai-sdk/anthropic (shared `anthropicProvider` factory from src/lib/anthropic.ts; main chat path stays on Sonnet 4.6)". Keep the rest of the docstring intact (RESEARCH §15 / §14 references still valid — judge math is provider-agnostic).
       - `estimateJudgeCost` returns `1` (1 cent rounded up). Keep value at `1` — Haiku at 1500 in / 200 out is ~0.25¢, still rounds up to 1¢ in this coarse projector. Update the inline arithmetic comment to reflect Haiku math.

    4. **`tests/lib/eval/cost.test.ts`** — Add a new `describe('extractAnthropicJudgeCost', () => { ... })` block matching the behavior above. Import `extractAnthropicJudgeCost` from `@/lib/eval/cost`.

    5. **`tests/lib/eval-models.test.ts`** — Update lines 17-25 (snapshot assertion comment + value), line 33 (env-override test value), and line 41 (`JUDGE_PROVIDER` literal). Replace the "Pitfall 4 GAP" comment with a one-line note explaining this is the dated-snapshot fix and pointing to the quick-task ID.

    Run `npm test` after each step. All tests should pass before moving to Task 2 — judge.test.ts is still mocking `@ai-sdk/google` at this point, but its `generateObject` mock is provider-agnostic (the call-site now imports `anthropicProvider` instead of `google`, but the mock at the `ai` package level still intercepts the call). Confirm green before Task 2.
  </action>
  <verify>
    <automated>npm test -- tests/lib/eval/cost.test.ts tests/lib/eval-models.test.ts</automated>
  </verify>
  <done>
    - `extractAnthropicJudgeCost` exported from `@/lib/eval/cost`; existing extractors unchanged.
    - `JUDGE_PROVIDER === 'anthropic'`, `JUDGE_MODEL_SNAPSHOT === 'claude-haiku-4-5-20251001'`.
    - `judge.ts` imports `anthropicProvider` and `extractAnthropicJudgeCost`; no `@ai-sdk/google` references remain in the file.
    - cost.test.ts adds 4 new `extractAnthropicJudgeCost` cases, all green.
    - eval-models.test.ts updated assertions all green.
    - Atomic commit: `refactor(260509-r39): Task 1 — flip judge to Haiku 4.5 + Haiku-priced cost extractor`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update judge.test.ts to mock Anthropic provider, not Google</name>
  <files>tests/lib/eval/judge.test.ts</files>
  <behavior>
    - Mock target swaps from `@ai-sdk/google` to `@/lib/anthropic` (mock the pre-wired `anthropicProvider` export — see key-conventions; cleaner than mocking the SDK package).
    - The `ai` package `generateObject` mock stays unchanged (provider-agnostic; same `{ object, usage }` return shape).
    - All four existing describe blocks (`judgeFactualFidelity`, `judgeVoiceFidelity`, `judgePersona`, `estimateJudgeCost`) still pass with their existing assertions on score/verdict/rationale shape.
    - `cost_cents` assertions are now driven by `extractAnthropicJudgeCost` (Haiku-priced). Add an exact-cents assertion in at least one case to catch a future cost-extractor regression: e.g. `inputTokens: 1_000_000, outputTokens: 1_000_000` → `cost_cents === 600`.
    - Error-wrapping tests (`/judgeFactualFidelity failed for case=cat1-case-fail/`, `/judgePersona failed for case=cat3-err/`) still pass — error wrapping is provider-agnostic.
  </behavior>
  <action>
    1. **Replace** the `vi.mock('@ai-sdk/google', ...)` block with a mock of `@/lib/anthropic`:
       ```typescript
       vi.mock('@/lib/anthropic', () => ({
         anthropicProvider: (model: string) => ({ __mock: 'anthropic', model }),
         // MODELS export not needed by judge.ts but exported for consumers that import the module
         MODELS: { MAIN: 'claude-sonnet-4-6', CLASSIFIER: 'claude-haiku-4-5' },
       }));
       ```

    2. **Strengthen** the `judgeFactualFidelity` cost assertion. Current shape is `expect(typeof result.cost_cents).toBe('number')` — replace with two assertions:
       - `expect(typeof result.cost_cents).toBe('number')` (kept)
       - For a `{ inputTokens: 1500, outputTokens: 200 }` mock, expect `result.cost_cents` ≥ 0 and ≤ 1 (Haiku math: 1500*$1/MTok + 200*$5/MTok = $0.0025 → 0¢ rounded). One additional case with 1M+1M tokens asserting `cost_cents === 600` to lock the extractor wiring.

    3. **Update** the env mock comment block — the `GOOGLE_GENERATIVE_AI_API_KEY` stub is still harmless to keep (env mock is a superset; doesn't drive the test), but optionally drop it to reduce confusion. Keeping it is fine.

    4. **Drop** the `'Gemini quota exceeded'` literal in the `judgeFactualFidelity` error test — replace with a generic message like `'Anthropic 529 overloaded'` to keep the test tone consistent with the new provider. The error-wrapping shape (`/judgeFactualFidelity failed for case=cat1-case-fail/`) is what's asserted; the inner text is illustrative.

    Run the file: `npm test -- tests/lib/eval/judge.test.ts`. All 5 cases green.
  </action>
  <verify>
    <automated>npm test -- tests/lib/eval/judge.test.ts</automated>
  </verify>
  <done>
    - judge.test.ts mocks `@/lib/anthropic` (not `@ai-sdk/google`).
    - One case asserts exact Haiku-priced cents (600 for 1M+1M).
    - All 5 existing test cases green.
    - Full `npm test` run also green (sanity — judge wiring is consumed by cat1.ts/cat2.ts/etc but those use mocks too).
    - Atomic commit: `test(260509-r39): Task 2 — judge.test.ts mocks Anthropic provider`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live cat1 smoke against localhost — verify zero schema-mismatch and zero rate-limit errors</name>
  <files>(no code changes — observation-only checkpoint)</files>
  <action>
    Joe runs the cat1-only smoke against a local dev server and reports back. This is a `checkpoint:human-verify` — Claude does NOT auto-resume; the executor pauses and waits for the resume signal below. Steps below are formatted as both `<action>` (validator-required) and `<how-to-verify>` (execute-plan checkpoint contract — the same content, intentionally duplicated so both downstream readers find it).

    1. Confirm `.env.local` has `ANTHROPIC_API_KEY` set (already required for the main chat path — should already be present).
    2. Start dev server in a separate shell: `npm run dev` (default localhost:3000).
    3. Wait until the dev server logs `Ready in Xs` and `/api/health` returns 200.
    4. In a second shell, run cat1 only (15 cases — the cat that previously had 5 schema-mismatch + 3 rate-limit errors against Gemini):
       ```
       EVAL_CATS=cat1 EVAL_TARGET_URL=http://localhost:3000 npm run eval
       ```
    5. Wait for completion. Expected duration: ~2-3 minutes (15 cases × ~10s each, no rate-limit throttling needed).
    6. Inspect the run output:
       - **MUST:** Zero occurrences of `No object generated: response did not match schema` (schema-mismatch — was 3-5/15 against Gemini).
       - **MUST:** Zero occurrences of `429` / `Too Many Requests` / `RATE_LIMIT_EXCEEDED` (rate-limit — was 3/15 against Gemini).
       - **MUST:** All 15 cases produce a verdict (pass OR fail; the *content* of the verdict is downstream Plan 05-04 territory — item #4 in deferred-items.md, still open).
       - **SHOULD:** Aggregate cost_cents reported by the runner is plausible (Haiku judge: ~15 calls × 0.25¢ ≈ 4¢ for cat1 alone; total run cost should be well under WARN_THRESHOLD_CENTS=150).
    7. Capture the runId from the run output (printed at start as `runId: ...`).
    8. **Pass-rate is NOT the gate here.** Pass-rate (silent fails / det allowlist calibration) is item #4 in deferred-items.md and remains open. This checkpoint only verifies that the *judge infrastructure* is healthy.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual smoke; resume-signal gates Task 4"</automated>
  </verify>
  <done>
    Joe replies with one of the resume-signal options below. Task 4 only proceeds on `approved runId=...`.
  </done>
  <what-built>
    Eval LLM judge swapped from Gemini 2.5 Flash to Claude Haiku 4.5 (`claude-haiku-4-5-20251001`). Three judge variants (factual fidelity, voice fidelity, persona) now route through `anthropicProvider` from `src/lib/anthropic.ts`. Cost extraction uses Haiku-specific pricing ($1/$5 per MTok). Test suite at full green via `npm test`.
  </what-built>
  <how-to-verify>
    See the numbered steps in `<action>` above. Same content; duplicated here for the execute-plan checkpoint contract.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved runId=<runId>` — both gate criteria met (zero schema mismatches AND zero rate-limit 429s); proceed to Task 4 (close-out deferred items).
    - `partial: schema-mismatch=<N> rate-limit=<N> runId=<runId>` — judge is healthier but not 100%; share the run output excerpts and Joe will decide whether to ship anyway or iterate.
    - `failed: <description>` — describe what broke; planner re-evaluates.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Close out Phase 5 deferred-items.md (#1 snapshot pin, #2 rate-limit, #3 schema flakiness)</name>
  <files>.planning/phases/05-eval-gates-launch/deferred-items.md</files>
  <action>
    Conditional on Task 3 returning `approved` (zero schema mismatches AND zero rate-limit 429s):

    1. Append a new top-level section after the existing `## Quick task 260509-q00 follow-ups` heading, BEFORE the four sub-headings:

       ```markdown
       **Status (2026-05-09 — quick task 260509-r39):** Items #1 (snapshot pin),
       #2 (rate-limit), #3 (schema flakiness) are RESOLVED by swapping the judge
       to Anthropic Haiku 4.5 (`claude-haiku-4-5-20251001`). Live cat1 smoke
       runId=<RUNID_FROM_TASK_3> at 2026-05-09 showed zero schema-mismatch and
       zero rate-limit errors. Item #4 (silent-fail inspection — disambiguate
       fabrication detections from allowlist over-strictness) remains OPEN as
       calibration work for Plan 05-04 follow-up.
       ```
       Replace `<RUNID_FROM_TASK_3>` with the runId returned in Task 3's resume signal.

    2. Mark each of the three sub-headings inline with a status banner under the existing **Severity:** line:

       ```markdown
       **Status:** RESOLVED 2026-05-09 (quick task 260509-r39 — judge swapped to
       Anthropic Haiku 4.5; live verify runId=<RUNID>).
       ```

       Insert this line for:
       - `### Snapshot pin broken — Gemini judge model not found`
       - `### Gemini free-tier rate limit — 5 RPM cap hit on cat1`
       - `### Structured-output schema mismatch — Gemini 2.5 Flash returns malformed JSON ~33% of the time`

       Do NOT modify:
       - `### Silent-fail inspection — disambiguate fabrication detections from allowlist over-strictness` (item #4 — still open).
       - `## Pre-existing TypeScript error in src/components/ChatUI.tsx` (different scope).
       - `## Live full-suite eval smoke (Plan 05-04 Task 4 + Plan 05-07 Task 4)` — note this entry references Gemini key as the blocker, which is now obsolete; add a follow-up paragraph at the end of that section reading:
         ```markdown
         **Update 2026-05-09 (quick task 260509-r39):** This blocker is partially
         dissolved — the Gemini key is no longer required because the judge now
         calls Anthropic (already provisioned for the main agent). Plan 05-04 +
         Plan 05-07 Task 4 live-verify is unblocked at the infra level.
         Per-cat pass-rate calibration (item #4 above) is now the remaining gate
         to closing both Task 4 deferrals.
         ```

    3. Verify the file still parses cleanly (open in editor; no markdown nesting issues).

    Atomic commit: `docs(260509-r39): Task 4 — close deferred items #1-3 after Haiku judge swap`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - deferred-items.md items #1, #2, #3 each carry a `**Status:** RESOLVED 2026-05-09 (quick task 260509-r39 — ...)` line with the Task 3 runId.
    - Item #4 unchanged (still open).
    - Plan 05-04 / 05-07 Task 4 deferral entry has an update note about the Gemini key no longer blocking.
    - `npm test` still green (full suite, sanity).
    - Atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval runner → Anthropic API | judge calls cross to Anthropic with full prompt + ground-truth facts; same trust posture as the chat path (already established) |
| eval runner → localhost:3000 dev server | local-only smoke (Task 3); not internet-reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r39-01 | Information Disclosure | judge prompts contain ground-truth facts about Joe (kb/resume.md content) sent to Anthropic | accept | Same posture as chat path; Anthropic's data-handling policy already approved by user for main agent. No PII beyond what's already in the public KB. |
| T-r39-02 | Tampering | `extractAnthropicJudgeCost` accidentally repointed to Sonnet pricing would 3x under-count main chat cost OR 3x over-count judge cost | mitigate | New extractor is a separate function; existing `extractAnthropicCost` unchanged; cost.test.ts asserts exact cents for both extractors with distinct expected values (1800 vs 600 for 1M+1M) — repointing trips the test. |
| T-r39-03 | Repudiation | model snapshot drift hides regressions in eval signal | mitigate | `JUDGE_MODEL_SNAPSHOT='claude-haiku-4-5-20251001'` is dated; Anthropic publishes numbered snapshots. eval-models.test.ts asserts the literal string. PR audit trail intact. |
| T-r39-04 | Denial of Service | runaway eval calls during dev consume Anthropic budget | accept | Same daily-spend-cap (`$3/day` from CLAUDE.md) covers eval calls. Cat1 smoke = ~4¢. Existing alarm path (Plan 04-06) already monitors. |
| T-r39-05 | Elevation of Privilege | None — judge runs server-side with same key surface as chat path | accept | No new key surface introduced. |
</threat_model>

<verification>
**Phase-level checks (run after Task 4 commits):**

1. `npm test` — full vitest run green.
2. `grep -r "@ai-sdk/google" src/` — should match ZERO files (judge no longer imports it; package stays in package.json deliberately, see key-conventions).
3. `grep -rn "claude-haiku-4-5-20251001" src/lib/eval-models.ts` — exactly one match.
4. `grep -rn "extractAnthropicJudgeCost" src/` — at least 2 matches (cost.ts export + judge.ts import).
5. `grep -rn "extractAnthropicCost" src/` — unchanged from pre-plan baseline (chat-path cost path untouched).
6. `git log --oneline -5` — shows 4 atomic commits matching the task names.
</verification>

<success_criteria>
- `JUDGE_PROVIDER === 'anthropic'` and `JUDGE_MODEL_SNAPSHOT === 'claude-haiku-4-5-20251001'`.
- `src/lib/eval/judge.ts` no longer imports from `@ai-sdk/google`.
- `extractAnthropicJudgeCost` exists in `cost.ts`; existing `extractAnthropicCost` (Sonnet) and `extractGoogleCost` are byte-identical to pre-plan baseline.
- `npm test` exits 0; ≥ 480 total tests passing (was 416 at HEAD; 4-5 new cost-extractor cases added in Task 1, no removals).
- Live cat1 smoke against `http://localhost:3000` shows zero `No object generated` errors and zero `429`/`RATE_LIMIT` errors across 15 cases.
- deferred-items.md items #1, #2, #3 each have a `**Status:** RESOLVED ...` line referencing the Task 3 runId.
- Item #4 (silent-fail inspection) is still flagged as open.
- Main chat path unchanged: `/api/chat` still uses Sonnet 4.6 via the same `anthropicProvider`; classifier still uses Haiku 4.5 alias via the direct SDK; no `tests/lib/route/*` test changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260509-r39-swap-eval-judge-to-claude-haiku-4-5/260509-r39-SUMMARY.md` capturing:
- The four atomic commits (SHA + subject for each).
- Task 3 runId and excerpts confirming zero schema mismatches + zero rate-limits.
- The pre-vs-post test count delta.
- A note that item #4 (silent-fail inspection) remains open and is the last gate before Plan 05-04 / 05-07 Task 4 can close.
</output>
