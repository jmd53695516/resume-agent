---
status: complete
phase: 05.1-eval-content-trust-restoration
source: [05.1-01-SUMMARY.md]
started: 2026-05-10T19:00:00Z
updated: 2026-05-10T19:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Rate-limit reset script clears Upstash keys
expected: `npm run eval:reset-rl` exits 0 and prints `cleared N/M keys` (sliding-window expansion via redis.keys('<prefix>:<id>:*')). No env-validation errors.
result: pass

### 2. System-prompt vitest includes Item #8 assertion
expected: `npx vitest run tests/lib/system-prompt.test.ts` → 17/17 tests pass (was 16; +1 premise-smuggling rule assertion).
result: pass

### 3. cat1-fab-005 regression test guards rule + counter-facts
expected: `npx vitest run tests/lib/eval/cat1-fab-005-regression.test.ts` → 3/3 tests pass in <1s (asserts premise-smuggling rule + Snowflake-employer + 30-vs-200 team-size counter-facts present in buildSystemPrompt() output).
result: pass

### 4. Full vitest suite green including deflection-skip paths
expected: `npm test` → 541/541 tests pass across 59 test files; cat1_case_skipped_deflection + cat3_case_skipped_deflection log events fire during their fixture tests.
result: pass
note: User observed 544/544 across 59 files (3 more than SUMMARY-time count — additional tests landed since 05.1 close; treat as expected drift, not regression).

### 5. TypeScript strict + Next.js production build clean
expected: `npx tsc --noEmit` exits 0 with no errors; `npm run build` succeeds and compiles all 16 routes (the /api/chat route emits the new data-deflection chunk without breaking strict tsc).
result: pass
note: Next.js 16.2.4 (Turbopack) — compiled in 5.9s; TypeScript finished in 5.9s; 24/24 routes generated (8 more than SUMMARY-time 16; admin sub-routes + cron handlers added since 05.1 close; expected drift, not regression). Strict tsc clean.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
