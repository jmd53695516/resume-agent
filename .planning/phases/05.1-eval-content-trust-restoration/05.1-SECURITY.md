---
phase: 05.1
slug: eval-content-trust-restoration
status: verified
threats_open: 0
asvs_level: 1
created: 2026-05-10
---

# Phase 05.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Eval CLI → /api/chat | Eval CLI is local-dev / CI tooling; /api/chat is the production-shape endpoint. The new transient data-deflection chunk crosses this boundary. | Server-emitted enum reason (7 values: injection / offtopic / sensitive / borderline / ratelimit / spendcap / turncap) — no user data |
| Reset script → Upstash Redis | Local script directly mutates production-shape Redis state. Blast radius confined to known eval-CLI key prefixes. | Eval-CLI rate-limit keys only (`::1`, `127.0.0.1`, `dev` ipKeys + `eval-cli@joedollinger.dev` email) |
| Counter-facts YAML → cached system prompt | kb/profile.yml is in-repo, version-controlled, only edited by Joe. New counter_facts section becomes part of the cached system prompt visible to Sonnet. | Static system-prompt text (no PII, no credentials) |
| Working-state pin files → planning artifacts | `.pre-phase-sha` and `.cat3-baseline` live under `.planning/phases/...` but are gitignored and `rm`-ed in Task 4 PART F. Their VALUES (a SHA, an integer) are referenced from inside SUMMARY.md text only. | Non-secret: public git SHA + integer (0..6) |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-05.1-01 | Tampering | scripts/reset-eval-rate-limits.ts | mitigate | Explicit `PREFIX = 'resume-agent'` (line 30), bounded `LOCAL_IP_KEYS = ['::1', '127.0.0.1', 'dev']` (line 35), explicit `EMAIL = 'eval-cli@joedollinger.dev'` (line 53). Iteration bounded to 4 sub-prefixes (`ip10m`, `ipday`, `ipcost`, `emailday`) at lines 73-83 — no globs across unrelated namespaces. | closed |
| T-05.1-02 | Elevation of Privilege | Item #6 mechanism | mitigate | D-E-01 satisfied: src/app/api/chat/route.ts has no env-var read or API branch for reset. scripts/reset-eval-rate-limits.ts:23-24 declares offline-only. package.json:12 invokes via `node --env-file-if-exists=.env.local --import tsx` — no `/api/chat` runtime branch. | closed |
| T-05.1-03 | Information Disclosure | data-deflection sideband chunk | accept | `transient: true` flag present at route.ts:99; node_modules/ai/dist/index.d.ts:2151-2158 confirms transient excludes chunk from `useChat` message rebuild. Reason payload bounded to `DeflectionReason` enum (route.ts:85). No user input flows into chunk data. | closed |
| T-05.1-04 | Tampering | counter_facts: YAML | mitigate | kb/profile.yml:147-169 contains the counter_facts block. src/lib/system-prompt.ts:60-70 compiles KB into cached prompt server-side at module init. File is in-repo, version-controlled, edited via GSD workflow. Content is static system-prompt text — no user-input injection vector. | closed |
| T-05.1-05 | Spoofing | data-deflection chunk forgery by Sonnet | accept | `data-deflection` chunk is server-emitted only inside `deflectionResponse()` (route.ts:97-102). Sonnet's streamText path emits `text-delta` chunks via `result.toUIMessageStreamResponse()` (route.ts:458), never `data-${NAME}` chunks. Eval CLI parser (agent-client.ts:79-86) discriminates by `type === 'data-deflection'` — text-delta payloads cannot cross-channel forge. | closed |
| T-05.1-06 | Denial of Service | Reset script malicious invocation | accept | Script is local-only, requires Upstash credentials in `.env.local` (`env.ts` zod-parses at module init; missing creds throws before any redis call). Production user keys use real client IPs from Vercel `ipAddress(req)` (route.ts:216) — different ipKey shape from the eval-CLI literals, so iteration set never touches them. | closed |
| T-05.1-07 | Information Disclosure | `.pre-phase-sha` + `.cat3-baseline` ephemeral pin files | accept | .gitignore:65-70 contains all three patterns (`.pre-phase-sha`, `.cat3-baseline`, `.cat3-baseline-detail`) with documenting comment. Task 4 PART F rm-ed the files (current `git status` shows no tracked or untracked pin files). Contents are non-secret (public SHA + integer 0..6) — zero PII, zero credentials. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-05.1-01 | T-05.1-03 | Transient data-deflection chunk emits a bounded enum reason (7 values), never user data. AI SDK v6 `transient: true` flag (node_modules/ai/dist/index.d.ts:2151-2158) excludes the chunk from `useChat`'s message-rebuild path — never lands in `messages` table or client-side history. Eval CLI consumes server-to-server. Benefit (eval signal trustworthiness) > risk (LOW). | Joe Dollinger | 2026-05-10 |
| AR-05.1-02 | T-05.1-05 | Cross-channel forgery is structurally impossible: Sonnet output flows through `text-delta` typed chunks; eval CLI parser discriminates by chunk `type`. Even if Sonnet output contained the literal string `"data-deflection"` in a text-delta `delta` field, the parser ignores it. Risk = NIL. | Joe Dollinger | 2026-05-10 |
| AR-05.1-03 | T-05.1-06 | Reset script requires `.env.local` Upstash credentials. Worst-case attacker with `.env.local` access has bigger problems. Blast radius bounded to eval-CLI ipKey shapes (`::1`, `127.0.0.1`, `dev`) + one explicit email — production user keys use real client IPs and are not in the iteration set. | Joe Dollinger | 2026-05-10 |
| AR-05.1-04 | T-05.1-07 | Pin files contain only public, non-secret data (a public git SHA already in `git log` + an integer 0..6 pass count). Both files are gitignored from creation (Task 1 PART 0 step 0 added the patterns BEFORE the files were written) and `rm`-ed in Task 4 PART F. Even on accidental commit, no PII, no credentials, no attacker leverage. | Joe Dollinger | 2026-05-10 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-10 | 7 | 7 | 0 | gsd-security-auditor (claude-sonnet-4-6) |

### Audit Notes (2026-05-10)

- Result: **SECURED** — all 7 threats verified closed.
- ASVS level: L1; block_on: high,critical (no high/critical findings).
- Auditor flagged one documented widening (`EVAL_RESET_IP` env var at scripts/reset-eval-rate-limits.ts:36-51 lets operator add additional IPs to the iteration set). Bounded by the same per-ip key-pattern shape; operator must explicitly opt in. Does not invalidate T-05.1-01's "no globs across unrelated namespaces" invariant.
- Auditor flagged one immaterial SUMMARY/code discrepancy (decisions[5] says role_negatives has 3 entries; kb/profile.yml has 2). Total counter_facts entry count is 10 either way. Not a security issue.
- No `## Threat Flags` section in SUMMARY.md; no unregistered flags surfaced.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-05-10
