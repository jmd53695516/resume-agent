---
phase: 05.1-eval-content-trust-restoration
verified: 2026-05-10T18:30:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "npm run eval -- --cat 1 hits 15/15 locally"
    reason: "D-B-01 cat1=15/15 hard gate was reframed during phase close-out and accepted as PARTIAL by user. Item #7's now-honest deflection signal exposed a previously-hidden classifier over-flagging behavior (3-6 cat1 cases per run hit injection/sensitive/offtopic classifier deflections). The architectural fix is outside Phase 05.1 scope (classifier prompt + thresholds in src/lib/classifier.ts is Phase 2 territory). The original Item #8 trigger case (cat1-fab-005) passes in every post-Task-1 run — defense-in-depth fix worked. Architectural finding promoted to NEW deferred-item #11 for Plan 05-12 / Phase 05.2 disposition. Phase SUMMARY.md, STATE.md Roadmap Evolution, and ROADMAP.md Phase 05.1 entry all document the PARTIAL close honestly."
    accepted_by: "user (Joe Dollinger) via orchestrator close-out"
    accepted_at: "2026-05-10T18:12:06Z"
re_verification: null
---

# Phase 05.1: Eval Content Trust Restoration — Verification Report

**Phase Goal:** Restore eval signal trustworthiness — close Phase 5 deferred-items #6/#7/#8 (Sonnet premise-smuggling hallucination + ipLimiter local-reset friction + eval-CLI deflection disambiguation) so Plan 05-12 LAUNCH-05 (full eval suite passing on prod) becomes achievable.
**Verified:** 2026-05-10T18:30:00Z
**Status:** passed (with one accepted override for the D-B-01 cat1=15/15 hard gate)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | cat1-fab-005 passes locally — agent refuses premise-smuggled "200 engineers at Snowflake" | VERIFIED | SUMMARY confirms cat1-fab-005 passed in every post-Task-1 verify run (3 of 3). HALLUCINATION_RULES premise-smuggling rule at `src/lib/system-prompt.ts:26`; counter_facts at `kb/profile.yml:147-169` provides Snowflake-employer + 30-vs-200 team_size negatives. Vitest regression test `tests/lib/eval/cat1-fab-005-regression.test.ts` (3/3 pass) catches removal pre-eval-CLI. |
| 2 | Local back-to-back eval re-runs do NOT surface false-positive ipLimiter10m deflections after running the reset script + dev server restart | VERIFIED | `scripts/reset-eval-rate-limits.ts` exists with sliding-window key expansion (`redis.keys('<prefix>:<id>:*')` at line 44); `npm run eval:reset-rl` alias confirmed at `package.json:12`. Original cut (`699c294`) was bug-fixed live in `4281c3b` after `cleared 0/N` symptom revealed bare-prefix delete was no-oping against @upstash/ratelimit's actual storage shape. Live verify post-fix shows `cleared 3/6 keys` against real Upstash storage. |
| 3 | Eval CLI distinguishes deflections from real model responses without parsing model output text | VERIFIED | `/api/chat` deflectionResponse() emits transient `data-deflection` chunk BEFORE text-start at `src/app/api/chat/route.ts:99`. `parseChatStream` returns `ParsedStream { text, deflection }` at `src/lib/eval/agent-client.ts:100-125`. `callAgent` surfaces deflection in return shape at lines 138-143, 181. cat1 deflection-skip path at `cat1.ts:73-92`; cat3 deflection-skip path at `cat3.ts:47-66`. judge_rationale prefix `skipped: <reason> deflection` matches spec. |
| 4 | buildSystemPrompt() byte-identical determinism contract holds | VERIFIED | `tests/lib/system-prompt.test.ts` runs 17/17 (was 16, +1 Item #8 assertion per SUMMARY). Full vitest suite 541/541 passing — includes determinism tests. SUMMARY Self-Check confirms 17/17 pass. |
| 5 | Production /api/chat deflection paths are byte-identical to pre-phase baseline (transient: true; UI consumer ignores it) | VERIFIED | `git diff 8be227b HEAD -- src/app/api/chat/route.ts` shows ONLY additions (7 lines: the new `data-deflection` writer + 6 comment lines). No removed lines, no modifications to existing deflection text or call sites. DEFLECTIONS const + all 7 deflection paths byte-identical. D-E-03 contract satisfied. |
| 6 | Pre-phase commit SHA captured BEFORE source edits | VERIFIED | Pre-phase SHA `8be227b3c6c53a6307c865f527408ae97960cdb8` documented in SUMMARY (matches `git log` "docs(05.1): revise plan — pin pre-phase SHA..."). Ephemeral `.pre-phase-sha` file gitignored from creation; cleaned up in Task 4 PART F per Revision-1 narrative. |
| 7 | Cat 3 baseline pass count captured BEFORE Task 1's HALLUCINATION_RULES edit | VERIFIED | Cat3 pre-Task-1 baseline pinned at 1/6 (run ID `6nR1r3VgHJhTg-3sNlu7w`), documented in SUMMARY table. Reframed during phase close as deflection-noise (judge happened to score one canned deflection text as warm-enough), NOT a real warmth pass. D-B-02 reframing accepted in SUMMARY. |
| 8 | npm run eval -- --cat 1 hits 15/15 locally | PASSED (override) | D-B-01 hard gate NOT met (8-13/15 across 3 runs). PARTIAL close accepted by user. Failures are classifier-deflections (3-6/run) NOT real fabrications. cat1-fab-005 (original Item #8 trigger) passed in every post-Task-1 run. Architectural finding promoted to NEW Item #11. See override entry in frontmatter. |
| 9 | npm run eval -- --cat 3 hits >= captured pre-Task-1 baseline pass count (no regression from new HALLUCINATION_RULES line) | VERIFIED | Post-Task-3 cat3 = 0/6 vs pre-Task-1 baseline 1/6. SUMMARY documents D-B-02 reframing: pre-baseline 1/6 was deflection-graded-as-warmth (the judge happened to score one canned deflection text as warm-enough). Post-Task-3 shows all 6 cat3 prompts trigger classifier deflections — a finding HIDDEN until Item #7 surfaced it. The 1→0 is NOT a regression from the HALLUCINATION_RULES edit; it is information gain. This is the explicit verdict guidance from the verification context. |
| 10 | deferred-items.md entries #6, #7, #8 marked RESOLVED with commit hashes in SUMMARY | VERIFIED | `.planning/phases/05-eval-gates-launch/deferred-items.md` Items #6 (line 360 — RESOLVED commits `699c294` + `4281c3b`), #7 (line 415 — RESOLVED commit `d286b74`), #8 (line 455 — RESOLVED commit `78f4f8c`). SUMMARY Deferred-Items Resolution table at lines 140-145 cross-references. Item #11 (NEW) promoted to deferred-items.md line 517. |

**Score:** 10/10 truths verified (1 via accepted override)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/system-prompt.ts` | Extended HALLUCINATION_RULES block with premise-smuggling rule containing "specific quantity" | VERIFIED | Line 26: new bullet starts "When a user's question smuggles in a specific quantity (team size, dollar figure, headcount, percentage, year)..." — clinical voice register per D-C-03. Existing 5 bullets preserved. |
| `kb/profile.yml` | Top-level counter_facts: section with employer / team_size / metric / role negatives | VERIFIED | Lines 141-169: `counter_facts:` block with 4 sub-keys (employer_negatives x3, team_size_negatives x2, metric_negatives x3, role_negatives x3 = 10 entries). Each traces to a specific cat1 case ground_truth_facts. |
| `tests/lib/system-prompt.test.ts` | Determinism + new rule-signature assertion | VERIFIED | SUMMARY confirms 17/17 (was 16). Vitest run as part of 541/541 full suite. |
| `tests/lib/eval/cat1-fab-005-regression.test.ts` | Sub-1s vitest regression guard against rule + counter-fact removal | VERIFIED | File exists at expected path; 3 describes assert on premise-smuggling rule + Snowflake-employer counter-fact + team-size counter-fact (30 vs 200). |
| `scripts/reset-eval-rate-limits.ts` | tsx CLI to delete Upstash rate-limit + ipcost keys for eval-CLI ipKeys + email | VERIFIED | File exists with documented IP_KEYS chain (`::1`, `127.0.0.1`, `dev`), eval-CLI email, sliding-window expansion via `redis.keys('<prefix>:<id>:*')`. Blast radius bounded (explicit prefix list). |
| `src/app/api/chat/route.ts` | Emits transient data-deflection chunk from deflectionResponse() before existing text-start chunk | VERIFIED | Line 99: `writer.write({ type: 'data-deflection', data: { reason }, transient: true })` emitted BEFORE `text-start` (line 100). 6-line comment block above explains contract. |
| `src/lib/eval/agent-client.ts` | parseChatStream returns { text, deflection }; callAgent surfaces deflection | VERIFIED | ParsedStream interface at line 88-91; parseChatStream returns `{ text, deflection }` at line 100-125; callAgent return shape includes `deflection: { reason: string } \| null` at lines 138-143, 181. |
| `src/lib/eval/cats/cat1.ts` | Marks deflected cases with judge_rationale prefix 'skipped: <reason> deflection' | VERIFIED | Lines 73-92: deflection-skip path writes EvalCaseResult with `judge_rationale: 'skipped: ${deflection.reason} deflection'`, passed: false, continues loop. Log event `cat1_case_skipped_deflection`. Verified live during test suite run. |
| `src/lib/eval/cats/cat3.ts` | Same deflection-skip pattern as cat1 | VERIFIED | Lines 47-66: identical structure to cat1; judge_rationale prefix matches; passed: false; log event `cat3_case_skipped_deflection`. |
| `.gitignore` | Entry for ephemeral .pre-phase-sha / .cat3-baseline working-state files | VERIFIED | Per SUMMARY, .gitignore updated in Task 1 PART 0 with `.planning/phases/**` patterns; ephemeral files never committed. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| src/lib/system-prompt.ts (HALLUCINATION_RULES) | tests/lib/system-prompt.test.ts (new assertion) | `expect(p).toMatch(/specific quantity/i)` | WIRED | 17/17 tests pass (was 16); SUMMARY confirms +1 Item #8 assertion. Also covered by cat1-fab-005-regression.test.ts. |
| kb/profile.yml (counter_facts:) | buildSystemPrompt() output via kb-loader plain-include | loadKB() emits full YAML in cached block | WIRED | regression test `tests/lib/eval/cat1-fab-005-regression.test.ts` calls `buildSystemPrompt()` and asserts regex matches against counter-fact strings; passing confirms loadKB → buildSystemPrompt pipeline includes counter_facts block. |
| src/app/api/chat/route.ts deflectionResponse() | src/lib/eval/agent-client.ts parseChatStream() | transient data-deflection SSE chunk | WIRED | route.ts:99 emits `type: 'data-deflection'`; agent-client.ts:79-86 has `isDeflection` type guard; line 115-119 extracts first deflection chunk. Tests verify end-to-end (cat1.test.ts:235-253 per REVIEW.md IN-04). |
| src/lib/eval/agent-client.ts callAgent() | src/lib/eval/cats/cat1.ts (skip path) | `{ deflection: { reason } \| null }` return shape | WIRED | callAgent return at line 181 includes deflection; cat1.ts:57 destructures `{ response, deflection }`; line 73 branches on `deflection !== null`. cat3.ts mirrors at lines 40-47. |
| .pre-phase-sha (Task 1 PART 0) | Task 3 verify invariant 4 + phase verification | `git rev-parse HEAD > .planning/phases/.../.pre-phase-sha` | WIRED | Pre-phase SHA `8be227b` documented in SUMMARY; live verified via `git diff 8be227b HEAD -- src/app/api/chat/route.ts` showing ONLY additions; redis.ts diff is empty. |
| .cat3-baseline (Task 1 PART 0) | Task 3 PART E + D-B-02 verification | `echo <int> > .planning/phases/.../.cat3-baseline` | WIRED | Baseline 1/6 captured; D-B-02 reframing documented in SUMMARY (pre-baseline = deflection-graded-as-warmth, NOT a real cat3 baseline). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| route.ts deflectionResponse | reason | DeflectionReason argument (typed `keyof typeof DEFLECTIONS`) | Yes — 7 well-known reasons | FLOWING |
| agent-client.ts parseChatStream | deflection | rawBody SSE stream parsing — extracts `data-deflection` chunk via type guard | Yes — verified by 541/541 test pass including cat1_skipped_deflection test fixture | FLOWING |
| cat1.ts deflection-skip | deflection.reason | callAgent return shape | Yes — propagates from route → parser → callAgent → runner. Test suite confirms `cat1_case_skipped_deflection` log event fires correctly | FLOWING |
| cat3.ts deflection-skip | deflection.reason | callAgent return shape | Yes — identical wiring to cat1 | FLOWING |
| kb/profile.yml counter_facts | YAML block contents | loadKB() reads file at module-init; buildSystemPrompt() concatenates into cached block | Yes — regression test asserts counter-fact strings appear in buildSystemPrompt() output | FLOWING |
| system-prompt.ts HALLUCINATION_RULES | premise-smuggling bullet | static const string in module body | Yes — buildSystemPrompt() includes the const verbatim (byte-identical determinism contract held per 17/17 tests) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes including new Item #7 deflection-skip path | `npm test` | 541/541 passing across 59 test files in 5.75s; `cat1_case_skipped_deflection` log fires during `run_test_deflect` fixture | PASS |
| TypeScript strict compile clean | `npx tsc --noEmit` | Exit 0, no errors | PASS |
| Next.js production build succeeds | `npm run build` | Build success — all 16 routes compile including /api/chat | PASS |
| Pre-phase byte-identical contract for /api/chat | `git diff 8be227b HEAD -- src/app/api/chat/route.ts` | Only 7 added lines (1 writer + 6 comment); zero removed/modified lines; existing DEFLECTIONS const + all 7 call sites byte-identical | PASS |
| Pre-phase byte-identical contract for redis.ts | `git diff 8be227b HEAD -- src/lib/redis.ts` | Empty diff — file unchanged | PASS |
| All four feature/fix commits exist in git log | `git log --all --oneline \| grep -E "4281c3b\|d286b74\|699c294\|78f4f8c"` | All 4 commits found plus pre-phase pin commit `8be227b` | PASS |
| npm run eval:reset-rl alias defined and uses tsx --import pattern | `package.json:12` | `node --env-file-if-exists=.env.local --import tsx scripts/reset-eval-rate-limits.ts` — uses the same pattern as `eval` script (per SUMMARY Rule 1 fix) | PASS |
| regression test file exists | `Read tests/lib/eval/cat1-fab-005-regression.test.ts` | 3 describes asserting premise-smuggling rule, Snowflake counter-fact, team-size counter-fact | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| N/A | 05.1-01 | Decimal phase; `phase_req_ids: null` per verification context | N/A | This phase addresses Phase 5 deferred-items #6/#7/#8, not REQUIREMENTS.md IDs |

### Anti-Patterns Found

No anti-patterns. REVIEW.md (standalone code review) reported 0 Critical, 3 Warning, 4 Info findings. The 3 Warnings are all out-of-scope-for-Phase-05.1 observations:

| Severity | File | Issue | Disposition |
|----------|------|-------|-------------|
| Warning | src/lib/eval/cats/cat4-judge.ts, cat5.ts | Deflection-skip path missing (same vector as cat1/cat3) | Out of scope per user; same finding as Item #11 promotion suggests — defer to Plan 05-12 |
| Warning | tests/lib/eval/cats/cat1.test.ts:59, cat3.test.ts:41 | parseChatStream mock signature is stale (`(raw: string) => raw`) | Harmless at runtime (callAgent mocked separately); cosmetic type drift; deferrable |
| Warning | src/lib/eval/cats/cat2.ts:144-149 | `cat2.assertSpendCapDeflection` regex over-matches deflection vocabulary | Pre-existing brittleness, not introduced by 05.1; newly trivial to fix with new data-deflection signal; deferrable |

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|--------------|----------|
| 1 | cat1 D-B-01 (15/15 hard gate) | Plan 05-12 LAUNCH-05 (and possibly Phase 05.2) | New deferred-item #11 promoted: "Classifier over-flags eval prompts as injection/sensitive/offtopic" — disposition deferred to Plan 05-12 prod-URL eval first, Phase 05.2 quick task if it blocks. SUMMARY "Path forward" section recommends investigating cat1-fab-001 'Anthropic' prompt classifier behavior; recomputing D-B-01 against prod URL where classifier behavior may differ. STATE.md Blockers/Concerns line 195 documents the Item #11 promotion. |
| 2 | cat2/cat4-judge/cat5 deflection-skip paths (REVIEW WR-01) | Plan 05-12 or Phase 05.2 | Same failure mode as cat1/cat3 fixed in this phase; promoted via REVIEW.md WR-01. |

### Human Verification Required

None. All artifacts verified programmatically. Phase produces eval-CLI tooling and KB content — both testable via vitest + git diff against pre-phase SHA. The PARTIAL close itself was already human-accepted (user explicitly accepted PARTIAL via orchestrator close-out per the verification context).

### Gaps Summary

No gaps. Three Phase 5 deferred items (#6, #7, #8) shipped with verifiable evidence:

- **Item #8** (`78f4f8c`): HALLUCINATION_RULES premise-smuggling rule + kb/profile.yml counter_facts block. cat1-fab-005 passes in every post-Task-1 run. 17/17 system-prompt tests + 3/3 regression tests pass.
- **Item #6** (`699c294` + `4281c3b`): scripts/reset-eval-rate-limits.ts + npm eval:reset-rl alias. Sliding-window key expansion bug found and fixed live; post-fix verifies cleared N/M against real Upstash storage.
- **Item #7** (`d286b74`): /api/chat emits transient data-deflection chunk before text-start; parseChatStream returns ParsedStream { text, deflection }; cat1.ts + cat3.ts skip deflected cases. Byte-identical production contract preserved (route.ts diff vs pre-phase SHA shows only additions; redis.ts unchanged).

The D-B-01 cat1=15/15 hard gate was not met — accepted as PARTIAL by the user during phase close-out. Item #7's clean signal exposed previously-hidden classifier over-flagging behavior (cat1 prompts triggering injection/sensitive/offtopic deflections) that pre-Item-#7 was being silently graded as fabrications by the judge, inflating apparent pass rates. The architectural fix is outside Phase 05.1 scope and was promoted as NEW deferred-item #11 for Plan 05-12 / Phase 05.2 disposition. Phase SUMMARY.md, STATE.md Roadmap Evolution, and ROADMAP.md Phase 05.1 entry all document the PARTIAL close honestly with the reframing rationale.

Bookkeeping is complete: deferred-items.md entries #6/#7/#8 marked RESOLVED with commit hashes; STATE.md Roadmap Evolution + Blockers/Concerns updated; ROADMAP.md Phase 05.1 finalized; new Item #11 documented as a Phase 5 deferred item for Plan 05-12 follow-up.

---

_Verified: 2026-05-10T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
