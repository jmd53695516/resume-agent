---
phase: 05-eval-gates-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 9 outstanding Phase 3 HUMAN-UAT items are walked against a live preview/dev environment with PASS/FAIL recorded"
    - "All 11 outstanding Phase 4 HUMAN-UAT items are walked against a live preview deploy with PASS/FAIL recorded"
    - "Discovered bugs are captured as either decimal phases (5.1, 5.2) or as launch-checklist follow-ups, with explicit owner + next-step per bug"
    - "Joe has a written go/no-go decision on whether Phase 5 eval suite work can begin on the current preview build"
  artifacts:
    - path: ".planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md"
      provides: "Committed artifact recording PASS/FAIL/BLOCKED for all 20 UAT items + go/no-go verdict"
      contains: "## Phase 3 UAT (9 items)"
  key_links:
    - from: ".planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md"
      to: ".planning/phases/03-tools-resilience/03-HUMAN-UAT.md"
      via: "Each Phase 3 item title copied verbatim with PASS/FAIL"
      pattern: "Live Exa call observable"
    - from: ".planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md"
      to: ".planning/phases/04-admin-observability/04-HUMAN-UAT.md"
      via: "Each Phase 4 item title copied verbatim with PASS/FAIL"
      pattern: "GitHub OAuth happy path"
---

<objective>
Pre-launch smoke. Walk Joe through all 20 outstanding HUMAN-UAT items (9 Phase 3 + 11 Phase 4) against a fresh preview deploy (or `npm run dev` if Vercel project not yet wired). Discovered bugs become decimal phases (5.1, 5.2…) or launch-checklist follow-ups. This plan blocks every downstream Phase 5 plan because eval work cannot stand on a buggy foundation.

Purpose: surface blockers BEFORE the eval suite is built. Eval runs against a known-good preview, not one with broken cron/email/OAuth.
Output: `.planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md` with go/no-go verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-eval-gates-launch/05-CONTEXT.md
@.planning/phases/05-eval-gates-launch/05-RESEARCH.md
@.planning/phases/03-tools-resilience/03-HUMAN-UAT.md
@.planning/phases/04-admin-observability/04-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision preview deploy + create results file</name>
  <files>.planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md</files>
  <read_first>
    - .planning/phases/03-tools-resilience/03-HUMAN-UAT.md (9 test items, copy titles verbatim)
    - .planning/phases/04-admin-observability/04-HUMAN-UAT.md (11 test items, copy titles verbatim)
    - .planning/STATE.md (Blockers/Concerns: SAFE-12, resume PDF 404)
  </read_first>
  <what-built>Empty results scaffold. Joe must (a) confirm a working preview URL exists OR run `npm run dev` locally, (b) confirm `.env.local` has all Phase 1-4 keys (ANTHROPIC, EXA, SUPABASE service-role, RESEND, CRON_SECRET, ADMIN_GITHUB_LOGINS, UPSTASH).</what-built>
  <how-to-verify>
    Step A — Decide environment:
      Option 1: If GitHub remote + Vercel project already exist for this repo, push a test branch and capture the preview URL.
      Option 2: If no remote yet (per Phase 4 deferred-UAT note), run `npm run dev` locally; some items (BetterStack, OAuth full round-trip) will be marked BLOCKED-NO-INFRA rather than FAIL.
    Step B — Create the results file at `.planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md` with:
      ```
      ---
      walked_at: 2026-MM-DD
      preview_url: <URL or "local:dev">
      env_mode: <"preview"|"local-dev">
      ---

      # Phase 5-01 HUMAN-UAT Results

      ## Phase 3 UAT (9 items)

      ### 1. Live Exa call observable in DevTools when triggering research_company
      result: <PASS|FAIL|BLOCKED-NO-INFRA>
      notes: ...

      ### 2. Pitch tool produces 3-paragraph tailored output with live clickable source URLs
      result: ...
      ... (continue for all 9 from 03-HUMAN-UAT.md verbatim)

      ## Phase 4 UAT (11 items)
      ### 1. GitHub OAuth happy path on deploy preview
      result: ...
      ... (continue for all 11 from 04-HUMAN-UAT.md verbatim)

      ## Bug Backlog
      | id | title | severity (block-launch / fix-before-eval / launch-checklist) | proposed action |
      |----|-------|--------------------------------------------------------------|-----------------|

      ## Verdict
      go/no-go: <GO|NO-GO>
      rationale: ...
      ```
  </how-to-verify>
  <acceptance_criteria>
    - File exists at .planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md
    - Frontmatter has `preview_url` (URL or "local:dev") and `env_mode` keys populated
    - File contains exactly 9 H3 entries under "## Phase 3 UAT (9 items)" with titles matching .planning/phases/03-tools-resilience/03-HUMAN-UAT.md
    - File contains exactly 11 H3 entries under "## Phase 4 UAT (11 items)" with titles matching .planning/phases/04-admin-observability/04-HUMAN-UAT.md
    - All 20 items have a `result:` line with one of PASS / FAIL / BLOCKED-NO-INFRA
    - "## Bug Backlog" table exists (rows allowed to be empty if no bugs)
    - "## Verdict" section has `go/no-go: GO` or `go/no-go: NO-GO`
  </acceptance_criteria>
  <action>Create the scaffold described above with all 20 item titles pre-filled (copy verbatim from the two source HUMAN-UAT files). Joe will fill `result:` per item during the walk in Task 2.</action>
  <resume-signal>Type "scaffold-ready" once the file exists with all 20 placeholder entries and the env_mode + preview_url frontmatter is set.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Joe walks all 20 UAT items, records results, files verdict</name>
  <files>.planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md</files>
  <read_first>
    - .planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md (the scaffold from Task 1)
    - .planning/phases/03-tools-resilience/03-HUMAN-UAT.md (full expected behaviors per item)
    - .planning/phases/04-admin-observability/04-HUMAN-UAT.md (full expected behaviors per item)
  </read_first>
  <what-built>Joe steps through each item, records PASS/FAIL/BLOCKED-NO-INFRA, captures bugs.</what-built>
  <how-to-verify>
    For each of the 20 items:
      1. Read the `expected:` block in the source HUMAN-UAT file.
      2. Reproduce the scenario against the preview URL (or local dev for items that don't need infra).
      3. Edit `05-01-HUMAN-UAT-RESULTS.md` → set `result:` and add notes.
      4. If FAIL: add a row to "## Bug Backlog" with severity:
         - `block-launch` — broken core flow (chat doesn't stream, OAuth 500s, etc.)
         - `fix-before-eval` — eval suite would be unreliable until fixed (e.g., trace panel renders empty)
         - `launch-checklist` — cosmetic / non-blocking (e.g., "missing favicon")
      5. After all 20 items: write the Verdict section.
         - GO if zero `block-launch` and zero `fix-before-eval` bugs.
         - NO-GO otherwise → orchestrator opens decimal-phase 5.1 to fix the `block-launch` items, this plan re-runs.

    Specific expectations to match:
      - Phase 3 item 9 (Resume PDF 404): expected to be FAIL or BLOCKED today; this is acknowledged in STATE.md and resolved in plan 05-11 — mark FAIL with note "deferred to 05-11" and DO NOT count toward NO-GO verdict.
      - Phase 4 item 9 (cron-job.org schedules): if remote+Vercel not yet wired, mark BLOCKED-NO-INFRA — this work folds into plan 05-11 launch checklist.
      - Phase 4 item 10 (BetterStack): same — BLOCKED-NO-INFRA in local-dev mode is acceptable.
      - Phase 4 item 6 (force-trip spend-cap alarm): requires Redis access; if local Upstash key works, run it; otherwise BLOCKED-NO-INFRA.

    Verdict format:
      ```
      go/no-go: GO
      rationale: 18 PASS, 2 BLOCKED-NO-INFRA (Phase 4 items 9, 10 — deferred to 05-11), 1 FAIL (Phase 3 item 9 — deferred to 05-11). Zero blockers.
      ```
  </how-to-verify>
  <acceptance_criteria>
    - All 20 items have non-empty `result:` (no item left as placeholder)
    - Verdict line is exactly `go/no-go: GO` or `go/no-go: NO-GO` (not "maybe", not blank)
    - If verdict is GO: zero rows in Bug Backlog with severity `block-launch` or `fix-before-eval`
    - If verdict is NO-GO: at least one Bug Backlog row with `block-launch` or `fix-before-eval` severity AND rationale references those bug IDs
    - File committed to git in same commit as plan 05-01 close
  </acceptance_criteria>
  <action>Joe walks each item, fills results file, decides verdict. If NO-GO, orchestrator inserts decimal-phase 5.1 (or 5.X) for fixes before plan 05-02 starts.</action>
  <resume-signal>Type "uat-walked verdict-GO" or "uat-walked verdict-NO-GO" with the rationale appended.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan is a documentation walkthrough; no production code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | UAT results file may contain a real preview URL | accept | Preview URLs are not secrets (Vercel previews are publicly indexable when not behind Deployment Protection); URL alone is not exploitable without app-level auth/rate-limit bypass. Joe's email gate + Phase 2 rate limits still apply. |
| T-05-01-02 | Tampering | UAT results edited to claim PASS without actually running checks | accept | Self-reported by Joe; future eval suite (cat 1-6) is the verifiable gate. Document trust model: "this is Joe-attested, eval CLI is mechanical." |
</threat_model>

<verification>
- `.planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md` exists
- File has all 20 items with non-empty `result:` lines
- Verdict line present and one of GO/NO-GO
- Committed to git
</verification>

<success_criteria>
- Joe has go/no-go verdict on whether Phase 5-02 onward can proceed
- Any block-launch or fix-before-eval bugs are captured in Bug Backlog with proposed action
- Deferred items (resume PDF, cron-job.org schedules, BetterStack) explicitly tagged as "deferred to 05-11"
</success_criteria>

<output>
After completion, create `.planning/phases/05-eval-gates-launch/05-01-SUMMARY.md` summarizing the walkthrough outcome (counts of PASS/FAIL/BLOCKED, any decimal phases opened).
</output>
