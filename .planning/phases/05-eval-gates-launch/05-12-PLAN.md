---
phase: 05-eval-gates-launch
plan: 12
type: execute
wave: 9
depends_on: ["05-11"]
files_modified:
  - public/joe-dollinger-resume.pdf
  - public/resume-qr.png
  - kb/guardrails.md
  - .planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md
  - .planning/phases/05-eval-gates-launch/safe-12-evidence.png
autonomous: false
requirements: ["LAUNCH-01", "LAUNCH-02", "LAUNCH-03", "LAUNCH-04", "LAUNCH-05", "LAUNCH-06", "LAUNCH-07", "SAFE-12"]
must_haves:
  truths:
    - "Public domain configured: custom subdomain on a Joe-owned domain (LOCKED user decision; final subdomain string captured at task time, e.g., chat.joedollinger.com), CNAME → cname.vercel-dns.com (LAUNCH-01)"
    - "QR PNG generated from final prod URL via `npx qrcode <url> -o public/resume-qr.png -w 300 -e M` and committed (LAUNCH-02)"
    - "joe-dollinger-resume.pdf placed at public/joe-dollinger-resume.pdf — fixes 03-HUMAN-UAT item 9 (acknowledged 404 in Phase 3 STATE)"
    - "URL added to LinkedIn / digital resume PDF / personal site (LAUNCH-03)"
    - "Friend-test (LAUNCH-04): 3 testers (≥1 PM, ≥1 non-PM); non-PM answers Q1 'feels substantive Y/N' = Y; sign-off filed in launch checklist; awkward-issues fixed before resume-link activation"
    - "All EVAL-* requirements PASSING against PRODUCTION deploy (not preview) — verified by triggering one eval run on prod and observing 100% pass (LAUNCH-05)"
    - "Pre-launch checklist walked: resume.md final, guardrails.md Joe-signed, eval cat 1 + cat 4 passing, friend-tests done, real transcript verified end-to-end in /admin/sessions (LAUNCH-06)"
    - "Anthropic org-level $20/mo cap set in Console; screenshot evidence committed at .planning/phases/05-eval-gates-launch/safe-12-evidence.png (SAFE-12)"
    - "Branch protection + Deployment Checks confirmed gating prod (LAUNCH-07; A7 spot-test from Plan 05-10 satisfies this)"
    - "Resume link state flipped: paper resume printed with QR; LinkedIn/PDF/site updated"
  artifacts:
    - path: "public/joe-dollinger-resume.pdf"
      provides: "PDF Joe drops into repo; resolves 03-HUMAN-UAT item 9 404"
      contains: "(binary)"
    - path: "public/resume-qr.png"
      provides: "≥250×300px QR pointing to prod URL"
      contains: "(binary)"
    - path: ".planning/phases/05-eval-gates-launch/safe-12-evidence.png"
      provides: "Screenshot of Anthropic Console → Settings → Limits showing $20/mo cap"
      contains: "(screenshot)"
    - path: ".planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md"
      provides: "Launch sign-off artifact: domain, QR, friend-test results, EVAL pass status, SAFE-12 evidence path"
      contains: "## Friend-test sign-off"
    - path: "kb/guardrails.md"
      provides: "Joe-signed guardrails (Joe adds initials + date at end)"
      contains: "Signed:"
  key_links:
    - from: "public/resume-qr.png"
      to: "https://<final_prod_domain>"
      via: "QR scan resolves to final URL"
      pattern: "(generated from URL)"
    - from: ".planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md"
      to: "Friend-test results + SAFE-12 evidence + EVAL pass row id"
      via: "Single sign-off artifact pre-launch"
      pattern: "go/no-go"
---

<objective>
Final launch plan. Combines LAUNCH-01..07 + SAFE-12 + LAUNCH-04 friend-test execution into a single sign-off artifact. Per LOCKED user decision, the public domain is a custom subdomain on a Joe-owned domain (e.g., `chat.joedollinger.com`) with CNAME → `cname.vercel-dns.com`.

Friend-test executes per CONTEXT D-D-03: Joe DMs 3 specific people (≥1 PM, ≥1 non-PM); each gets the preview (or prod) URL + 5-question Google Form; non-PM tester's Q1 ("feels substantive Y/N") MUST be Y to satisfy LAUNCH-04. "Awkward" issues block resume-link activation per LAUNCH-04 wording.

Output: domain live, QR + PDF in repo, LinkedIn/PDF/site updated, guardrails signed, SAFE-12 evidence committed, friend-test sign-off filed, prod EVAL-pass run id captured, paper resume printed, resume link "go-live" recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-eval-gates-launch/05-CONTEXT.md
@.planning/phases/05-eval-gates-launch/05-RESEARCH.md
@.planning/STATE.md
@kb/guardrails.md
@.planning/phases/05-eval-gates-launch/05-01-HUMAN-UAT-RESULTS.md

<interfaces>
From orchestrator locked decisions:
- Domain = custom subdomain on Joe-owned domain; CNAME to cname.vercel-dns.com; planner cannot pre-fill the literal subdomain string

From RESEARCH §12 (QR generation):
```bash
npx qrcode "https://<prod-domain>" -o public/resume-qr.png -w 300 -e M
```

From RESEARCH §13 (SAFE-12):
- Set in Console → Settings → Limits → Spend Limits → $20/mo
- Verification = screenshot at .planning/phases/05-eval-gates-launch/safe-12-evidence.png (no first-class GET-spend-limit API endpoint)

From CONTEXT D-D-03 (friend-test):
- 3 testers (≥1 PM, ≥1 non-PM)
- Async link + 5-question Google Form
- Q1 "feels substantive Y/N" must be Y from non-PM
- "Awkward" → block link activation

From STATE.md Blockers/Concerns:
- "drop joe-dollinger-resume.pdf into public/ before public deploy" (Phase 5 LAUNCH-* note)
- "SAFE-12 was deferred during Plan 02-01 Task 3"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Joe configures domain CNAME + drops PDF + generates QR + signs guardrails</name>
  <files>public/joe-dollinger-resume.pdf, public/resume-qr.png, kb/guardrails.md</files>
  <read_first>
    - .planning/phases/05-eval-gates-launch/05-RESEARCH.md (§12 QR command)
    - kb/guardrails.md (current file; Joe adds sign-off line at end)
    - .planning/STATE.md (Phase 3 + 5 Blockers entries on PDF + SAFE-12)
  </read_first>
  <what-built>Domain DNS config + PDF + QR PNG + guardrails sign-off. Joe-supplied artifacts at execute time.</what-built>
  <how-to-verify>
    **Step A — Domain (LAUNCH-01)**: Joe decides the literal subdomain string at task time (orchestrator locked: subdomain on Joe-owned domain).
    1. Add domain in Vercel: Project → Settings → Domains → Add → enter subdomain
    2. Vercel shows: "Add CNAME at &lt;subdomain&gt; → cname.vercel-dns.com"
    3. Joe adds CNAME at registrar (Cloudflare/Namecheap/etc.)
    4. Wait for DNS propagation; Vercel auto-issues Let's Encrypt cert
    5. Capture final subdomain in resume signal — referenced by Tasks 2-4

    **Step B — Drop PDF (resolves 03-HUMAN-UAT item 9)**:
    1. Joe drags `joe-dollinger-resume.pdf` (his current resume) into `public/`
    2. Confirm `&lt;subdomain&gt;/joe-dollinger-resume.pdf` returns 200 after redeploy

    **Step C — Generate QR (LAUNCH-02)**:
    ```bash
    npx qrcode "https://<final_subdomain>" -o public/resume-qr.png -w 300 -e M
    ```
    Then verify on Windows: `Test-Path public\resume-qr.png` returns True; open the PNG, scan with phone, confirm it resolves to the final URL.

    **Step D — Sign guardrails (LAUNCH-06 prerequisite)**:
    Edit `kb/guardrails.md` and append at end:
    ```markdown

    ---

    Signed: Joe Dollinger
    Date: 2026-MM-DD
    ```
  </how-to-verify>
  
  <action>See &lt;how-to-verify&gt; above — this is a checkpoint:human-verify/action task driven by the steps in &lt;how-to-verify&gt;. Joe executes those steps and replies via &lt;resume-signal&gt;.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human-attested per &lt;acceptance_criteria&gt;</automated>
  </verify>
  <done>Checkpoint signaled per &lt;resume-signal&gt;; orchestrator records outcome.</done>
  <acceptance_criteria>
    - public/joe-dollinger-resume.pdf exists (binary; verified by `test -f` + non-zero size)
    - public/resume-qr.png exists, ≥250×300px (verified by file existence + phone-scan resolves to final URL)
    - kb/guardrails.md contains literal `Signed: Joe Dollinger` + ISO date (verified by grep)
    - Joe records final subdomain in resume signal
  </acceptance_criteria>
  <resume-signal>"Domain=&lt;subdomain&gt; live; PDF dropped; QR scans correctly; guardrails signed".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Joe sets Anthropic org $20/mo cap (SAFE-12); commits screenshot evidence</name>
  <files>.planning/phases/05-eval-gates-launch/safe-12-evidence.png</files>
  <read_first>
    - .planning/phases/05-eval-gates-launch/05-RESEARCH.md (§13 lines 793-810 — exact Anthropic Console path; screenshot is the verification mechanism)
    - .planning/STATE.md (Blockers entry: SAFE-12 deferred from Plan 02-01)
  </read_first>
  <what-built>Anthropic org-level monthly spend cap of $20/mo, set in Anthropic Console. Verification = screenshot committed to repo (no programmatic GET-spend-limit endpoint per RESEARCH §13).</what-built>
  <how-to-verify>
    1. Visit https://console.anthropic.com → Settings → Limits → Spend Limits
    2. Click "Change Limit" (or "Set spend limit" if none configured)
    3. Enter `20` (USD/month)
    4. Save
    5. Screenshot the configured limit (must show: "$20.00/month" + Joe's org name)
    6. Save screenshot to `.planning/phases/05-eval-gates-launch/safe-12-evidence.png`
    7. Commit
  </how-to-verify>
  
  <action>See &lt;how-to-verify&gt; above — this is a checkpoint:human-verify/action task driven by the steps in &lt;how-to-verify&gt;. Joe executes those steps and replies via &lt;resume-signal&gt;.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human-attested per &lt;acceptance_criteria&gt;</automated>
  </verify>
  <done>Checkpoint signaled per &lt;resume-signal&gt;; orchestrator records outcome.</done>
  <acceptance_criteria>
    - Screenshot committed at exact path `.planning/phases/05-eval-gates-launch/safe-12-evidence.png`
    - File is a non-zero-byte PNG
    - Screenshot visibly shows "$20" / month cap (Joe verifies content; not auto-checkable)
  </acceptance_criteria>
  <resume-signal>"SAFE-12: $20/mo cap set; evidence at .planning/phases/05-eval-gates-launch/safe-12-evidence.png".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Update LinkedIn + digital resume PDF + personal site with the live URL</name>
  <files></files>
  <read_first>
    - LAUNCH-03 requirement
  </read_first>
  <what-built>URL distribution: LinkedIn profile, PDF embedded URL field, Joe's personal site link.</what-built>
  <how-to-verify>
    1. LinkedIn: edit profile → Contact info or Featured section → add `https://&lt;final_subdomain&gt;` with caption "Chat with my agent"
    2. PDF (digital resume): add the URL near the email/contact block (Joe edits his master resume Word/Pages doc → re-export PDF → upload to wherever Joe's hosting it)
    3. Personal site: add prominent link on homepage or contact page
    4. Joe captures URLs (LinkedIn, PDF host, personal site) in resume signal so future Joe knows where the link lives
  </how-to-verify>
  
  <action>See &lt;how-to-verify&gt; above — this is a checkpoint:human-verify/action task driven by the steps in &lt;how-to-verify&gt;. Joe executes those steps and replies via &lt;resume-signal&gt;.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human-attested per &lt;acceptance_criteria&gt;</automated>
  </verify>
  <done>Checkpoint signaled per &lt;resume-signal&gt;; orchestrator records outcome.</done>
  <acceptance_criteria>
    - Joe confirms all three locations updated
    - Each URL accessible (Joe spot-checks: opens link in incognito, lands on agent landing page)
  </acceptance_criteria>
  <resume-signal>"URL added to: LinkedIn (link), PDF (resume host URL), personal site (URL)".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Run friend-test (LAUNCH-04) — 3 testers via Google Form</name>
  <files>.planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/05-eval-gates-launch/05-CONTEXT.md (D-D-03 — 5-question Google Form)
  </read_first>
  <what-built>3 friend-tester sessions completed, results recorded in launch checklist artifact.</what-built>
  <how-to-verify>
    **Step A — Set up Google Form** with 5 questions per CONTEXT D-D-03:
    1. "Feels substantive (Y/N)?"
    2. "Anything awkward — pick a moment."
    3. "One favorite moment."
    4. "One cringe moment."
    5. Open feedback.

    **Step B — DM 3 specific people**: ≥1 PM, ≥1 non-PM. Joe sends each: prod URL + Google Form link. Async — no scheduling.

    **Step C — Wait for responses + record in launch checklist**:
    Create `.planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md`:
    ```markdown
    ---
    launch_date: 2026-MM-DD
    final_url: https://&lt;subdomain&gt;
    ---

    # Phase 5 Launch Checklist

    ## Domain (LAUNCH-01..03)
    - [x] Subdomain CNAME → cname.vercel-dns.com configured
    - [x] PDF dropped at public/joe-dollinger-resume.pdf
    - [x] QR PNG generated → public/resume-qr.png
    - [x] LinkedIn updated
    - [x] PDF updated
    - [x] Personal site updated

    ## SAFE-12
    - [x] Anthropic org $20/mo cap set; evidence: safe-12-evidence.png

    ## EVAL pass on prod (LAUNCH-05)
    - [x] eval_runs row id: &lt;runId&gt; — all 6 cats PASS against prod URL

    ## Friend-test (LAUNCH-04)
    - Tester 1 (PM): &lt;name redacted in repo if Joe prefers, OR initial+role&gt;
      - Q1 (substantive Y/N): Y
      - Q2 (awkward): &lt;quote or none&gt;
      - Q3 (favorite): &lt;quote&gt;
      - Q4 (cringe): &lt;quote or none&gt;
      - Q5 (open): &lt;quote&gt;
    - Tester 2 (non-PM): ...
      - Q1 (substantive Y/N): MUST BE Y for pass
      - ...
    - Tester 3 (PM or non-PM): ...

    ## Awkward-issues triage
    | Tester | Issue | Severity | Fix decision |
    |--------|-------|----------|--------------|
    |   ...  |  ...  | block-launch / fix-before-launch / accept | ... |

    ## Pre-launch checklist (LAUNCH-06)
    - [x] resume.md final
    - [x] kb/guardrails.md Joe-signed
    - [x] EVAL cat 1 (15/15) passing on prod
    - [x] EVAL cat 4 (LLM-judge ≥4.0 + blind A/B &lt; 70%) passing on prod
    - [x] Friend-test sign-off (3 testers, ≥1 PM, ≥1 non-PM, non-PM Y on Q1)
    - [x] Real transcript verified end-to-end in /admin/sessions (Joe sends one chat as anonymous, sees row + transcript with traces)

    ## LAUNCH-07 (preview-to-prod gate)
    - [x] A7 spot-test passed in Plan 05-10 (eval failure blocks Vercel promotion)

    ## Resume link go-live
    - [x] Paper resume printed with QR (date: 2026-MM-DD)
    - [x] Final go/no-go: GO

    ---

    Signed: Joe Dollinger
    Date: 2026-MM-DD
    ```

    **Step D — Awkward-issue triage**:
    - Each "awkward" / "cringe" item from any tester reviewed
    - If `block-launch`: open decimal phase 5.X; do NOT mark go-live
    - If `fix-before-launch`: fix in this plan or as decimal phase before paper-print
    - If `accept`: note rationale in checklist; proceed

    **Step E — LAUNCH-05 — trigger one prod-URL eval run**:
    Run `EVAL_TARGET_URL=&lt;prod_url&gt; npm run eval` locally OR trigger via cron-job.org weekly schedule (already configured in 05-11). Capture the runId in the checklist; require status='passed' (zero failed cases).

    **Step F — LAUNCH-06 real transcript verification**:
    Joe in incognito → loads landing page as if a recruiter → enters email → sends a chat → opens /admin/sessions → confirms row + transcript with traces visible. Note in checklist.

    **Step G — Final verdict + paper print**:
    If all checkboxes green and no block-launch issues → Joe prints paper resume with QR and writes "GO" + date in checklist. Otherwise NO-GO + decimal-phase 5.X for fix.
  </how-to-verify>
  
  <action>See &lt;how-to-verify&gt; above — this is a checkpoint:human-verify/action task driven by the steps in &lt;how-to-verify&gt;. Joe executes those steps and replies via &lt;resume-signal&gt;.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human-attested per &lt;acceptance_criteria&gt;</automated>
  </verify>
  <done>Checkpoint signaled per &lt;resume-signal&gt;; orchestrator records outcome.</done>
  <acceptance_criteria>
    - .planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md exists with all sections populated
    - 3 tester rows present (Q1 result captured for each); ≥1 PM AND ≥1 non-PM tagged in role
    - Non-PM tester's Q1 = Y (LAUNCH-04 hard requirement)
    - One prod-URL eval_runs row id with status='passed' captured (LAUNCH-05)
    - Real transcript verification noted (LAUNCH-06)
    - Final verdict line: `Final go/no-go: GO` (or NO-GO with rationale + decimal phase opened)
    - File ends with `Signed: Joe Dollinger` + date
  </acceptance_criteria>
  <resume-signal>"LAUNCH-CHECKLIST filed; 3 testers recorded (roles: PM, non-PM, &lt;3rd&gt;); non-PM Q1=Y; prod eval runId=&lt;id&gt; PASS; verdict: GO" — OR NO-GO with details.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public DNS → Vercel TLS | Custom subdomain CNAME; Let's Encrypt auto-issued |
| Friend-tester → prod /api/chat | Untrusted user; rate limits + classifier preflight active (Phase 2) |
| LinkedIn/PDF/site URL → public | Anyone with the URL can hit /api/chat; intentional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-01 | Information Disclosure | LAUNCH-CHECKLIST.md may include tester names | mitigate | Use initials + role only (Joe's discretion); friends-only DM, not public PR; private repo until launch; if public after launch, redact names |
| T-05-12-02 | Tampering | Custom domain DNS hijack (registrar account compromise) | accept | Standard DNS-security hygiene (registrar 2FA, account-lock); same as any custom domain. Mitigated by Vercel Let's Encrypt auto-renewal — if cert breaks, traffic fails closed |
| T-05-12-03 | Repudiation | Joe later disputes friend-test results | accept | Google Form auto-timestamps + records identity (Form requires sign-in by default); checklist preserves quotes |
| T-05-12-04 | Denial of Service | Public URL on resume drives heavy recruiter traffic → spend cap fires | mitigate | SAFE-12 ($20/mo Anthropic) + per-IP/per-email/per-session rate limits (Phase 2); spend cap deflects gracefully; eval cat-2 spend-cap synthetic verifies the deflection path |
| T-05-12-05 | Information Disclosure | Joe's resume PDF in public/ may contain personal info recruiters wouldn't otherwise have (phone, address) | accept | Joe controls what goes in the PDF; standard resume-public-distribution model |
| T-05-12-06 | Spoofing | QR PNG could be re-printed by a third party with a different URL | accept | Adversary-modified QR doesn't affect Joe's paper resume; out of threat scope |
</threat_model>

<verification>
- public/joe-dollinger-resume.pdf + public/resume-qr.png + safe-12-evidence.png exist (Tasks 1-2)
- LAUNCH-CHECKLIST.md complete with all sections + signed (Task 4)
- 3 testers including ≥1 PM + ≥1 non-PM; non-PM Q1=Y (Task 4 acceptance)
- Prod eval runId captured with status='passed' (Task 4 LAUNCH-05)
</verification>

<success_criteria>
- LAUNCH-01: custom subdomain live, CNAME configured, TLS active
- LAUNCH-02: QR PNG committed, scans to prod URL
- LAUNCH-03: LinkedIn + PDF + personal site updated
- LAUNCH-04: 3 testers (≥1 PM, ≥1 non-PM); non-PM Q1=Y; awkward-issues triaged
- LAUNCH-05: all EVAL passing on prod (one runId captured)
- LAUNCH-06: pre-launch checklist complete with sign-off
- LAUNCH-07: A7 spot-test passed (referenced from Plan 05-10)
- SAFE-12: $20/mo cap with screenshot evidence
- Resume link go-live recorded; paper resume printed
</success_criteria>

<output>
After completion, create `.planning/phases/05-eval-gates-launch/05-12-SUMMARY.md` referencing the LAUNCH-CHECKLIST.md as the durable sign-off artifact + recording the prod eval runId + go-live timestamp + first-recruiter date (when known).
</output>
