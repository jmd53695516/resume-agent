---
phase: 999.1
plan: 02
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md
  - .planning/phases/05-eval-gates-launch/05-12-PLAN.md
  - .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md
autonomous: false
requirements: []
context_decisions: ["D-04", "D-05", "D-07", "D-08"]
must_haves:
  truths:
    - "3 cold-cache CI runs of cats=cat4-judge against https://joe-dollinger-chat.com triggered sequentially via `gh workflow run eval.yml`; each run lands on a fresh ubuntu-latest runner with no shared Anthropic cache state"
    - "All 3 runs MUST show cat4 5/5 pass with cat4-prompt-003 avg ≥ 3.8 (D-04). If even 1 fails, the plan kicks back to root-cause investigation — do NOT iterate the fix without re-characterizing variance (D-04 anti-pattern #4)"
    - "Each run's GitHub Actions run-id, Supabase eval_runs row-id, cat4-prompt-003 per-case avg, and aggregate avg captured in `999.1-VERIFICATION-LOG.md`"
    - "Plan 05-12 D-12-B-01 launch-gate cross-reference amended (D-08): a forward-looking note that 'cat4 PASS' definition is now `per_case ≥ 3.8 AND aggregate ≥ 4.0` effective 2026-05-14 (Phase 999.1), and Plan 05-12 original sign-off at the legacy 4.0/4.0 threshold (per D-07) is preserved — supersession is forward-only"
    - "999.1 close-out SUMMARY links back to the amended Plan 05-12 spec for auditable trail (D-08 Option b per RESEARCH §Plan 05-12 Cross-Reference)"
  artifacts:
    - path: ".planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md"
      provides: "Durable N=3 cold-cache verification audit trail — 3 rows with GH run-ids, eval_runs row-ids, cat4-prompt-003 avgs, aggregate avgs, durations, costs"
      contains: "## N=3 cold-cache verification"
    - path: ".planning/phases/05-eval-gates-launch/05-12-PLAN.md"
      provides: "Plan 05-12 must_haves.truths line 22 + Objective lines 56-57 + Success-criteria line 436 + LAUNCH-CHECKLIST template lines 316-319 — each gains an inline supersession note pointing at Phase 999.1's 3.8/4.0 cat4-PASS definition"
      contains: "Phase 999.1"
    - path: ".planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md"
      provides: "D-12-B-01 amended with: 'Effective 2026-05-14 per Phase 999.1, cat4 PASS = per_case_min_avg 3.8 + aggregate_min_avg 4.0. D-07 affirms forward-looking; original Plan 05-12 sign-off (2026-05-13) was against the legacy 4.0/4.0 threshold.'"
      contains: "Phase 999.1"
  key_links:
    - from: ".planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md"
      to: "GitHub Actions run-ids + Supabase eval_runs row-ids"
      via: "3 rows in a markdown table with one row per CI run"
      pattern: "gh_run_id"
    - from: ".planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-SUMMARY.md (created at phase close-out)"
      to: ".planning/phases/05-eval-gates-launch/05-12-PLAN.md + 05-12-CONTEXT-ADDENDUM.md"
      via: "Inline markdown links + supersession note explaining 3.8 threshold vs 4.0 sign-off"
      pattern: "Phase 999.1"
---

<objective>
N=3 cold-cache CI verification of the Phase 999.1-01 fix + D-08 close-out cross-reference to Plan 05-12.

Per CONTEXT D-04, ALL 3 cold-cache CI runs of `cats=cat4-judge` against `https://joe-dollinger-chat.com` MUST show cat4 5/5 PASS with cat4-prompt-003 per-case avg ≥ 3.8. Anything less kicks back to root-cause investigation — the plan explicitly DOES NOT iterate the fix in a chase loop (D-04 + anti-pattern #4).

Per CONTEXT D-08, the Plan 05-12 D-12-B-01 launch-gate definition is amended with a forward-looking supersession note (per D-07, Plan 05-12's original sign-off at 4.0/4.0 is preserved). The 999.1 SUMMARY links back to the amended spec for auditable trail.

This plan is `autonomous: false` because it requires `gh` CLI + manual cost approval (~$0.36 CI spend per D-04 RESEARCH §OQ-4) + user-attested verification of 3 sequential CI runs.

Output: durable VERIFICATION-LOG.md with 3 run records; Plan 05-12 PLAN.md + CONTEXT-ADDENDUM.md amended; phase-close SUMMARY (created in this plan's <output> step) cross-linking everything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-CONTEXT.md
@.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-RESEARCH.md
@.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-01-PLAN.md
@.planning/phases/05-eval-gates-launch/05-12-PLAN.md

<interfaces>
<!-- N=3 verification mechanic per RESEARCH §OQ-4. Verbatim commands the executor will run. -->

From `.github/workflows/eval.yml` (workflow_dispatch inputs declared at lines 8-18 per RESEARCH §Sources):
- `target_url` — required string input
- `cats` — required string input; comma-separated category filter (eval CLI converts via run-evals.ts argv handling shipped in Plan 05-13)

Verified `gh` invocation (RESEARCH §Code Examples):
```bash
for i in 1 2 3; do
  echo "=== run $i ==="
  gh workflow run eval.yml \
    -f target_url=https://joe-dollinger-chat.com \
    -f cats=cat4-judge
  gh run watch  # blocks until the most-recent run reaches a terminal state
done
```

Pass criteria per D-04: ALL 3 runs show cat4 5/5 PASS with `cat4-prompt-003` per-case avg ≥ 3.8. ANY single failure → kick back to root-cause investigation; do NOT iterate the fix.

Cost estimate per RESEARCH §OQ-4: 3 × ~12¢ = ~$0.36 (cat4-judge only, narrowed from full eval gate). Plus ~$0.32/run × 3 = ~$1 in warmup tokens charged to Anthropic billing (NOT visible in eval_runs rows per cost-isolation per Plan 999.1-01 T-999.1-01 mitigation).

From RESEARCH §Plan 05-12 Cross-Reference — the four references in Plan 05-12 that need supersession notes:
1. `05-12-PLAN.md` line 22 (frontmatter must_haves.truths — "cat1=15/15 + cat4 PASSING against PRODUCTION deploy")
2. `05-12-PLAN.md` lines 56-57 (Objective block — "LAUNCH-05 scope (per addendum D-12-B-01): narrowed from 'all EVAL passing' to roadmap-aligned `cat1=15/15 + cat4 PASS` on prod")
3. `05-12-PLAN.md` line 436 (Success criteria — "LAUNCH-05: cat1=15/15 + cat4 PASS on prod")
4. `05-12-PLAN.md` lines 316-319 (LAUNCH-CHECKLIST template inside how-to-verify — "cat4 = PASS on prod — eval_runs row id: <cat4-runId>")

The canonical definition of "cat4 PASS" lives in `05-12-CONTEXT-ADDENDUM.md` D-12-B-01 (Plan 05-12 references but does not redefine). The amendment goes there; Plan 05-12 PLAN.md changes are inline supersession notes pointing at the addendum.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger 3 sequential cold-cache CI runs via `gh workflow run`; capture each run's evidence into VERIFICATION-LOG.md</name>
  <files>.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-RESEARCH.md §OQ-4 + §Phase 6 Variance Baseline + §Code Examples (verbatim `gh workflow run` commands; pass criteria; cost estimate)
    - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-CONTEXT.md §Verification Rigor (D-04 + D-05 — N=3 requirement and the anti-pattern of iterating the fix without re-characterizing variance)
    - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-01-PLAN.md (confirm Plan 999.1-01 has merged to main BEFORE running CI — otherwise the runner deploys the unfixed code)
    - .github/workflows/eval.yml (confirm workflow_dispatch inputs `target_url` + `cats` still declared)
  </read_first>
  <what-built>
    A durable VERIFICATION-LOG.md capturing 3 sequential cold-cache CI runs of `cats=cat4-judge` against prod. Each row records: GH Actions run-id, Supabase eval_runs row-id, cat4-prompt-003 per-case avg, aggregate avg, status (PASS/FAIL), wall-clock duration, and per-run cost in cents.

    This is the empirical evidence that the warmup + threshold relaxation (Plan 999.1-01) closes the cold-cache borderline class. All 3 MUST pass per D-04.
  </what-built>
  <how-to-verify>
    **Pre-flight:**
    1. Confirm Plan 999.1-01 is merged to main (so the deployed code on `https://joe-dollinger-chat.com` carries the warmup + 3.8 threshold). Check `git log --oneline main -5` shows the 999.1-01 commits, and `vercel ls` (or the Vercel dashboard) shows a successful deploy of that HEAD.
    2. Confirm `gh` CLI is authenticated: `gh auth status` returns logged-in.
    3. Confirm `evals/cat-04-voice.yaml` on main has `per_case_min_avg: 3.8` (`gh api repos/jmd53695516/resume-agent/contents/evals/cat-04-voice.yaml | jq -r .content | base64 -d | grep per_case_min_avg`).

    **Step A — Trigger 3 sequential cold-cache runs.**

    Run, ONE AT A TIME, with `gh run watch` between each:

    ```bash
    # Run 1
    gh workflow run eval.yml \
      -f target_url=https://joe-dollinger-chat.com \
      -f cats=cat4-judge
    gh run watch   # blocks until terminal state

    # Run 2 — only after Run 1 reaches a terminal state
    gh workflow run eval.yml \
      -f target_url=https://joe-dollinger-chat.com \
      -f cats=cat4-judge
    gh run watch

    # Run 3
    gh workflow run eval.yml \
      -f target_url=https://joe-dollinger-chat.com \
      -f cats=cat4-judge
    gh run watch
    ```

    **Why sequential, not parallel:** Anthropic prompt-cache hits across two runs that overlap within the 5-min default TTL would mean Run 2's "cold" cache is actually warm from Run 1's warmup — that defeats the verification purpose. Sequential (each cat4-judge run takes ~3-5 min including warmup + 5 cases + judge calls, naturally exceeding the 5-min TTL between dispatches).

    **Cost: ~$0.36 in eval_runs rows + ~$1 invisible warmup tokens** (per RESEARCH §OQ-4); under the $20/mo Anthropic org cap.

    **Step B — Capture each run's evidence into `999.1-VERIFICATION-LOG.md`.**

    Create the file at `.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md` with this structure:

    ```markdown
    # Phase 999.1 — N=3 cold-cache verification log

    **Goal (D-04):** All 3 cold-cache CI runs of `cats=cat4-judge` against `https://joe-dollinger-chat.com` must show cat4 5/5 PASS with cat4-prompt-003 per-case avg ≥ 3.8. If even 1 fails, kick back to root-cause investigation — do NOT iterate the fix.

    **Pre-flight verified:**
    - [x] Plan 999.1-01 merged to main (commit: <sha>)
    - [x] Vercel deploy of <sha> live on joe-dollinger-chat.com
    - [x] `evals/cat-04-voice.yaml` on main shows `per_case_min_avg: 3.8`
    - [x] `gh auth status` logged in

    ## N=3 cold-cache verification

    | Run | gh_run_id | eval_runs_row_id | cat4-prompt-003 avg | aggregate avg | per_case 5/5? | cost_cents | duration_ms | verdict |
    |---|---|---|---|---|---|---|---|---|
    | 1 | <gh-id-1> | <supabase-id-1> | <n.nn> | <n.nn> | yes/no | <n> | <n> | PASS/FAIL |
    | 2 | <gh-id-2> | <supabase-id-2> | <n.nn> | <n.nn> | yes/no | <n> | <n> | PASS/FAIL |
    | 3 | <gh-id-3> | <supabase-id-3> | <n.nn> | <n.nn> | yes/no | <n> | <n> | PASS/FAIL |

    ## Pass criteria (D-04)
    - [ ] Run 1 PASS — cat4 5/5 AND cat4-prompt-003 ≥ 3.8
    - [ ] Run 2 PASS — cat4 5/5 AND cat4-prompt-003 ≥ 3.8
    - [ ] Run 3 PASS — cat4 5/5 AND cat4-prompt-003 ≥ 3.8

    ## Verdict
    `3/3 PASS — fix verified` OR `<n>/3 PASS — KICK BACK to root-cause per D-04 anti-pattern #4`

    ## Notes
    - Cost summary: total eval_runs cost = <sum> cents; estimated invisible Anthropic warmup tokens = ~$1 (per RESEARCH §OQ-4 cost-isolation).
    - Wall-clock: ~3-5 min/run × 3 = ~10-15 min total.
    - `cat4_warmup_complete` log lines confirmed present in each run's Vercel logs (sanity check that the warmup helper executed; otherwise the cold-cache fix is not in effect).
    ```

    For each run, capture:
    - **gh_run_id:** from `gh run list --workflow=eval.yml --limit 3 --json databaseId,createdAt | jq` (most recent first)
    - **eval_runs_row_id:** from the Vercel logs of that run (search Pino output for `eval_run_started` → `runId`) OR from `gh run view <gh_run_id> --log | grep runId`
    - **cat4-prompt-003 avg + aggregate avg:** from the run's final summary log line (`cat4_judge_complete`) OR from Supabase `eval_cases` table query: `SELECT case_id, judge_score FROM eval_cases WHERE run_id = '<runId>' AND case_id = 'cat4-prompt-003'`
    - **per_case 5/5?:** all 5 cat4 cases have `judge_verdict = 'pass'`
    - **cost_cents + duration_ms:** from the eval_runs row (`SELECT total_cost_cents, EXTRACT(EPOCH FROM (completed_at - started_at)) * 1000 AS duration_ms FROM eval_runs WHERE id = '<runId>'`)
    - **verdict:** PASS if `per_case 5/5? = yes` AND `cat4-prompt-003 avg >= 3.8` AND `aggregate avg >= 4.0`; otherwise FAIL.

    **Step C — Surface the verdict.**

    - If `3/3 PASS`: write `## Verdict: 3/3 PASS — fix verified` into VERIFICATION-LOG.md. Proceed to Task 2.
    - If `<3/3 PASS`: write `## Verdict: <n>/3 PASS — KICK BACK to root-cause per D-04 anti-pattern #4`. STOP. Do NOT proceed to Task 2. Surface the failure to user with the failing run's `judge_rationale` from `eval_cases`, and recommend opening a new phase (999.1-A or sibling) for root-cause investigation — NOT a threshold-tightening iteration.

    **Step D — Confirm the warmup actually executed (sanity).**

    For at least Run 1, inspect Vercel logs for the runtime window of that GH Actions run. Search for the literal string `cat4_warmup_complete` (or `cat4_warmup_failed_proceeding_anyway` if it errored). If neither shows up in the logs, the warmup helper is not being invoked despite the code change — escalate (likely a build/deploy cache issue, not a verification failure per se). Note the result in VERIFICATION-LOG.md `## Notes` section.
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; above. This is a checkpoint:human-action task driven by the steps in &lt;how-to-verify&gt; — Joe (or any authenticated executor) runs the 3 `gh workflow run` commands sequentially, queries eval_runs / eval_cases via Supabase MCP or admin dashboard for the per-case scores, populates VERIFICATION-LOG.md, and signals back per &lt;resume-signal&gt;. NO code edits, NO fix iteration if verification fails (D-04 + anti-pattern #4).</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human-attested. 3 GH Actions run-ids + 3 Supabase eval_runs row-ids in VERIFICATION-LOG.md are the evidence per &lt;acceptance_criteria&gt;.</automated>
  </verify>
  <done>Checkpoint signaled per &lt;resume-signal&gt;. If verdict is `3/3 PASS`, orchestrator advances to Task 2. If verdict is `<3/3 PASS`, orchestrator records kick-back per D-04 and surfaces to user; Task 2 does NOT execute.</done>
  <acceptance_criteria>
    - `.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md` exists
    - File contains a 3-row table with columns: gh_run_id, eval_runs_row_id, cat4-prompt-003 avg, aggregate avg, per_case 5/5?, cost_cents, duration_ms, verdict
    - All 3 rows populated with actual values (no `<placeholder>` strings remain in the table cells)
    - File contains the literal string `## Verdict: 3/3 PASS — fix verified` (success path) OR `## Verdict: <n>/3 PASS — KICK BACK to root-cause per D-04 anti-pattern #4` (failure path)
    - For each row: `cat4-prompt-003 avg` is a number ≥ 3.8 if verdict is PASS; `aggregate avg` is a number ≥ 4.0 if verdict is PASS; `per_case 5/5?` reads `yes` if verdict is PASS
    - Vercel log sanity check noted: `cat4_warmup_complete` log line confirmed present (or absence escalated)
  </acceptance_criteria>
  <resume-signal>"N=3 verification: &lt;3/3 PASS — fix verified | n/3 PASS — kick back&gt;. VERIFICATION-LOG.md at .planning/phases/999.1-.../999.1-VERIFICATION-LOG.md. Run-ids: &lt;gh1&gt;, &lt;gh2&gt;, &lt;gh3&gt;. cat4-prompt-003 avgs: &lt;a1&gt;, &lt;a2&gt;, &lt;a3&gt;."</resume-signal>
</task>

<task type="auto">
  <name>Task 2: D-08 cross-reference — amend Plan 05-12 PLAN.md + CONTEXT-ADDENDUM.md with forward-looking supersession note pointing at Phase 999.1 (per D-07 + D-08); commit and surface link from 999.1 SUMMARY</name>
  <files>.planning/phases/05-eval-gates-launch/05-12-PLAN.md, .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md</files>
  <read_first>
    - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-CONTEXT.md §D-07 + §D-08 (supersession is forward-only; Plan 05-12 launch-gate at sign-off-time remains the legacy 4.0/4.0 threshold)
    - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-RESEARCH.md §Plan 05-12 Cross-Reference (exact lines in 05-12-PLAN.md that need notes; Option (b) cross-link recommended)
    - .planning/phases/05-eval-gates-launch/05-12-PLAN.md (read in full — frontmatter line 22, Objective lines 55-65, Success-criteria around line 430-440, LAUNCH-CHECKLIST template lines 315-330)
    - .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md (read in full — locate D-12-B-01 block; this is where the canonical definition lives)
    - .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md (Task-1 output — Task 2 only runs if verdict is `3/3 PASS`)
  </read_first>
  <action>
    **Preconditions:** Task 1 verdict is `3/3 PASS`. If it is `<3/3 PASS`, this task DOES NOT execute (D-04 says kick back; threshold-tightening or fix-iteration is anti-pattern).

    **Step 1 — Amend `05-12-CONTEXT-ADDENDUM.md` D-12-B-01 (canonical definition).**

    Open `.planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md`. Locate the `D-12-B-01` section (search for the literal `D-12-B-01`). Append the following note at the END of the D-12-B-01 block (do NOT rewrite the existing text — append a clearly-delimited supersession note so the original sign-off context is preserved per D-07):

    ```markdown

    ---

    **Supersession note (added 2026-05-14 per Phase 999.1):**

    Effective 2026-05-14, the "cat4 PASS" definition is amended:
    - per_case_min_avg: **3.8** (relaxed from 4.0)
    - aggregate_min_avg: **4.0** (UNCHANGED)

    Per CONTEXT D-07, this is forward-looking only. Plan 05-12's original launch-gate sign-off (2026-05-13) was against the legacy 4.0/4.0 threshold and remains valid as the launch-time gate — preview 4.20/5-of-5 + prod 4.52/5-of-5 cleared the legacy gate by ≥ 0.20 on aggregate and 5/5 on per-case. The threshold relaxation applies to:
    - Ongoing CI evals (`gh workflow run eval.yml`)
    - Plan 05-12 friend-test sign-off if re-run
    - v1.1+ work

    Rationale: Phase 999.1 resolved cat4-prompt-003 cold-cache borderline-ness via (d) Sonnet prompt-cache warmup in `runCat4Judge` + (c) per_case threshold relaxation 4.0 → 3.8. Aggregate threshold stays at 4.0 because aggregate has never been the source of false-fails (all 7 Phase 6 variance-map runs had aggregate ≥ 4.20). See `.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-RESEARCH.md` for the full analysis and `999.1-VERIFICATION-LOG.md` for the N=3 cold-cache verification evidence.
    ```

    **Step 2 — Amend `05-12-PLAN.md` with inline supersession pointers at each of the 4 reference sites.**

    Per RESEARCH §Plan 05-12 Cross-Reference, locate these four lines and append (do NOT replace) a supersession parenthetical:

    a) **Frontmatter `must_haves.truths` line 22** — find the literal:
    ```
    "cat1=15/15 + cat4 PASSING against PRODUCTION deploy — verified by triggering one eval run on prod (LAUNCH-05). Other categories (cat2/cat3/cat5/cat6) reported as documented baseline in LAUNCH-CHECKLIST.md but NOT blocking. Per 05-12-CONTEXT-ADDENDUM.md D-12-B-01."
    ```
    Append after the period: ` (Cat4 PASS def amended forward-looking 2026-05-14 per Phase 999.1: per_case 3.8, aggregate 4.0. See addendum D-12-B-01 supersession note.)`

    b) **Objective lines 55-65** — find the literal:
    ```
    **LAUNCH-05 scope (per addendum D-12-B-01):** narrowed from "all EVAL passing" to roadmap-aligned `cat1=15/15 + cat4 PASS` on prod; other cats reported as documented baseline in LAUNCH-CHECKLIST but NOT blocking.
    ```
    Append a new line immediately after:
    ```

    **Forward-looking amendment (Phase 999.1, 2026-05-14):** Cat4 PASS now = per_case_min_avg 3.8 AND aggregate_min_avg 4.0. Plan 05-12's original sign-off (preview 4.20/5-of-5, prod 4.52/5-of-5) cleared the legacy 4.0/4.0 gate; supersession is forward-only per D-07. See `05-12-CONTEXT-ADDENDUM.md` D-12-B-01 supersession note.
    ```

    c) **Success criteria around line 436** — find the literal:
    ```
    - LAUNCH-05: cat1=15/15 + cat4 PASS on prod (per addendum D-12-B-01); other cats documented baseline in LAUNCH-CHECKLIST
    ```
    Append: ` — Cat4 PASS def amended forward-looking 2026-05-14 per Phase 999.1 (3.8/4.0); see addendum D-12-B-01.`

    d) **LAUNCH-CHECKLIST template lines 316-319** — find the literal:
    ```
    ## EVAL pass on prod (LAUNCH-05) — per addendum D-12-B-01
    **Hard gates (must pass):**
    - [x] cat1 = 15/15 on prod — eval_runs row id: <cat1-runId> (D-B-01 zero-fabrication)
    - [x] cat4 = PASS on prod — eval_runs row id: <cat4-runId> (voice fidelity)
    ```
    Append after the cat4 line:
    ```
      - Cat4 PASS def amended forward-looking 2026-05-14 per Phase 999.1: per_case_min_avg 3.8 + aggregate_min_avg 4.0. Plan 05-12 original sign-off remains valid against legacy 4.0/4.0 per D-07.
    ```

    **Step 3 — Sanity-check the edits.**

    Run `grep -n "Phase 999.1" .planning/phases/05-eval-gates-launch/05-12-PLAN.md` — expect at least 4 matches (one per inline note above).
    Run `grep -n "Supersession note (added 2026-05-14 per Phase 999.1)" .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md` — expect 1 match.

    **Step 4 — Note about the SUMMARY cross-link.**

    The 999.1 phase-close SUMMARY (created in the &lt;output&gt; step of this plan, NOT in Task 2 itself) MUST link back to `05-12-PLAN.md` + `05-12-CONTEXT-ADDENDUM.md` D-12-B-01. The SUMMARY-creation step at the bottom of this plan handles that; Task 2 just amends Plan 05-12.

    **Step 5 — Commit.**

    Stage and commit:
    ```
    git add .planning/phases/05-eval-gates-launch/05-12-PLAN.md .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md .planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md
    git commit -m "docs(999.1): D-08 close-out — amend Plan 05-12 cat4-PASS def (3.8/4.0 forward-looking)"
    ```

    Do NOT amend the Plan 05-12 commit history; create a NEW commit on main. The Phase 05 sign-off remains valid in history per D-07.
  </action>
  <verify>
    <automated>grep -c "Phase 999.1" .planning/phases/05-eval-gates-launch/05-12-PLAN.md returns at least 4; grep -c "Supersession note (added 2026-05-14 per Phase 999.1)" .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md returns exactly 1; git log -1 --name-only shows the 3 files in the close-out commit.</automated>
  </verify>
  <done>
    - `05-12-PLAN.md` has 4 inline supersession notes pointing at Phase 999.1's 3.8/4.0 definition
    - `05-12-CONTEXT-ADDENDUM.md` D-12-B-01 has a clearly-delimited supersession note appended (NOT rewriting the original sign-off context)
    - Commit landed on main with all three files (`05-12-PLAN.md`, `05-12-CONTEXT-ADDENDUM.md`, `999.1-VERIFICATION-LOG.md`)
  </done>
  <acceptance_criteria>
    - `grep -c "Phase 999.1" .planning/phases/05-eval-gates-launch/05-12-PLAN.md` returns ≥ 4
    - `grep -c "Supersession note (added 2026-05-14 per Phase 999.1)" .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md` returns exactly 1
    - `grep -c "per_case_min_avg 3.8" .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md` returns ≥ 1
    - `grep -c "aggregate_min_avg 4.0" .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md` returns ≥ 1
    - `git log -1 --pretty=format:%s` includes the string `docs(999.1)` and references D-08 close-out
    - `git log -1 --name-only` lists `.planning/phases/05-eval-gates-launch/05-12-PLAN.md`, `.planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md`, and `.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-VERIFICATION-LOG.md`
    - Original Plan 05-12 launch-gate text (preview 4.20/5-of-5, prod 4.52/5-of-5 evidence) NOT removed or altered — only appended-to (verified by `git diff HEAD~1 -- .planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md` showing only additions, no deletions inside D-12-B-01 except appending the supersession note at the end)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gh` CLI → GitHub Actions API | Authenticated dispatcher; uses Joe's PAT scope `workflow:write` |
| GitHub Actions runner → prod `https://joe-dollinger-chat.com` | Each run is a fresh ubuntu-latest runner; no shared Anthropic cache state (intentional — cold-cache reproduction) |
| Supabase eval_runs / eval_cases rows | Read-only verification queries; no row mutation by this plan |
| Plan 05-12 PLAN.md + CONTEXT-ADDENDUM.md | Plan-time documents; append-only edits preserve original sign-off context per D-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-02-01 | Tampering | A bad-faith executor (or muscle-memory mistake) silently retries the fix-iteration loop instead of kicking back to root-cause when N=3 verification fails | mitigate | Task 1 acceptance criterion includes the literal verdict-string check (`KICK BACK to root-cause per D-04 anti-pattern #4`) and the &lt;done&gt; clause STOPS the orchestrator from advancing to Task 2 if verdict is &lt;3/3 PASS. CONTEXT anti-pattern #4 is enumerated. RESEARCH §Anti-Pattern Reminders restates this. |
| T-999.1-02-02 | Tampering | The supersession note silently overwrites the original Plan 05-12 sign-off context (violates D-07) | mitigate | Task 2 explicitly uses append-only edits with clearly-delimited `---` separators before the new note. Acceptance criterion verifies original text NOT removed (`git diff` shows only additions inside D-12-B-01). |
| T-999.1-02-03 | Repudiation | Future Joe sees `per_case 3.8` and cannot trace why (silent gate-definition drift between Plan 05-12 and 999.x) | mitigate | The D-08 close-out task is the structural mitigation: every Plan 05-12 cat4-PASS reference now carries an inline pointer to Phase 999.1, AND the addendum D-12-B-01 supersession note is the canonical record. Plus the 999.1 SUMMARY (created in &lt;output&gt; step) links back. Four-way cross-linking ensures auditable trail. |
| T-999.1-02-04 | Information Disclosure | VERIFICATION-LOG.md captures Supabase row IDs in a public-ish repo (private until launch but eventually public per project posture) | accept | eval_runs row IDs are opaque nanoids and reveal nothing without Supabase admin credentials. Pattern matches Plan 06-06 VERIFICATION-LOG.md which already lives in the same repo at the same trust posture. |
| T-999.1-02-05 | Denial of Service | The 3 sequential CI dispatches consume ~$1.36 of Anthropic spend (eval + warmup); if SAFETY_GATES_ENABLED is on and the eval-cli email isn't allowlisted, the gates fire | accept | SAFETY_GATES_ENABLED is OFF by default (260512-tku); allowlists for eval-cli@joedollinger.dev exist (260512-r4s/ro4/sne). Cost well under $20/mo Anthropic org cap. If SEED-002 has re-enabled the gates between 999.1-01 merge and 999.1-02 execution, verify allowlist still covers warmup-POST (per Plan 999.1-01 T-999.1-02 mitigation). |
</threat_model>

<verification>
- Task 1: VERIFICATION-LOG.md exists with 3 populated rows; verdict line present; Vercel-log sanity-check noted
- Task 2: 4 inline supersession notes in 05-12-PLAN.md + 1 supersession block in 05-12-CONTEXT-ADDENDUM.md D-12-B-01; close-out commit on main lists all 3 docs
- The 999.1 phase-close SUMMARY (created in &lt;output&gt; below) links to all four artifacts: VERIFICATION-LOG.md, 05-12-PLAN.md amended sections, 05-12-CONTEXT-ADDENDUM.md amended D-12-B-01, and Plan 999.1-01-SUMMARY.md
</verification>

<success_criteria>
- D-04 satisfied: N=3 cold-cache verification ALL PASS (3/3); each run captured in VERIFICATION-LOG.md with concrete IDs and scores
- D-05 satisfied: cold-cache trigger mechanic used was `gh workflow run eval.yml -f target_url=https://joe-dollinger-chat.com -f cats=cat4-judge` × 3 sequential (NOT empty-commit retriggers per RESEARCH Pitfall 6)
- D-07 honored: Plan 05-12 original launch-gate sign-off context preserved (append-only edits; supersession explicitly tagged "forward-looking only")
- D-08 closed: Plan 05-12 PLAN.md + CONTEXT-ADDENDUM.md amended with cross-references to Phase 999.1; 999.1 SUMMARY links back to amended Plan 05-12
- If verification fails (&lt;3/3 PASS): Task 2 does NOT execute; orchestrator records kick-back; no silent threshold-tightening per anti-pattern #4
</success_criteria>

<output>
After both tasks complete (verdict 3/3 PASS), create `.planning/phases/999.1-cat4-prompt-003-cold-cache-borderline-ness-fix/999.1-SUMMARY.md` (phase-close summary, not plan-close) containing:

- **Outcome:** "Phase 999.1 COMPLETE — cat4-prompt-003 cold-cache flake resolved. N=3 cold-cache CI verification: 3/3 PASS."
- **Diff summary** from Plan 999.1-01: cat4-judge.ts (~30 LOC delta), cat-04-voice.yaml (1 numeric edit + comment), tests/lib/eval/cats/cat4-judge.test.ts (6+ new tests across loadCat4Config + warmupSonnetCache).
- **N=3 verification table** (mirror of VERIFICATION-LOG.md, condensed): 3 run-ids + 3 eval_runs row-ids + 3 cat4-prompt-003 avgs + 3 aggregate avgs + verdict.
- **D-08 cross-reference:** explicit markdown links to `.planning/phases/05-eval-gates-launch/05-12-PLAN.md` and `.planning/phases/05-eval-gates-launch/05-12-CONTEXT-ADDENDUM.md` D-12-B-01 supersession note. State: "Plan 05-12 launch-gate at sign-off-time (2026-05-13) was the legacy 4.0/4.0 threshold (D-07 preserved); effective 2026-05-14 the gate is 3.8/4.0 per this phase."
- **Cost summary:** total CI eval cost (from VERIFICATION-LOG.md row sums); estimated invisible Anthropic warmup spend (~$1, cost-isolated per Plan 999.1-01 T-999.1-01).
- **Deferred items:** none expected. If kick-back happened, the SUMMARY records the failure and the next phase number reserved for root-cause investigation.
- **STATE.md + ROADMAP.md updates:** mark Phase 999.1 closed in the backlog section; remove from backlog parking-lot if appropriate.
- **Forward references:** none — this phase has no downstream dependencies in the v1.0 milestone.
</output>
