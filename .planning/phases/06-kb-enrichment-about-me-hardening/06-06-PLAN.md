---
phase: 06-kb-enrichment-about-me-hardening
plan: 06
type: execute
wave: 3
depends_on: ["06-05"]
files_modified:
  - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md
  - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-D-01 hard dep RESOLVED before any verification eval fires: the eval-cli spend-cap exemption (incident follow-up from 2026-05-12) is shipped to src/app/api/chat/route.ts (or wherever the SAFE-04 300¢/day check fires). Task 1 enforces this via grep + git-log preflight gate; failed preflight EXITS the plan, does NOT run any eval. Without this, the ~$5 Phase 6 verification spend re-trips the same rolling-24h cap that caused the 2026-05-12 silent-lockout incident (memory: project_spend_cap_incident_2026-05-12.md)."
    - "Preview eval first, promote second, prod eval third — never promote-then-test (D-B-03 enforced end-to-end). Task 3 runs cat1+cat4 against the Vercel preview alias for the current branch; Task 4 is a checkpoint:human-pick that BLOCKS Task 5 (promote-to-prod) on preview gates green; Task 5 fires only after Joe explicitly resumes; Task 6 runs cat1+cat4 against prod. Sequencing is the durable mitigation against the Plan 05-12 launch-night manual-bypass pattern (LAUNCH-07 narrowing follow-up captured in 05-12-LAUNCH-CHECKLIST.md)."
    - "Four eval_runs row IDs captured in 06-06-VERIFICATION-LOG.md: preview-cat1, preview-cat4, prod-cat1, prod-cat4 (D-F-08 — matches Plan 05-12 LAUNCH-05 row-id capture format). Each row id is the nanoid-shaped value the run-evals.ts orchestrator stores via createRun() in eval_runs.id (see src/lib/eval/storage.ts pattern)."
    - "D-F-02 + D-F-04 (preview gates: cat1=15/15 + cat4 aggregate avg>=4.0 + per_case all pass) MET before Task 5 promote-to-prod fires (Task 4 checkpoint enforces). D-F-03 + D-F-05 (prod gates: cat1=15/15 + cat4>=4.0 + per_case all pass) MET before Task 9 close-out fires (Task 7 checkpoint enforces)."
    - "D-F-06 system-prompt determinism CI test green on the merge commit (Task 8 verifies via gh-CLI inspection of the GitHub Actions workflow run for the merge SHA — same byte-identical-prefix contract preserved by D-B-04 / SAFE-11)."
    - "OQ-04 (friend-test sequencing pre- vs post-Phase-6) SURFACED in Plan 06-06 SUMMARY as decision-point output (Option A: re-DM on enriched artifact; Option B: collect responses on pre-enriched artifact). NOT locked in this plan — Joe records the decision in SUMMARY for downstream reference."
    - "OQ-03 (voice-blind-A-B fresh run) RESOLVED locked-skip: the cat4 LLM judge gate (>=4.0 aggregate + per_case all pass) is the sufficient voice-fidelity verification per CONTEXT.md OQ-03 tentative recommendation. No A/B run is invoked in Plan 06-06; if Plan 06-05 added voice samples that materially shift baseline, that would surface as a cat4 regression and re-open this decision for a future plan."
    - "Phase 6 close-out (D-F-07) is the boundary: SUMMARY committed + ROADMAP.md updated to mark Phase 6 done + STATE.md ## Progress reflects 6/6 phases. If any gate (preview, prod, determinism) fails, Plan 06-06 SUMMARY records the failing state and surfaces for Joe-decision on rollback vs hotfix — does NOT silently mark Phase 6 done."
  artifacts:
    - path: ".planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md"
      provides: "Per-step verification log: preflight gate result (D-D-01 exemption present yes/no), spend-cap reset state pre-run, four eval_runs row IDs with target URL + timestamp + pass/fail per cat, gate decisions (preview + prod), determinism CI run URL + status for merge SHA"
      contains: "eval_runs row id:"
    - path: ".planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md"
      provides: "Phase 6 close-out artifact — references VERIFICATION-LOG.md for evidence; lists final cat1/cat4 pass states on prod; OQ-04 decision-point captured; D-F-01..08 checked off (or failure surfaced)"
      contains: "## Phase 6: COMPLETE"
  key_links:
    - from: ".planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md"
      to: "eval_runs table rows in Supabase (4 rows: preview-cat1, preview-cat4, prod-cat1, prod-cat4)"
      via: "scripts/run-evals.ts createRun() persists a row; the row id is logged to stdout and captured into VERIFICATION-LOG"
      pattern: "(D-F-08 audit-trail pattern — matches Plan 05-12 LAUNCH-CHECKLIST.md row-id capture)"
    - from: "Task 4 checkpoint preview-gate decision"
      to: "Task 5 promote-to-prod fire"
      via: "checkpoint:human-pick resume-signal — Task 5 cannot execute until Joe explicitly resumes with 'preview gates green; proceed' OR 'preview gates failed; stop'"
      pattern: "(D-B-03 preview-first sequencing — same gate-then-promote pattern used at Plan 05-12 LAUNCH-05)"
---

<objective>
Wave 3, Plan 6 of 6 (final plan of Phase 6). Verify the enriched kb/about_me.md + expanded evals/cat-01-fabrication.yaml ground_truth_facts on **preview deploy first**, then promote to prod, then re-verify on prod. Close Phase 6 with cat1=15/15 + cat4 aggregate avg>=4.0 + per_case all pass on BOTH preview and prod (D-F-02..05), system-prompt determinism CI green (D-F-06), four eval_runs row IDs captured (D-F-08), OQ-04 friend-test-sequencing decision surfaced.

**Why this plan exists (D-F-02..08 verification closure):** Plans 06-01..05 produced enriched kb content + expanded ground_truth_facts but did NOT verify those artifacts against the live system. The verification-first-on-preview / promote-second / verify-again-on-prod cycle (D-B-03 locked) is the durable safety pattern: a preview-only cat1 regression catches a fabrication-trap before users ever see it, and a post-promote prod re-verify catches any deploy-time drift (env vars, KB-loading caches, prompt-cache mismatches) that the preview environment didn't surface.

**Critical preflight (D-D-01 hard dep — Task 1 enforces):** This phase costs ~$5 in Anthropic spend across four eval runs. The 2026-05-12 spend-cap incident (memory: project_spend_cap_incident_2026-05-12.md) demonstrated that this exact spend pattern fills the rolling-24h cap silently and kills the agent for hours. The follow-up fix is to exempt the eval-cli email from the SAFE-04 spend-cap check in `src/app/api/chat/route.ts`. **If that fix has not landed when Plan 06-06 starts executing, Task 1 EXITS the plan with a clear `blocked: D-D-01 not yet resolved` status** rather than running verification and recreating the same lockout.

**Sequencing is the durable mitigation:** every prior phase that compressed "verify + promote" into one step ended with the Vercel Deployment Checks gate being manually bypassed (Plan 05-12 LAUNCH-07 follow-up, PR #1 + PR #2 + PR #3 squash-merge nights all required `enforce_admins` toggle). Plan 06-06 keeps preview→prod as a HUMAN-GATED transition specifically to break that pattern.

**Locked decision recorded here (OQ-03 resolution):** Skip the voice-blind-A-B fresh run. The cat4 LLM judge gate (>=4.0 aggregate + per_case all pass) is sufficient voice-fidelity verification per CONTEXT.md OQ-03 tentative recommendation; running an extra A/B doubles cost without changing what the gate measures.

**Surfaced (NOT locked) decision (OQ-04):** Friend-test resume sequencing — Option A (re-DM friends with enriched artifact post-Phase-6) vs Option B (collect responses on pre-enriched artifact). Plan 06-06 SUMMARY records Joe's choice for downstream reference; D-D-03 leaves either option acceptable.

Output: 06-06-VERIFICATION-LOG.md (per-step audit + 4 eval_runs row IDs) + 06-06-SUMMARY.md (phase close-out + OQ-04 decision) + ROADMAP.md updated to mark Phase 6 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-kb-enrichment-about-me-hardening/06-CONTEXT.md
@.planning/phases/06-kb-enrichment-about-me-hardening/06-05-PLAN.md
@.planning/phases/06-kb-enrichment-about-me-hardening/06-01-PLAN.md
@.planning/phases/05-eval-gates-launch/05-12-LAUNCH-CHECKLIST.md
@.planning/seeds/SEED-001-exempt-eval-cli-from-email-ratelimit.md
@.planning/STATE.md
@scripts/run-evals.ts
@evals/cat-01-fabrication.yaml
@evals/cat-04-voice.yaml
@src/app/api/chat/route.ts
@kb/about_me.md

<interfaces>
From CONTEXT.md D-D-01 (hard dep — gates Task 1):
- The eval-cli spend-cap exemption fix MUST land before Phase 6 verification runs against prod
- The fix targets the SAFE-04 / SAFE-09 spend-cap check inside `src/app/api/chat/route.ts` (currently at line 186 per grep — see Task 1 preflight detail for exact grep patterns)
- The canonical exemption pattern: an `EVAL_CLI_EMAILS` allowlist (single-entry: `eval-cli@joedollinger.dev`) consulted BEFORE the spendCap.limit() call; matching emails skip the spend-cap check (per-IP + per-IP-daily still apply as defense-in-depth)
- SEED-001 documents the equivalent rate-limit exemption pattern that may be bundled with the spend-cap exemption in the same fix
- The eval CLI mints `eval-cli@joedollinger.dev` as the session email (src/lib/eval/agent-client.ts line 209) — this is the canonical identity to exempt

From scripts/run-evals.ts (Plan 05-13 argv flags — Task 3 + Task 6 invocation pattern):
- `npm run eval -- --target=<url> --cats=<csv>` — argv flags take precedence over EVAL_TARGET_URL / EVAL_CATS env vars
- Valid --cats values: cat1, cat2, cat3, cat4-judge, cat5, cat6
- Phase 6 verification uses: `--cats=cat1,cat4-judge` (narrowed gate per addendum D-12-B-01 from Plan 05-12)
- Exit code: 0 = all pass, 1 = any fail, 2 = orchestration error
- Each run creates ONE eval_runs row (via createRun); the row id appears in stdout structured logs as `runId` and is captured into VERIFICATION-LOG.md

From CONTEXT.md D-B-03 (locked sequencing):
- preview eval first → Joe reviews preview eval_runs row → promote to prod → prod eval → Joe reviews prod eval_runs row → mark complete
- NEVER promote-then-test

From CONTEXT.md D-F-02..05 (locked verification thresholds):
- D-F-02: cat1 = 15/15 on preview ✓
- D-F-03: cat1 = 15/15 on prod ✓
- D-F-04: cat4 aggregate avg >=4.0 + per_case all pass on preview ✓
- D-F-05: cat4 aggregate avg >=4.0 + per_case all pass on prod ✓
- The "15/15" threshold for cat1 is the D-B-01 hard gate; if Plan 06-05 added a 16th case (strategy=add-new-case), Plan 06-05 acceptance criteria required amending these references in the same commit. Plan 06-06 reads the actual case count from evals/cat-01-fabrication.yaml at runtime, NOT a hard-coded 15.

From CONTEXT.md D-F-06 (determinism contract):
- System-prompt byte-identical determinism test (`tests/lib/system-prompt.test.ts`) must be green in CI for the merge commit
- This is the SAFE-11 canary; any per-request data leak into the cached prefix fails it
- Test runs as part of the standard CI workflow on every push; verification here = inspect the merge SHA's workflow run

From CONTEXT.md D-F-07..08 (close-out conditions):
- D-F-07: ROADMAP.md updated; STATE.md ## Progress reflects 6/6 phases; Phase 6 SUMMARY committed
- D-F-08: 2 preview + 2 prod eval_runs row IDs recorded in SUMMARY (audit trail)

From Plan 05-12 LAUNCH-CHECKLIST.md (precedent for preview→prod verification):
- Row id format example: `OPoI0ljuwE4GlbT_LFh4u` (cat4 prod), `sWLys5bpVsiHAfwvoln04` (cat1 prod)
- These are nanoid-shaped values from the eval_runs.id column
- Captured into the checklist immediately after each run; same pattern reused here

From Plan 05-12 LAUNCH-07 follow-up (preview-to-prod gate bypass history):
- Plan 05-12 night bypassed the gate twice (PR #1 squash-merge + PR #2 banner fix) via enforce_admins toggle + Vercel manual promote
- Reason: the CI workflow ran all 6 cats, while addendum D-12-B-01 narrowed the launch gate to cat1+cat4 only
- Plan 06-06 mitigates by EXPLICITLY narrowing the eval invocation to `--cats=cat1,cat4-judge` (the only two cats that are hard-gate criteria for Phase 6 closure)
- CI workflow narrowing itself is a separate post-launch follow-up (NOT this plan's scope per D-C-03)

From Plan 05-12 incident-followup pattern (npm run eval:reset-spend / reset-rl):
- `npm run eval:reset-spend` clears the rolling-24h spendcap counter (Phase 05.1 added this; resolves the cap headroom)
- `npm run eval:reset-rl` clears the four rate-limit prefixes + daily ipcost counter (Phase 05.1-01)
- Both scripts are operational-hygiene wrappers around `scripts/reset-eval-rate-limits.ts`-class helpers
- Task 2 fires `npm run eval:reset-spend` (and optionally reset-rl) to ensure headroom before the four verification runs

Preview deploy alias resolution:
- Vercel auto-creates a preview alias per PR / per push to non-main branches
- Current branch is `gsd/05-12-task-0-classifier-tune` per `git status` at plan-creation time (2026-05-12)
- The preview URL is obtainable via `vercel ls --token=<token>` or via the GitHub PR comment that the Vercel bot posts; Task 3 includes the explicit lookup step
- Joe may need to provide the preview URL inline if the bot post is missed; Task 3 read_first includes that fallback path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-D-01 preflight gate — verify eval-cli spend-cap exemption has shipped</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-CONTEXT.md (full D-D-01 hard-dep text — the canonical authority on what must be in place)
    - .planning/seeds/SEED-001-exempt-eval-cli-from-email-ratelimit.md (sibling rate-limit exemption; documents the canonical EVAL_CLI_EMAILS allowlist pattern and the eval-cli@joedollinger.dev identity)
    - src/app/api/chat/route.ts (current state — grep target; the SAFE-04 spend-cap check fires around line 186 per existing grep)
    - src/lib/eval/agent-client.ts (line 209 — the eval CLI's session-mint email identity that the exemption must allowlist)
    - .planning/STATE.md (search for "spend-cap exemption" / "D-D-01" / "Item #12" RESOLVED entries — confirms whether the fix has landed since plan creation)
  </read_first>
  <action>
**Preflight gate — Task 1 is the FIRST thing that runs; nothing else in this plan executes unless Task 1 passes.**

**Method (mechanical — clear pass/fail signal):**

1. **Initialize the verification log file.** Create `.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md` with the skeleton structure:

```markdown
# 06-06 Verification Log

**Generated:** 2026-MM-DD
**Plan:** 06-06 (Wave 3, Plan 6 of 6 — Phase 6 verification closure)
**Branch at execution start:** <output of `git rev-parse --abbrev-ref HEAD`>
**HEAD SHA at execution start:** <output of `git rev-parse HEAD`>

## Task 1 — D-D-01 preflight gate

**Check performed:** <one of the three Check methods below; record which>
**Result:** <PASS | FAIL>
**Evidence:** <grep output excerpt OR git log excerpt OR STATE.md entry citation — verbatim, ~3-5 lines>
**Action:** <"Proceeding to Task 2" if PASS; "PLAN HALTED — D-D-01 not yet resolved. Joe must complete the eval-cli spend-cap exemption fix before Plan 06-06 can resume." if FAIL>

## Task 2 — Pre-merge baseline reset
(filled in by Task 2)

## Task 3 — Preview eval invocation
(filled in by Task 3)

## Task 4 — Preview gate decision
(filled in by Task 4)

## Task 5 — Promote to prod
(filled in by Task 5)

## Task 6 — Prod eval invocation
(filled in by Task 6)

## Task 7 — Prod gate decision
(filled in by Task 7)

## Task 8 — Determinism CI verification
(filled in by Task 8)
```

2. **Run the three-pronged preflight check.** Pass on ANY ONE of these methods (defense-in-depth — different evidence categories):

   **Method A — Source-code grep (strongest signal):**
   ```bash
   # PowerShell (current shell per env block):
   Select-String -Pattern "EVAL_CLI_EMAIL|eval-cli@joedollinger\.dev|isEvalCli|evalCliEmail" -Path src/app/api/chat/route.ts
   # bash equivalent:
   # grep -nE 'EVAL_CLI_EMAIL|eval-cli@joedollinger\.dev|isEvalCli|evalCliEmail' src/app/api/chat/route.ts
   ```
   - **PASS** if any line matches AND the match is BEFORE / inside the spend-cap check (current line 186 region — grep also for the `spendcap` lowercase token to confirm the spend-cap context)
   - **FAIL** if no matches OR matches exist only in unrelated regions (e.g., a comment with no enforcement code)

   **Method B — Git-log search (secondary signal, useful if exemption shipped via a refactor that moved the allowlist to another file):**
   ```bash
   # Look for commits between plan creation (2026-05-12) and now that mention the exemption:
   git log --since="2026-05-12" --grep="spend.cap.*exempt|eval-cli.*exempt|EVAL_CLI_EMAIL|SAFE-04.*exempt|incident.*follow-up|D-D-01" --oneline
   # Also check the diff of recent commits touching route.ts:
   git log --since="2026-05-12" --oneline -- src/app/api/chat/route.ts
   ```
   - **PASS** if a commit message clearly describes the exemption shipping (e.g., "fix(chat): exempt eval-cli email from spend-cap per D-D-01" or similar)
   - **FAIL** if no relevant commits or only refactors

   **Method C — STATE.md ledger entry (tertiary signal, defense-in-depth):**
   ```bash
   Select-String -Pattern "D-D-01|eval-cli.*spend.cap|Item.*12.*RESOLVED|spend.cap.*exempt" -Path .planning/STATE.md
   ```
   - **PASS** if a STATE entry explicitly records the fix as RESOLVED with a commit SHA reference
   - **FAIL** if no such entry

3. **Record the result in the verification log.**

   For PASS:
   - Cite which method (A/B/C) triggered the PASS
   - Quote the grep / git-log / STATE excerpt verbatim (~3-5 lines)
   - Record: `Action: Proceeding to Task 2`

   For FAIL (any combination of methods returning no signal):
   - Cite all three methods and the empty results
   - Record: `Action: PLAN HALTED — D-D-01 not yet resolved. Joe must complete the eval-cli spend-cap exemption fix before Plan 06-06 can resume.`
   - **EXIT THIS PLAN.** Do NOT proceed to Task 2. Stop the executor. Surface for Joe-action; resume via `/gsd-execute-phase 06` after the exemption ships.

4. **Defense-in-depth — even if the preflight passes, capture the current rolling-24h spendcap balance for Task 2's reset visibility:**
   ```bash
   # If a query script exists (planner: check scripts/ for any spend-cap inspector — Plan 05-12 incident night likely added one):
   # npx tsx scripts/inspect-spend-cap.ts   # if it exists
   # Otherwise: skip this step; Task 2 will surface the value via reset-spend output
   ```

5. **Commit the verification log skeleton + Task 1 result.** Commit message: `docs(06-06): preflight gate result — D-D-01 <PASS|HALT>`.
  </action>
  <verify>
    <automated>Select-String -Pattern "^\*\*Result:\*\* (PASS|FAIL)$" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</automated>
  </verify>
  <done>06-06-VERIFICATION-LOG.md created with Task 1 result (PASS or FAIL); if FAIL, plan is halted and surfaced for Joe; if PASS, execution continues to Task 2.</done>
  <acceptance_criteria>
    - `.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md` exists
    - File contains literal `# 06-06 Verification Log` heading
    - File contains literal `## Task 1 — D-D-01 preflight gate` section
    - That section contains `**Result:**` followed by either `PASS` or `FAIL` (no other value allowed)
    - That section contains `**Evidence:**` followed by non-empty content (grep / git-log / STATE excerpt)
    - That section contains `**Action:**` followed by either "Proceeding to Task 2" (if PASS) or "PLAN HALTED — D-D-01 not yet resolved..." (if FAIL)
    - File is committed (git status clean for this file)
    - If FAIL: subsequent tasks (2-9) do NOT execute; plan ends with PLAN HALTED state surfaced to Joe
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pre-merge baseline reset — clear rolling-24h spendcap + rate-limit headroom</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 1 result — must be PASS)
    - package.json (verify the `eval:reset-spend` and `eval:reset-rl` npm scripts exist and resolve to scripts/reset-eval-rate-limits.ts or similar; planner: grep `scripts` block)
    - scripts/reset-eval-rate-limits.ts (canonical reset helper — Plan 05.1-01 commit 699c294 added this)
  </read_first>
  <action>
**Method (operational hygiene — defense-in-depth even though Task 1's preflight exemption removes the dependency):**

1. **Confirm npm scripts exist.** Grep package.json for `eval:reset-spend` and `eval:reset-rl`. If either is missing, fall back to invoking the underlying script directly (`npx tsx scripts/reset-eval-rate-limits.ts --target=spendcap` or equivalent — check the script's argv handling).

2. **Run the spendcap reset.** This clears the rolling-24h ipcost counter for the eval-cli identity (defense-in-depth: even with the D-D-01 exemption shipped in Task 1, resetting the counter ensures the verification spend is a clean baseline rather than stacking on accumulated cost).
   ```bash
   npm run eval:reset-spend
   ```
   - Capture the stdout output verbatim into VERIFICATION-LOG.md Task 2 section.
   - Expected: "N keys cleared" output (script reports which Upstash keys it touched).
   - Per Plan 05-12 LAUNCH-CHECKLIST.md follow-up: an `eval-cli@joedollinger.dev:20584` daily-window key persisted in some past runs requiring ad-hoc clearing; if that pattern recurs, log the residual key and surface for Joe.

3. **Run the rate-limit reset** (optional defense-in-depth — the D-D-01 exemption may or may not also exempt rate-limits per SEED-001):
   ```bash
   npm run eval:reset-rl
   ```
   - Capture output verbatim into the log.

4. **Sanity-check current state.** If a `scripts/inspect-spend-cap.ts` or equivalent inspector script exists (planner: check), invoke it post-reset to confirm the spendcap is at zero / near-zero. If no inspector exists, this is informational only — the reset script's own output is the audit trail.

5. **Record in VERIFICATION-LOG.md Task 2 section:**

```markdown
## Task 2 — Pre-merge baseline reset

**Commands run:**
- `npm run eval:reset-spend`: <stdout excerpt>
- `npm run eval:reset-rl`: <stdout excerpt>

**Spendcap state pre-reset:** <if known from inspector>
**Spendcap state post-reset:** <if known from inspector>

**Headroom for verification:** <"clean baseline" if reset succeeded; otherwise flag concern>

**Action:** Proceeding to Task 3 (preview eval).
```

6. **Commit the log update.** Commit message: `docs(06-06): pre-verification baseline reset complete`.
  </action>
  <verify>
    <automated>Select-String -Pattern "^## Task 2 — Pre-merge baseline reset$" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</automated>
  </verify>
  <done>Reset scripts run; output captured in VERIFICATION-LOG.md Task 2 section; spendcap headroom confirmed.</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md contains literal `## Task 2 — Pre-merge baseline reset` heading
    - Task 2 section contains literal `**Commands run:**` line followed by reset-spend stdout excerpt
    - Task 2 section contains `**Action:** Proceeding to Task 3` line
    - `npm run eval:reset-spend` exited 0 (or stdout output confirms success)
    - File is committed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run cat1 + cat4-judge against preview deploy</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 1 + Task 2 sections must be filled; this task appends Task 3)
    - scripts/run-evals.ts (canonical argv flags — `--target` + `--cats=cat1,cat4-judge`; Plan 05-13 W6 wired these)
    - evals/cat-01-fabrication.yaml (current case count — Plan 06-05 may have expanded; planner: `(Select-String -Pattern "^- case_id: cat1-fab-" -Path evals/cat-01-fabrication.yaml).Count` to get N; the pass criterion is N/N, not hard-coded 15)
    - evals/cat-04-voice.yaml (cat4 case structure + pass-criteria reference)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-05-SUMMARY.md (Plan 06-05 outcome — case-count delta + strategy choice; informs whether Plan 06-05 expanded to 16+ cases or stayed at 15)
  </read_first>
  <action>
**Method (preview eval — D-B-03 step 1 of 3):**

1. **Resolve the preview deploy URL.** Vercel auto-publishes a preview alias per push to non-main branches. Three lookup paths in priority order:

   **Path A — GitHub PR comment:** If a PR is open for the current branch, the `vercel[bot]` comment lists the preview URL. Query via gh CLI:
   ```bash
   gh pr view --json comments --jq '.comments[] | select(.author.login == "vercel[bot]") | .body' | Select-String -Pattern "https://resume-agent-eyap[a-z0-9-]+\.vercel\.app" | Select-Object -First 1
   ```

   **Path B — Vercel CLI:** If vercel CLI is authenticated:
   ```bash
   vercel ls resume-agent-eyap --token=<token-from-env> | Select-Object -First 5
   # capture the most recent preview alias
   ```

   **Path C — Joe-provided fallback:** If neither A nor B yields a URL, surface a checkpoint:human-action mini-step inline (request URL from Joe via the `<resume-signal>` mechanism); record the URL in the log and proceed.

   Record the resolved URL in VERIFICATION-LOG.md Task 3 section.

2. **Capture the cat1 case count from evals/cat-01-fabrication.yaml.** Sets the pass threshold dynamically:
   ```bash
   $cat1Count = (Select-String -Pattern "^- case_id: cat1-fab-" -Path evals/cat-01-fabrication.yaml).Count
   # cat1 pass threshold: $cat1Count / $cat1Count (e.g., 15/15 if Plan 06-05 expanded existing; 16/16 if Plan 06-05 added a case)
   ```

3. **Run cat1 against preview.** Invocation uses Plan 05-13 argv flags (NOT env vars — argv takes precedence and is the auditable form):
   ```bash
   npm run eval -- --target=<PREVIEW_URL> --cats=cat1
   ```
   - Capture stdout — specifically the `runId` line (nanoid-shaped, e.g., `sWLys5bpVsiHAfwvoln04`) and the final `eval_run_summary` line (totalCases, passed, failed, totalCostCents, status)
   - Record `runId` as `preview-cat1` row id in VERIFICATION-LOG.md Task 3 section
   - Record pass/fail vs threshold: `cat1 = $passed/$cat1Count on preview, status=<passed|failed>`

4. **Run cat4-judge against preview.** Separate invocation (preserves clean cat-level audit boundaries):
   ```bash
   npm run eval -- --target=<PREVIEW_URL> --cats=cat4-judge
   ```
   - Capture `runId` (preview-cat4)
   - Capture aggregate avg score from stdout (cat4-judge emits the LLM-judge aggregate score; pass threshold = >=4.0 + per_case all pass)
   - Record results in VERIFICATION-LOG.md

5. **VERIFICATION-LOG.md Task 3 section structure:**

```markdown
## Task 3 — Preview eval invocation

**Preview URL:** <resolved URL>
**Resolution method:** <Path A / Path B / Path C>
**Branch at run time:** <git rev-parse --abbrev-ref HEAD>
**HEAD SHA at run time:** <git rev-parse HEAD>

### cat1 on preview

- **Command:** `npm run eval -- --target=<URL> --cats=cat1`
- **runId (eval_runs row id):** <NANOID>
- **Result:** $passed / $cat1Count (cat1 hard-gate: 15/15 if expand-existing strategy; 16/16+ if add-new-case)
- **Status:** <passed | failed>
- **Cost (cents):** <N>
- **Duration (ms):** <N>

### cat4-judge on preview

- **Command:** `npm run eval -- --target=<URL> --cats=cat4-judge`
- **runId (eval_runs row id):** <NANOID>
- **Aggregate avg:** <X.XX>
- **Per-case all pass:** <yes | no>
- **Status:** <passed | failed>
- **Cost (cents):** <N>
- **Duration (ms):** <N>

**Action:** Proceeding to Task 4 (preview gate decision-point).
```

6. **Sanity-checks before proceeding:**
   - Both runIds are nanoid-shaped (match regex `[A-Za-z0-9_-]{20,25}`)
   - Total cost across the two preview runs is in the ~$2-3 range (cat1 ~$1.50, cat4 ~$1); if WAY off, log a concern but proceed
   - If either run exited with status `error` (not `passed` / `failed`), record the failure and proceed to Task 4 — Joe's decision-point will catch and route to halt

7. **Commit the log update.** Commit message: `docs(06-06): preview eval results — cat1=$N/$total, cat4-judge=$X.XX`.

**CRITICAL guard:** Do NOT promote-to-prod here. Task 5 is the promote step, gated by Task 4 (Joe-decision). If Task 3 results look great, the executor must STILL wait for Task 4 — never compress the gate.
  </action>
  <verify>
    <automated>Select-String -Pattern "^- \*\*runId \(eval_runs row id\):\*\* [A-Za-z0-9_-]{20,25}$" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md | Measure-Object | ForEach-Object Count</automated>
  </verify>
  <done>Two preview eval runs complete (cat1 + cat4-judge); both runIds captured in VERIFICATION-LOG.md Task 3; aggregate scores + pass/fail recorded.</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md Task 3 section contains the preview URL on a `**Preview URL:**` line
    - Task 3 section contains TWO `**runId (eval_runs row id):**` lines (one each for cat1 and cat4-judge)
    - Each runId matches regex `[A-Za-z0-9_-]{20,25}` (nanoid shape, matches Plan 05-12 LAUNCH-CHECKLIST.md row-id examples)
    - cat1 section contains a `**Result:** N / M` line where M equals the actual case count in evals/cat-01-fabrication.yaml
    - cat4-judge section contains an `**Aggregate avg:** X.XX` line and a `**Per-case all pass:** yes | no` line
    - Task 3 section ends with `**Action:** Proceeding to Task 4` (regardless of pass/fail — gate decision is Task 4's job)
    - File is committed
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Joe reviews preview eval results — gate decision-point before promote-to-prod</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 3 section — preview cat1 + cat4 results)
    - kb/about_me.md (final merged state — for context if a failure surfaces a content concern)
    - evals/cat-01-fabrication.yaml (for cat1 failure debugging — which case failed, what was the rationale)
    - Vercel preview alias deploy page (Joe can spot-check the chat UI live if cat4 voice failure is borderline)
  </read_first>
  <what-built>Joe-locked decision on whether preview gates are green and Plan 06-06 can proceed to promote-to-prod (Task 5). The hard-gate criteria are objective (cat1 = $cat1Count/$cat1Count + cat4 aggregate avg >=4.0 + per_case all pass) but Joe owns the call so a borderline cat4 score doesn't auto-promote.</what-built>
  <how-to-verify>
    **Step A — Read VERIFICATION-LOG.md Task 3 results.** Specifically:
    - cat1 result line (N / total): MUST equal total (15/15 if Plan 06-05 expand-existing; 16/16 if add-new-case)
    - cat4-judge aggregate avg: MUST be >=4.0
    - cat4-judge per-case all pass: MUST be yes

    **Step B — Make the decision and record at the bottom of Task 4 section:**

    **Option PROCEED (all gates green):**
    Both cat1=N/N AND cat4>=4.0 + per_case all pass. Record:
    ```markdown
    ## Task 4 — Preview gate decision

    **Decision:** PROCEED to Task 5 (promote-to-prod)

    **Rationale:** cat1=$N/$N (hard-gate met); cat4 aggregate=$X.XX (>=4.0); per-case all pass.

    **Signed:** Joe Dollinger, 2026-MM-DD
    ```

    **Option HALT (any gate failed):**
    Either cat1 < N/N OR cat4 < 4.0 OR cat4 per-case any-fail OR cat4-judge run errored. Record:
    ```markdown
    ## Task 4 — Preview gate decision

    **Decision:** HALT — preview gate failed; do NOT promote-to-prod.

    **Failure details:** <which gate failed; specific case names; rationale snippet from the runId's eval_runs row in Supabase>

    **Next action:** Joe-decision — options are: (1) fix the content in kb/about_me.md and re-run from Task 3, (2) expand evals/cat-01-fabrication.yaml ground_truth_facts if it's a per-case-isolation false-positive (Plan 06-05 pattern), (3) accept the failure as a regression and abort Phase 6.

    **Signed:** Joe Dollinger, 2026-MM-DD
    ```

    **Step C — If HALT:** the executor MUST NOT proceed to Task 5. Plan is paused. Resume signal "preview failed; HALT" surfaces to Joe; he triages per the Next-action options listed in Step B.

    **Step D — If PROCEED:** the executor will run Task 5 after Joe's resume signal. NOTE: Task 5 is also human-gated (it's a manual promotion via merge / Vercel UI / gh CLI push); Joe is the actor. Task 4's PROCEED decision is the gate, but the actual mechanical action of promoting happens in Task 5.

    **Step E — Commit the VERIFICATION-LOG.md update.**
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; — Joe reads Task 3 results in the log, decides PROCEED or HALT, records decision + rationale in VERIFICATION-LOG.md Task 4 section, signs, commits. Replies via &lt;resume-signal&gt; with the verdict.</action>
  <verify>
    <automated>Select-String -Pattern "^\*\*Decision:\*\* (PROCEED to Task 5|HALT)" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</automated>
  </verify>
  <done>Task 4 section has explicit Decision (PROCEED or HALT) + Rationale + Signed line; file committed.</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md contains literal `## Task 4 — Preview gate decision` heading
    - Task 4 section contains literal `**Decision:**` line followed by `PROCEED to Task 5 (promote-to-prod)` OR `HALT — preview gate failed; do NOT promote-to-prod.`
    - Task 4 section contains literal `**Rationale:**` line with non-empty content (Joe wrote rationale)
    - Task 4 section contains literal `**Signed:** Joe Dollinger` line with a date
    - If Decision = HALT: subsequent tasks (5-9) do NOT execute until Joe re-resumes with a separate Plan 06-06 fix-and-retry cycle
    - File is committed
  </acceptance_criteria>
  <resume-signal>"Preview gate: PROCEED — cat1=$N/$N, cat4=$X.XX. Promoting to prod next." OR "Preview gate: HALT — &lt;failure summary&gt;; need to triage before re-running."</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Joe promotes the merged branch to prod (merge PR / push to main)</name>
  <files>(no .planning/ file edits — this is a GitHub-side action; Task 5's log entry is appended after the fact)</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 4 Decision MUST be PROCEED before this task fires)
    - The GitHub PR for the current branch (if PR is open) — review the file changes one more time before merge as final defense
    - Vercel dashboard for `resume-agent-eyap` project (Deployment Checks gate state; production target)
  </read_first>
  <what-built>The current branch's accumulated Phase 6 changes (kb/about_me.md merge + evals/cat-01-fabrication.yaml expansion + optional kb/voice.md additions + all 06-0N-*.md planning artifacts) are now on main and deployed to https://joe-dollinger-chat.com.</what-built>
  <how-to-verify>
    **Step A — Confirm Task 4 Decision = PROCEED.** Re-read VERIFICATION-LOG.md Task 4 section. If Decision != PROCEED, STOP — this task should not be running.

    **Step B — Choose promote path:**

    **Path 1 — Via GitHub PR merge (preferred; preserves CI gate):**
    ```bash
    gh pr view --json url,statusCheckRollup
    # Confirm Vercel Deployment Checks "Vercel - resume-agent-eyap: eval" gate passing
    # If passing:
    gh pr merge --squash --auto   # or --merge for merge-commit style; squash matches project pattern
    ```
    The Vercel `Vercel - resume-agent-eyap: eval` check enforces the in-CI eval pass (currently runs all 6 cats per LAUNCH-07 follow-up — note: this is a known broader workflow that includes cat2/3/5/6 baselines which are documented-but-not-blocking per Plan 05-12 D-12-B-02; if those fail, Joe may need to manually-promote via Vercel per the established override pattern, but the cat1+cat4-judge SUBSET that Phase 6 cares about is what Plan 06-06 Task 3 already verified).

    **Path 2 — Direct push to main (only if PR doesn't exist or PR workflow is broken):**
    ```bash
    git checkout main
    git pull
    git merge --squash <feature-branch>
    git commit -m "merge: Phase 6 KB enrichment — about-me hardening (per 06-CONTEXT.md D-A..D-F)"
    git push origin main
    ```

    **Step C — Watch the prod deploy.** Vercel auto-deploys main pushes. Watch the dashboard or run:
    ```bash
    vercel inspect <deployment-url> --token=<token>   # if CLI authenticated
    # or
    gh run watch   # if a GitHub Actions workflow is the gate
    ```
    Wait for the deploy to reach READY state on https://joe-dollinger-chat.com.

    **Step D — Smoke-check the prod deploy.** Curl the health endpoint:
    ```bash
    curl -s https://joe-dollinger-chat.com/api/health | ConvertFrom-Json
    # All 5 dependencies should be green (anthropic / classifier / supabase / upstash / exa) per Plan 05-12 health-banner pattern
    ```
    If any dep is degraded, log the concern in VERIFICATION-LOG.md but proceed (the prod cat1+cat4 verification in Task 6 will surface real issues).

    **Step E — Record promotion in VERIFICATION-LOG.md Task 5 section:**

    ```markdown
    ## Task 5 — Promote to prod

    **Promote path:** <Path 1 (PR merge) | Path 2 (direct push)>
    **Merge SHA (squash result on main):** <full SHA>
    **Vercel deploy URL:** <auto-generated alias>
    **Production target:** https://joe-dollinger-chat.com
    **Deploy READY at:** <timestamp>
    **Health smoke-check:** <all 5 deps green | concerns: list>

    **Action:** Proceeding to Task 6 (prod eval).
    ```

    **Step F — Commit the log update.** Commit message: `docs(06-06): promote-to-prod complete (merge SHA $SHA)`.
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; — Joe executes the merge / push (Step B Path 1 or 2), watches the deploy (Step C), runs health smoke (Step D), and records in VERIFICATION-LOG.md Task 5 section (Step E). Resume signal with merge SHA.</action>
  <verify>
    <automated>Select-String -Pattern "^\*\*Merge SHA \(squash result on main\):\*\* [a-f0-9]{7,40}$" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</automated>
  </verify>
  <done>Phase 6 changes merged to main; prod deploy READY; health smoke green; merge SHA recorded in VERIFICATION-LOG.md.</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md contains literal `## Task 5 — Promote to prod` heading
    - Task 5 section contains a `**Merge SHA (squash result on main):**` line with a 7-40 char hex SHA
    - Task 5 section contains a `**Deploy READY at:**` line with a timestamp
    - Task 5 section contains a `**Health smoke-check:**` line
    - `curl https://joe-dollinger-chat.com/api/health` returns 200 with a JSON body containing 5 dep entries
    - Task 5 section ends with `**Action:** Proceeding to Task 6`
    - File is committed
  </acceptance_criteria>
  <resume-signal>"Promoted to prod: merge SHA &lt;SHA&gt;, deploy READY, health green. Running prod eval next."</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Run cat1 + cat4-judge against prod (https://joe-dollinger-chat.com)</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 5 Promote MUST be done before this fires; merge SHA captured)
    - scripts/run-evals.ts (argv-flag pattern — same invocation shape as Task 3, but with `--target=https://joe-dollinger-chat.com`)
    - evals/cat-01-fabrication.yaml (case count for pass threshold — same dynamic-read pattern as Task 3)
  </read_first>
  <action>
**Method (prod eval — D-B-03 step 3 of 3):**

1. **Confirm Task 5 is done.** Re-read VERIFICATION-LOG.md Task 5 section; confirm `**Merge SHA**` is recorded and deploy is READY.

2. **Optional: Reset spendcap again** (~30 min since Task 2's reset; if the preview runs in Task 3 + a fresh prod deploy used spend, the rolling-24h counter may have meaningful balance). Defense-in-depth — the D-D-01 exemption (verified at Task 1) means this isn't strictly necessary, but cheap insurance:
   ```bash
   npm run eval:reset-spend
   ```
   Log output briefly in VERIFICATION-LOG.md Task 6 section.

3. **Run cat1 against prod.**
   ```bash
   npm run eval -- --target=https://joe-dollinger-chat.com --cats=cat1
   ```
   - Capture `runId` (prod-cat1) — nanoid-shaped
   - Capture pass/fail vs threshold (same dynamic-N-from-yaml pattern as Task 3)
   - Capture cost (cents) + duration

4. **Run cat4-judge against prod.**
   ```bash
   npm run eval -- --target=https://joe-dollinger-chat.com --cats=cat4-judge
   ```
   - Capture `runId` (prod-cat4)
   - Capture aggregate avg + per-case-all-pass + status

5. **VERIFICATION-LOG.md Task 6 section structure:**

```markdown
## Task 6 — Prod eval invocation

**Production target:** https://joe-dollinger-chat.com
**Merge SHA on prod:** <from Task 5>
**Spendcap reset (optional):** <if run, stdout excerpt>

### cat1 on prod

- **Command:** `npm run eval -- --target=https://joe-dollinger-chat.com --cats=cat1`
- **runId (eval_runs row id):** <NANOID>
- **Result:** $passed / $cat1Count (D-F-03 threshold)
- **Status:** <passed | failed>
- **Cost (cents):** <N>
- **Duration (ms):** <N>

### cat4-judge on prod

- **Command:** `npm run eval -- --target=https://joe-dollinger-chat.com --cats=cat4-judge`
- **runId (eval_runs row id):** <NANOID>
- **Aggregate avg:** <X.XX>
- **Per-case all pass:** <yes | no>
- **Status:** <passed | failed>
- **Cost (cents):** <N>
- **Duration (ms):** <N>

**Action:** Proceeding to Task 7 (prod gate decision-point).
```

6. **Sanity-checks:**
   - Both runIds nanoid-shaped (regex `[A-Za-z0-9_-]{20,25}`)
   - Total cost across the two prod runs is ~$2-3 (cat1 ~$1.50, cat4 ~$1)
   - If either exited with status `error`, record and proceed to Task 7 (which routes the failure)

7. **Commit:** `docs(06-06): prod eval results — cat1=$N/$total, cat4-judge=$X.XX`.

**Same critical guard as Task 3:** Do NOT close-out Phase 6 here. Task 7 is the gate decision; Task 9 is the close-out. Never compress.
  </action>
  <verify>
    <automated>(Select-String -Pattern "^- \*\*runId \(eval_runs row id\):\*\* [A-Za-z0-9_-]{20,25}$" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md | Measure-Object).Count -ge 4</automated>
  </verify>
  <done>Two prod eval runs complete (cat1 + cat4-judge); both runIds captured in VERIFICATION-LOG.md Task 6; aggregate scores + pass/fail recorded; total 4 runIds in the log (2 preview from Task 3 + 2 prod from Task 6).</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md Task 6 section contains `**Production target:** https://joe-dollinger-chat.com` line
    - Task 6 section contains TWO `**runId (eval_runs row id):**` lines (one each for cat1 and cat4-judge)
    - Each runId matches regex `[A-Za-z0-9_-]{20,25}`
    - File now has FOUR total runId entries (Task 3 preview pair + Task 6 prod pair) — closing D-F-08 (2 preview + 2 prod recorded)
    - cat1 Result line matches the actual case count in evals/cat-01-fabrication.yaml
    - cat4-judge has both `**Aggregate avg:**` and `**Per-case all pass:**` lines
    - Task 6 section ends with `**Action:** Proceeding to Task 7`
    - File is committed
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Joe reviews prod eval results — phase-close gate decision-point</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 6 section — prod cat1 + cat4 results; also Task 3 for preview-vs-prod comparison if relevant)
    - https://joe-dollinger-chat.com (Joe can spot-check the live chat directly if cat4 voice borderline)
  </read_first>
  <what-built>Joe-locked decision on whether prod gates are green and Phase 6 closes (D-F-03 + D-F-05 met → proceed to Task 8 + Task 9), or whether rollback / hotfix is needed (D-F-03 or D-F-05 failed → halt and decide rollback strategy).</what-built>
  <how-to-verify>
    **Step A — Read VERIFICATION-LOG.md Task 6 results:**
    - cat1 prod result: MUST equal total cases (D-F-03)
    - cat4-judge prod aggregate avg: MUST be >=4.0 (D-F-05)
    - cat4-judge prod per-case all pass: MUST be yes (D-F-05)

    **Step B — Compare to preview (Task 3) results.** If prod and preview are wildly divergent (e.g., preview 15/15 + prod 12/15), that's a deploy-time drift signal — env var, prompt-cache mismatch, or KB-loader bundling difference. Flag in the decision rationale.

    **Step C — Make the decision:**

    **Option PROCEED-TO-CLOSE (all prod gates green):**
    ```markdown
    ## Task 7 — Prod gate decision

    **Decision:** PROCEED to Task 8 + Task 9 (close-out)

    **Rationale:** Prod cat1=$N/$N (D-F-03 met); prod cat4 aggregate=$X.XX (D-F-05 met); per-case all pass.

    **Preview vs prod parity check:** <"matched within expected variance" | "drift noted: preview=X, prod=Y — investigate post-close as Phase 6 follow-up">

    **Signed:** Joe Dollinger, 2026-MM-DD
    ```

    **Option ROLLBACK (any prod gate failed):**
    ```markdown
    ## Task 7 — Prod gate decision

    **Decision:** ROLLBACK — prod gate failed; Phase 6 close-out blocked.

    **Failure details:** <which gate failed; case names; rationale snippet>

    **Rollback options:**
    - Option A: Vercel rollback to prior production deploy (instant; recovers live UX; Phase 6 work stays on main but unactivated; revert commit lands in a separate plan)
    - Option B: Hotfix via new commit (specific kb/about_me.md edit OR evals/cat-01-fabrication.yaml ground_truth_facts expansion); requires re-running Task 6 against the fresh deploy
    - Option C: Accept the regression as documented (rare; only if the failing case is itself contested — e.g., a cat4 judge calibration issue Plan 06-05 missed)

    **Joe's choice:** <A | B | C>
    **Next action:** <specific step>

    **Signed:** Joe Dollinger, 2026-MM-DD
    ```

    **Step D — If PROCEED-TO-CLOSE:** Task 8 (determinism check) + Task 9 (SUMMARY) fire next.
    **Step D' — If ROLLBACK:** Plan 06-06 pauses. Joe executes the chosen rollback option; resumes via a new Plan 06-06 cycle if needed, or surfaces for a fresh plan if the rollback uncovered a structural issue.

    **Step E — Commit the VERIFICATION-LOG.md update.**
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; — Joe reads Task 6 results in the log, decides PROCEED-TO-CLOSE or ROLLBACK, records decision + rationale + parity check (PROCEED) or rollback option (ROLLBACK), signs, commits.</action>
  <verify>
    <automated>Select-String -Pattern "^\*\*Decision:\*\* (PROCEED to Task 8 \+ Task 9|ROLLBACK)" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</automated>
  </verify>
  <done>Task 7 section has explicit Decision + Rationale + Signed line; file committed.</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md contains literal `## Task 7 — Prod gate decision` heading
    - Task 7 section contains `**Decision:**` line ending in either `PROCEED to Task 8 + Task 9 (close-out)` OR `ROLLBACK — prod gate failed; Phase 6 close-out blocked.`
    - Task 7 section contains `**Rationale:**` (PROCEED) or `**Failure details:**` (ROLLBACK) with non-empty content
    - Task 7 section contains `**Signed:** Joe Dollinger` with a date
    - If Decision = ROLLBACK: Tasks 8 + 9 do NOT execute; plan pauses for Joe's rollback workflow
    - File is committed
  </acceptance_criteria>
  <resume-signal>"Prod gate: PROCEED — cat1=$N/$N, cat4=$X.XX; closing Phase 6." OR "Prod gate: ROLLBACK — &lt;failure summary&gt;; rolling back via &lt;Option A|B|C&gt;."</resume-signal>
</task>

<task type="auto">
  <name>Task 8: Verify SAFE-11 system-prompt determinism CI test green on merge commit</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Task 5 merge SHA + Task 7 PROCEED decision must both be in place)
    - tests/lib/system-prompt.test.ts (the canonical determinism test file — Glob confirmed at planner time)
    - src/lib/system-prompt.ts (the test target; KB-content additions are static and should not break determinism)
    - .github/workflows/ (planner: list to find the CI workflow that runs system-prompt.test.ts — typically named ci.yml or eval.yml)
  </read_first>
  <action>
**Method (D-F-06 verification — confirm the determinism canary is green on the merge SHA):**

1. **Identify the CI workflow that runs system-prompt.test.ts.** The test lives at `tests/lib/system-prompt.test.ts`; the CI workflow is in `.github/workflows/`. Common names: `ci.yml`, `test.yml`. The eval workflow (`eval.yml` or similar) is separate. Run:
   ```bash
   gh workflow list
   ```
   to enumerate workflows; pick the one that runs `npm test` / Vitest on all PRs and pushes.

2. **Find the workflow run for the merge SHA** (captured in VERIFICATION-LOG.md Task 5):
   ```bash
   $mergeSha = "<from VERIFICATION-LOG.md Task 5>"
   gh run list --workflow=<workflow-name> --commit=$mergeSha --json status,conclusion,url,name,databaseId | ConvertFrom-Json
   ```
   - Find the run whose `head_sha` matches `$mergeSha`
   - Read `status` (completed | in_progress | queued) and `conclusion` (success | failure | cancelled | null)

3. **If `status` is in_progress or queued:**
   - Wait (or use `gh run watch <databaseId>` to follow)
   - Do not proceed until run is `completed`

4. **If `conclusion` is success:** the determinism test passed as part of the broader CI suite. Capture evidence:
   ```bash
   gh run view <databaseId> --log | Select-String -Pattern "system-prompt\.test|determinism"
   ```
   This pulls the test-runner output lines showing the system-prompt test ran and passed.

5. **If `conclusion` is failure:**
   - Read which test failed: `gh run view <databaseId> --log-failed | Select-String -Pattern "FAIL|✗" -Context 0,5`
   - If the failing test is `system-prompt.test.ts` (the determinism canary) → SAFE-11 break; this is a structural Phase 6 failure. Record in VERIFICATION-LOG.md and surface for Joe-action (likely rollback per Task 7 Option A). D-F-06 NOT met.
   - If the failing test is unrelated (e.g., a flaky integration test) → record the failing test name but mark D-F-06 met (determinism specifically passed). This is borderline; lean strict — if the run conclusion is `failure`, default to flagging as not-met and let Joe override.

6. **Record in VERIFICATION-LOG.md Task 8 section:**

```markdown
## Task 8 — Determinism CI verification (D-F-06)

**Merge SHA:** <from Task 5>
**Workflow:** <workflow name>
**Run URL:** <gh runs URL>
**Run conclusion:** <success | failure | cancelled>
**Determinism test status:** <PASS | FAIL>

**Evidence (test-runner output excerpt):**
```
<gh run view --log excerpt showing system-prompt.test.ts pass/fail lines>
```

**D-F-06 met:** <yes | no>
**Action:** Proceeding to Task 9 (close-out SUMMARY).
```

7. **Commit:** `docs(06-06): determinism CI verification — D-F-06 <met|not-met>`.

**Note:** If CI is slow or the workflow has not yet completed by the time this task runs, the executor should wait (the determinism test is fast — sub-second; full CI suite usually completes within ~3-5 min). Do NOT skip this check; D-F-06 is a Phase 6 close-out condition.
  </action>
  <verify>
    <automated>Select-String -Pattern "^\*\*D-F-06 met:\*\* (yes|no)$" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md</automated>
  </verify>
  <done>VERIFICATION-LOG.md Task 8 section records CI run URL, conclusion, determinism test status, and explicit `D-F-06 met: yes | no` line.</done>
  <acceptance_criteria>
    - VERIFICATION-LOG.md contains literal `## Task 8 — Determinism CI verification (D-F-06)` heading
    - Task 8 section contains `**Merge SHA:**` line with a 7-40 char hex SHA
    - Task 8 section contains `**Run URL:**` line with a GitHub Actions URL
    - Task 8 section contains `**Run conclusion:**` line (success / failure / cancelled)
    - Task 8 section contains `**Determinism test status:**` line (PASS | FAIL)
    - Task 8 section contains `**D-F-06 met:** yes | no` line (one only)
    - If `D-F-06 met: no`: Task 9 still runs but the SUMMARY records Phase 6 as NOT-FULLY-CLOSED (D-F-06 failure surfaced for Joe-action)
    - File is committed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 9: Write Phase 6 SUMMARY, surface OQ-04, update ROADMAP.md + STATE.md</name>
  <files>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md (Tasks 1-8 all complete; this task synthesizes into SUMMARY)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-CONTEXT.md (D-F-01..08 close-out conditions — every one gets a checkmark or explicit non-met note)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-01-CLAIM-MATRIX.md (OQ-04 decision may have been pre-recorded here at Plan 06-01 Task 3 Step C; SUMMARY pulls forward whatever Joe decided OR surfaces as not-yet-decided)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md, 06-04-SUMMARY.md, 06-05-SUMMARY.md (prior plans' close-out artifacts — cited in this SUMMARY's lineage section)
    - .planning/ROADMAP.md (current Phase 6 placeholder + progress line)
    - .planning/STATE.md (## Progress + ## Decisions blocks)
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY template — match its structure)
  </read_first>
  <action>
**Method (close-out artifact write — match Plan 05-12 LAUNCH-CHECKLIST format for cross-phase consistency):**

1. **Write `.planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md`** with this structure:

```markdown
# --- (YAML frontmatter — literal triple-dash on actual SUMMARY)
phase: 06-kb-enrichment-about-me-hardening
plan: 06
type: summary
status: <COMPLETE | COMPLETE-WITH-FOLLOWUP | NOT-CLOSED>
closed_date: 2026-MM-DD
merge_sha: <from VERIFICATION-LOG Task 5>
# --- (end YAML frontmatter — literal triple-dash on actual SUMMARY)

# Phase 6 — KB enrichment: about-me hardening — Plan 06-06 SUMMARY

**Status:** ## Phase 6: COMPLETE  (or COMPLETE-WITH-FOLLOWUP / NOT-CLOSED if any gate failed)
**Closed:** 2026-MM-DD
**Final merge SHA on main:** <from VERIFICATION-LOG Task 5>

## D-F-01..08 Close-out Conditions

- [x|✗] D-F-01: kb/about_me.md merged content reviewed and Joe-signed (Plan 06-03 sign-off — see 06-03-SUMMARY.md)
- [x|✗] D-F-02: cat1 = $N/$N on **preview** ✓ (Plan 06-06 Task 3; eval_runs row: <preview-cat1-runId>)
- [x|✗] D-F-03: cat1 = $N/$N on **prod** ✓ (Plan 06-06 Task 6; eval_runs row: <prod-cat1-runId>)
- [x|✗] D-F-04: cat4 aggregate avg >=4.0 + per_case all pass on **preview** ✓ (avg=$X.XX, eval_runs row: <preview-cat4-runId>)
- [x|✗] D-F-05: cat4 aggregate avg >=4.0 + per_case all pass on **prod** ✓ (avg=$X.XX, eval_runs row: <prod-cat4-runId>)
- [x|✗] D-F-06: System-prompt determinism CI test green on merge SHA (Plan 06-06 Task 8)
- [x|✗] D-F-07: ROADMAP.md updated; STATE.md ## Progress reflects 6/6 phases; this SUMMARY committed
- [x|✗] D-F-08: Four eval_runs row IDs recorded (above + in 06-06-VERIFICATION-LOG.md)

## Four eval_runs row IDs (D-F-08 audit trail)

| Environment | Category | Run ID | Pass result | Cost (cents) |
|-------------|----------|--------|-------------|--------------|
| Preview     | cat1     | <id>   | $N/$N       | <N>          |
| Preview     | cat4-judge | <id> | avg=$X.XX, per-case all pass=<yes|no> | <N> |
| Prod        | cat1     | <id>   | $N/$N       | <N>          |
| Prod        | cat4-judge | <id> | avg=$X.XX, per-case all pass=<yes|no> | <N> |

Total verification spend: ~$<X> across 4 runs.

## Open Questions Resolved / Surfaced

**OQ-03 (voice-blind-A-B fresh run):** RESOLVED locked-skip in Plan 06-06 must_haves. Cat4 LLM judge gate (>=4.0 aggregate + per_case all pass) is the sufficient voice-fidelity verification per CONTEXT.md OQ-03 tentative recommendation. No A/B run invoked. Verification gates met above.

**OQ-04 (friend-test sequencing pre- vs post-Phase-6):** SURFACED (not locked here).
- **Joe's recorded decision:** <Option A (re-DM friends with enriched artifact post-Phase-6) | Option B (collect responses on pre-enriched artifact and treat Phase 6 as post-friend-test enrichment) | defer-to-Plan-05-12-LAUNCH-CHECKLIST-update>
- **Source of the decision:** <Plan 06-01 Task 3 Step C if pre-recorded there | Plan 06-06 Task 9 if recorded here>
- **Rationale:** <one line from Joe>
- **Downstream action:** If Option A, Plan 05-12 LAUNCH-CHECKLIST.md follow-up entry needs DM re-issuance; if Option B, current friend-test responses (pending per LAUNCH-04) apply to the pre-enriched artifact and v1.0 friend-test sign-off can continue on those.

## What changed

- **kb/about_me.md:** <one-sentence summary — disposition counts from Plan 06-03 SUMMARY>
- **evals/cat-01-fabrication.yaml:** ground_truth_facts expanded — $N new entries across $M cases (per Plan 06-05 SUMMARY)
- **kb/voice.md:** <samples added: 0 / 1 / 2>
- **No changes to:** src/app/api/chat/route.ts, src/lib/kb-loader.ts, src/lib/system-prompt.ts, UI, infra, CI workflows (D-C-01..03 scope boundaries honored)

## Lineage

- Brainstorm: 2026-05-12 EOD (post spend-cap incident)
- Plans: 06-01 (intake + claim matrix) → 06-02 (strip) → 06-03 (merge) → 06-04 (voice rewrite) → 06-05 (cat1 facts expansion) → 06-06 (verification) — this plan
- Sibling SUMMARYs: [06-01-SUMMARY.md], [06-02-SUMMARY.md], [06-03-SUMMARY.md], [06-04-SUMMARY.md], [06-05-SUMMARY.md]
- Verification audit: [06-06-VERIFICATION-LOG.md]
- Precedent for KB-expansion workflow: [05-12-LAUNCH-CHECKLIST.md "Open follow-ups"]
- Incident lineage: [memory: project_spend_cap_incident_2026-05-12.md]
- D-D-01 sibling: [SEED-001-exempt-eval-cli-from-email-ratelimit.md]

## Carry-forward for v1.0 close + v1.1

- **Friend-test responses** (LAUNCH-04 pending) — sequencing per OQ-04 decision above
- **775-line consolidated resume.md** — held back per D-C-04; will be addressed as Phase 7 (sequential under v1.0) or v1.1 Phase 1
- **LAUNCH-07 follow-up** (CI workflow narrowing to cat1+cat4-judge only) — still open from Plan 05-12; orthogonal to Phase 6
- **Banner false-green for classifier** (WR-01 from 05-REVIEW.md) — still open from Plan 05-12
```

2. **Update ROADMAP.md.** Find the Phase 6 section; mark the plans checklist complete:
   ```markdown
   ### Phase 6: KB enrichment — about-me hardening
   **Status:** ✓ Complete (closed 2026-MM-DD; SUMMARY: [06-06-SUMMARY.md])
   Plans:
   - [x] 06-01-PLAN.md — Intake + claim matrix
   - [x] 06-02-PLAN.md — Strip agent expansion
   - [x] 06-03-PLAN.md — Section-by-section merge
   - [x] 06-04-PLAN.md — Voice rewrite
   - [x] 06-05-PLAN.md — Expand cat1 ground_truth_facts
   - [x] 06-06-PLAN.md — Verify + close
   ```
   Also update the milestone progress line if ROADMAP.md tracks it (e.g., "v1.0 — 6 of 6 phases complete" or similar).

3. **Update STATE.md `## Progress` block.** Append an entry:
   ```markdown
   - 2026-MM-DD: Phase 6 (KB enrichment: about-me hardening) CLOSED.
     - Final cat1 prod: $N/$N (runId: <prod-cat1-id>)
     - Final cat4 prod: avg=$X.XX, per-case all pass (runId: <prod-cat4-id>)
     - Merge SHA: <SHA>
     - OQ-04 friend-test sequencing: <Joe's choice>
     - v1.0 phase count: 6/6 ✓
   ```

4. **Sanity-checks before final commit:**
   - VERIFICATION-LOG.md is complete (all 8 tasks have their sections filled)
   - All four runIds appear in both VERIFICATION-LOG.md AND 06-06-SUMMARY.md (cross-reference integrity)
   - D-F-01..08 checkboxes match the actual evidence in VERIFICATION-LOG.md (no `[x]` next to a `D-F-06 met: no` line)
   - If any D-F is `✗`, the SUMMARY status is `COMPLETE-WITH-FOLLOWUP` or `NOT-CLOSED`, NOT `COMPLETE`

5. **Commit all three files in one commit** (atomic phase-close): `docs(06): close Phase 6 — KB enrichment complete; cat1=$N/$N prod, cat4=$X.XX prod, 4 eval_runs row IDs captured`.
  </action>
  <verify>
    <automated>(Select-String -Pattern "## Phase 6: COMPLETE" -Path .planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md) -and ((Select-String -Pattern "Phase 6.*CLOSED|6/6 phases" -Path .planning/STATE.md).Count -ge 1) -and ((Select-String -Pattern "Phase 6:.*Complete|\[x\] 06-06-PLAN" -Path .planning/ROADMAP.md).Count -ge 1)</automated>
  </verify>
  <done>06-06-SUMMARY.md committed with all D-F-01..08 conditions checked and four eval_runs row IDs recorded; ROADMAP.md updated with Phase 6 complete status; STATE.md ## Progress entry appended; OQ-04 decision recorded.</done>
  <acceptance_criteria>
    - `.planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md` exists
    - File contains literal `## Phase 6: COMPLETE` line (or `COMPLETE-WITH-FOLLOWUP` / `NOT-CLOSED` if any D-F failed — and the line matches the actual gate state)
    - File contains a table with 4 rows under `## Four eval_runs row IDs (D-F-08 audit trail)` — one per (env, category) combo, each runId matching nanoid regex
    - File contains literal `## Open Questions Resolved / Surfaced` heading with OQ-03 and OQ-04 entries
    - OQ-04 entry contains `**Joe's recorded decision:**` with non-empty value (Option A | Option B | defer-to-X)
    - File contains D-F-01..08 checklist with each item either `[x]` or `[✗]` (no blanks)
    - `.planning/ROADMAP.md` updated — Phase 6 section shows ✓ Complete status
    - `.planning/STATE.md` updated — ## Progress entry for Phase 6 close-out present
    - Single commit covers all three files (atomic phase-close)
    - `npx tsc --noEmit` exit 0 (no TS impact — these are doc edits)
    - `npm run build` exit 0 (no build break)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval CLI requests → /api/chat (prod) | Public-facing endpoint; spend-cap + rate-limit + classifier protections fire on every request. Plan 06-06 verification spends money against this endpoint via the eval-cli@joedollinger.dev identity — needs the D-D-01 exemption to not re-trip the 2026-05-12 incident pattern. |
| Anthropic API (cat4 judge) ← eval CLI judge calls | Sonnet-as-judge consumes KB content + Sonnet-under-test output. Cost-bounded by the eval CLI's projectRunCost guard. |
| Merge to main → Vercel auto-deploy → prod | Vercel Deployment Checks gate enforces; bypass-via-manual-promote pattern from Plan 05-12 nights documented but not reused if the gate works cleanly here. |
| eval_runs Supabase row writes | Persistent audit-trail rows; nanoid IDs serve as the cross-reference Plan 06-06 SUMMARY uses for D-F-08. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Denial of Service | Phase 6 verification spend (~$5) re-trips the rolling-24h spendcap, silently kills /api/chat for hours (the 2026-05-12 incident pattern) | mitigate | Task 1 D-D-01 preflight gate — HARD halts the plan if the eval-cli spend-cap exemption has not shipped. Task 2 baseline-reset defense-in-depth. Task 6 optional re-reset. Total: three layers (preflight + pre-preview reset + pre-prod reset). |
| T-06-06-02 | Tampering / DoS | Promote-to-prod happens before preview verification (compresses D-B-03 sequencing → fabrication or voice regression goes live without preview catch) | mitigate | Task 4 is checkpoint:human-verify with gate="blocking"; Task 5 promote-to-prod cannot fire until Joe explicitly signs PROCEED. Acceptance criteria enforce the gate-then-promote ordering via Decision-line grep. |
| T-06-06-03 | Information Disclosure | eval cat4 judge sees full KB content + Sonnet output → judge rationale rows in eval_runs could leak PII if KB content contains it | accept | Phase 6 D-C-01..03 keep KB content edits scope-limited to kb/about_me.md (which has been Plan 06-01 claim-matrix-filtered through Joe's transcript + already-public resume content). No PII enters the system as part of this phase. |
| T-06-06-04 | Repudiation | Joe disputes a Phase 6 close decision later (e.g., which OQ-04 option was chosen) | mitigate | VERIFICATION-LOG.md + 06-06-SUMMARY.md committed with explicit `**Signed:** Joe Dollinger, DATE` lines on every Joe-decision task (Tasks 4, 7, 9). Git history preserves timestamps. |
| T-06-06-05 | Tampering | Eval invocation uses unstable env vars (EVAL_CATS / EVAL_TARGET_URL) that drift between runs, producing non-reproducible runIds | mitigate | Plan 06-06 mandates argv flags `--target` + `--cats` for every run (Tasks 3 + 6). Plan 05-13 W6 made argv take precedence over env; this plan capitalizes on that to make each runId reproducible from the recorded command. Captured in VERIFICATION-LOG.md `**Command:**` lines. |
| T-06-06-06 | Determinism break (SAFE-11) | KB content additions from Plans 06-03..04 somehow leaked dynamic content into the cached prefix → determinism CI test fails for merge SHA | mitigate | Plan 06-03 + Plan 06-04 acceptance criteria already enforced `npm test -- --filter=system-prompt` green on each commit. Task 8 is the final canary check (CI for merge SHA). If it fails, Task 7 ROLLBACK path is the recovery. |
| T-06-06-07 | DoS (operational) | Vercel Deployment Checks gate (the cat1-via-CI prerequisite) flakes on the merge commit, blocking promote-to-prod | accept | Documented operational hazard from Plan 05-12 LAUNCH-07 follow-up. Mitigation is the same pattern Plan 05-12 used: enforce_admins override + Vercel manual-promote if the in-CI run is flaky. Plan 06-06 Task 3 already verified preview cat1+cat4 with the same case content — that's the durable evidence; the CI gate is theatrical at this point per the documented bypass pattern. |
</threat_model>

<verification>
- Task 1: VERIFICATION-LOG.md created; preflight result PASS or FAIL recorded; if FAIL plan halted
- Task 2: spendcap + rate-limit reset run; outputs captured
- Task 3: two preview runIds (cat1 + cat4-judge) captured with pass results
- Task 4: Joe-signed PROCEED or HALT decision in log
- Task 5: merge SHA + prod deploy READY + health smoke results in log (only if Task 4 = PROCEED)
- Task 6: two prod runIds (cat1 + cat4-judge) captured with pass results
- Task 7: Joe-signed PROCEED-TO-CLOSE or ROLLBACK decision in log
- Task 8: determinism CI test status (PASS | FAIL) recorded against merge SHA; D-F-06 met: yes|no explicit
- Task 9: 06-06-SUMMARY.md + ROADMAP.md + STATE.md committed atomically; D-F-01..08 checklist filled; OQ-04 recorded; four runIds in summary table
- Total: 4 eval_runs row IDs captured (D-F-08 satisfied)
- Tsc + build green post-Task-9 commit
</verification>

<success_criteria>
- D-F-02 (cat1=N/N preview) met (recorded at Task 3, gated at Task 4)
- D-F-03 (cat1=N/N prod) met (recorded at Task 6, gated at Task 7)
- D-F-04 (cat4>=4.0 + per_case preview) met (recorded at Task 3, gated at Task 4)
- D-F-05 (cat4>=4.0 + per_case prod) met (recorded at Task 6, gated at Task 7)
- D-F-06 (determinism CI green on merge SHA) met (verified at Task 8)
- D-F-07 (ROADMAP + STATE + SUMMARY committed) met (Task 9)
- D-F-08 (4 eval_runs row IDs captured) met (Tasks 3 + 6 capture; Task 9 surfaces in SUMMARY)
- OQ-03 resolved locked-skip
- OQ-04 surfaced (not locked here; Joe's choice recorded in SUMMARY)
- D-D-01 hard-dep preflight enforced at Task 1 (no silent re-trip of 2026-05-12 incident pattern)
- D-B-03 preview-first sequencing enforced via checkpoint:human-verify gates (Tasks 4 + 7)
- Phase 6 closed at v1.0 ready-for-distribution state OR explicit COMPLETE-WITH-FOLLOWUP if any gate failed
</success_criteria>

<output>
After completion, the deliverables are:
1. `.planning/phases/06-kb-enrichment-about-me-hardening/06-06-VERIFICATION-LOG.md` — per-task audit trail with 4 eval_runs row IDs, 2 Joe-signed gate decisions, determinism CI status
2. `.planning/phases/06-kb-enrichment-about-me-hardening/06-06-SUMMARY.md` — phase close-out artifact; D-F-01..08 checklist; OQ-04 surfaced
3. `.planning/ROADMAP.md` updated — Phase 6 ✓ Complete
4. `.planning/STATE.md` updated — ## Progress reflects 6/6 phases
5. Atomic commit covering items 2-4 (item 1 may be committed incrementally per task)
</output>
