---
phase: 06-kb-enrichment-about-me-hardening
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - kb/about_me.md
  - .planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "kb/about_me.md is the existing target — sections preserved or augmented or replaced per Joe's per-section call. NEVER wholesale overwrite (D-A-03 locked: section-by-section keep/augment/replace)"
    - "Each section topic in the stripped file is presented alongside the corresponding section in current kb/about_me.md; Joe sees both, picks disposition, executor applies"
    - "Output is a updated kb/about_me.md that is content-true (claims grounded, no fabrication) but NOT yet voice-true (voice rewrite is Plan 06-04). This separation keeps the merge decision orthogonal to voice tuning"
    - "06-03-MERGE-DECISIONS.md captures the per-section Joe-call audit trail (which sections were kept / augmented / replaced) for future reference and rollback safety"
    - "This is the highest Joe-time cost in the phase (~60-90 min) per CONTEXT workflow table; tasks structured as 'present comparison + capture decision' pairs, NOT 'apply X automatically'"
  artifacts:
    - path: "kb/about_me.md"
      provides: "Updated about-me KB section — merged content, claim-grounded, pre-voice-rewrite"
      contains: "# About Me"
    - path: ".planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md"
      provides: "Per-section disposition log: section topic, current-kb content excerpt, stripped-source content excerpt, Joe's keep/augment/replace decision + rationale"
      contains: "## Section"
  key_links:
    - from: "kb/about_me.md"
      to: "kb-loader.ts cached-prefix assembly"
      via: "buildSystemPrompt() reads kb/*.md into cached system prompt"
      pattern: "(SAFE-11 determinism contract — D-B-04 CI test enforces byte-identical)"
    - from: "06-03-MERGE-DECISIONS.md"
      to: "Each section in the merged kb/about_me.md"
      via: "row-per-section audit trail"
      pattern: "## Section"
---

<objective>
Wave 2, Plan 3 of 6. Section-by-section merge of the stripped LLM source into the existing kb/about_me.md. This is a Joe-driven decision plan: tasks are structured as "present comparison → capture decision" pairs, NOT autonomous apply-X.

The highest Joe-time cost in the phase (CONTEXT workflow table: ~60-90 min). Each section in the stripped source is matched to a section/topic in current kb/about_me.md. Joe picks one of three dispositions per section:

- **keep** — existing kb/about_me.md section stays as-is; discard stripped content for this section
- **augment** — append stripped content alongside existing (additive); both survive
- **replace** — stripped content supersedes existing for this section

The executor's job is mechanical: identify section topics, build the comparison table, write Joe-resolved dispositions into kb/about_me.md, log every decision in 06-03-MERGE-DECISIONS.md. Voice rewrite is Plan 06-04, not here — keeping merge orthogonal to voice tuning.

This plan is `autonomous: false` because every section disposition is a Joe-call. Per CONTEXT D-B-02, this is one of the three Joe-approval gates in the phase (Plans 06-02, 06-03, 06-04).

Output: updated kb/about_me.md (content-true, pre-voice) + 06-03-MERGE-DECISIONS.md (per-section audit trail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-kb-enrichment-about-me-hardening/06-CONTEXT.md
@.planning/phases/06-kb-enrichment-about-me-hardening/06-01-PLAN.md
@.planning/phases/06-kb-enrichment-about-me-hardening/06-02-PLAN.md
@kb/about_me.md
@src/lib/kb-loader.ts
@src/lib/system-prompt.ts

<interfaces>
From CONTEXT.md D-A-03 (locked):
- Merge target = EXISTING kb/about_me.md (not a new file)
- Section-by-section keep / augment / replace decisions
- No wholesale overwrite
- Joe approves each section's disposition before merge

Current kb/about_me.md structure (read 2026-05-12):
- 5 paragraphs as the file body, no internal ## headings
- Topics (planner inferred):
  - Para 1: How I ended up in product (Under Armour BI lead → EDW ownership)
  - Para 2: What I like about product (EDW migration + Tableau sandbox at Under Armour)
  - Para 3: What energizes me now (Snowflake Cortex AI, streams, client onsite)
  - Para 4: What I'm looking for (Senior PM role, data space, company size, location)
  - Para 5: Hobbies (golf, handicap 8.7, datagolf.com, DraftKings)
- Total: ~600 words

Section-mapping approach for Task 1:
The stripped source from Plan 06-02 will have its own structure (with or without ## headings). The executor maps each stripped-source chunk to one of the 5 existing-kb topics above. New topics in stripped source that don't fit any existing topic become a 6th, 7th, ... "Net-new" candidate paragraph; Joe decides whether to add them (replace=N/A; augment=append at end; or strip-by-skip).

From CONTEXT.md D-C-01..03 (out of scope):
- /api/chat/route.ts, kb-loader.ts, system-prompt.ts assembly logic NOT modified
- UI / infra / env / package.json / CI workflows NOT modified

From CONTEXT.md D-B-04 (determinism contract):
- src/lib/system-prompt.test.ts byte-identical determinism test runs in CI on every push
- Any per-request data leak into the cached prefix fails the test
- KB content additions are static text; safe by definition

Token budget guard:
- Total kb/ currently <50k tokens (CHAT-03 requirement)
- Plan 06-03 should add no more than ~300 new words to kb/about_me.md (rough budget — current file is ~600 words; expanding to ~900 keeps it well inside cached-prompt size targets)
- Token overflow check is part of Plan 06-06 verification (cache_read_input_tokens stays in healthy range; system-prompt determinism test stays green)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build per-section comparison file for Joe's merge decisions</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md</files>
  <read_first>
    - kb/about_me.md (existing target — full read; the 5 paragraphs to map against)
    - docs/transcripts/06-about-me/llm-about-me.stripped.md (the stripped source from Plan 06-02 — full read; gitignored)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-02-STRIP-REVIEW.md (Joe's Plan 06-02 sign-off — surfaces any notes flagged for 06-03)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-01-CLAIM-MATRIX.md (canonical claim dispositions — sanity-check no `strip` claims sneaked through)
  </read_first>
  <action>
**Method:**

1. **Identify sections in the stripped source.** Read `docs/transcripts/06-about-me/llm-about-me.stripped.md`. The stripped output may or may not have ## headings. If headings exist, use them. If not, segment by topic boundary (paragraph + topic shift) — typically 4-7 sections in a ~400-600 word about-me file.

2. **Map each stripped-source section to an existing kb/about_me.md topic.** Use this mapping table as the starting point (executor adapts based on actual stripped content):

   | Existing kb topic | Existing kb excerpt anchor (verbatim from kb/about_me.md) |
   |-------------------|----------------------------------------------------------|
   | T1: Origin / Under Armour BI lead | "I ended up in product management kind of by accident" |
   | T2: Why I like product / EDW migration | "I really like being in product because I like creating stuff" |
   | T3: What energizes me / Snowflake Cortex AI | "What energizes me now is leveraging a lot of the new features in Snowflake" |
   | T4: What I'm looking for / Senior PM role | "What I'm looking for is a Senior Product Manager role" |
   | T5: Hobbies / golf | "I'm obsessed with golf" |
   | TN: Net-new (no existing match) | (any stripped section that doesn't fit T1-T5) |

3. **Write `.planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md`** with this structure (one section block per stripped-source section):

```markdown
# 06-03 Merge Decisions — Section-by-section about_me.md merge

**Generated:** 2026-MM-DD
**Existing target:** kb/about_me.md (5 paragraphs, ~600 words pre-merge)
**Stripped source (gitignored):** docs/transcripts/06-about-me/llm-about-me.stripped.md
**Disposition options:** keep / augment / replace
**Default-on-no-decision:** keep (preserve existing)

---

## Section S1 — Topic: <e.g., "Origin / Under Armour BI lead">

### Existing kb/about_me.md content (verbatim)

> I ended up in product management kind of by accident. Before product management on the tech side really existed in corporate America, I was already doing it — dictating how a product functioned without the official title. ... (full paragraph)

### Stripped-source content for this topic

> <stripped-source excerpt mapped to T1>

### Differences flagged

- Stripped adds: <new factual claim X>, <new factual claim Y>
- Stripped omits: <claim Z that was in existing>
- Stripped phrasing differs: <yes / no — note "voice rewrite is Plan 06-04; do not over-weight phrasing here">

### Joe decision

**Disposition:** keep | augment | replace

**Rationale (one line):** <Joe fills in>

**Apply notes (if augment or replace):** <which specific claims/sentences merge; planner-suggested wording for the executor>

---

## Section S2 — Topic: <T2>

(repeat structure)

---

## Section SN — Net-new topic (if any)

<if stripped source has a topic not in existing kb/about_me.md — e.g., a paragraph about specific PM philosophy or career trajectory not yet represented>

**Disposition:** keep-as-net-new | strip-net-new

(keep-as-net-new = append as a 6th paragraph; strip-net-new = drop)

---

## Token budget check

- Current kb/about_me.md word count: ~600 words / ~800 tokens
- Projected post-merge word count: ~N words / ~N tokens
- Total kb/ post-merge: ~N tokens (must stay <50k per CHAT-03)
- Cached prompt impact: <pass/concern>
```

4. **Pre-populate disposition with planner recommendation** for each section based on the claim matrix + Joe's 06-02 notes. Joe overrides freely in Task 2.

5. **Commit the decisions file.**
  </action>
  <verify>
    <automated>test -f .planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md && grep -c "^## Section S" .planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md</automated>
  </verify>
  <done>06-03-MERGE-DECISIONS.md exists with N section blocks (one per stripped-source section); each section block has Existing content, Stripped content, Differences, and Disposition (with planner default).</done>
  <acceptance_criteria>
    - `.planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md` exists
    - File contains literal `# 06-03 Merge Decisions` heading
    - File contains at least 3 `## Section S` blocks (most about-me files have at least 3 distinct topics)
    - Each `## Section S` block contains literal subheadings `### Existing kb/about_me.md content`, `### Stripped-source content`, `### Differences flagged`, `### Joe decision`
    - Each `### Joe decision` block contains literal `**Disposition:**` line with planner-default value pre-filled
    - File contains literal `## Token budget check` section with current and projected word counts
    - File is committed (`git status` clean)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Joe walks the merge decisions file and locks per-section dispositions</name>
  <files>.planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md (the decisions file from Task 1 — Joe fills in dispositions)
    - kb/about_me.md (existing target — Joe should re-read to ground his decisions)
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-CONTEXT.md (D-A-03 disposition definitions)
  </read_first>
  <what-built>Joe-resolved per-section dispositions captured in 06-03-MERGE-DECISIONS.md. Every section has an explicit keep/augment/replace verdict + one-line rationale. Apply notes filled in for any augment/replace section so Task 3 executor knows the exact merge wording.</what-built>
  <how-to-verify>
    **Step A — Read the comparison file.** Open `06-03-MERGE-DECISIONS.md`. For each `## Section S` block:
    1. Read the Existing content excerpt
    2. Read the Stripped-source content excerpt
    3. Read Differences flagged
    4. Decide: keep / augment / replace (or keep-as-net-new / strip-net-new for net-new sections)
    5. Edit the `**Disposition:**` line to your chosen verdict
    6. Fill in the `**Rationale:**` line (one sentence — what tipped you)
    7. For augment/replace: fill in `**Apply notes:**` with the exact merge instruction the executor needs. Examples:
       - "Augment: append this sentence at end of paragraph: 'And I'm energized by the Cortex AI work...'"
       - "Replace: swap entire current paragraph 3 with the stripped version, but keep my last sentence 'It just simplified things' as the closer."
       - "Replace: but tighten the new version's 'enhances the relationship' phrasing (Plan 06-04 will voice-rewrite this anyway)"

    **Step B — Sanity guidance for each disposition:**
    - `keep` = existing wins. Strip content is discarded for this section. Choose when existing is voice-true and stripped doesn't add net-new factual content.
    - `augment` = additive. Both existing AND stripped content survive (typically: existing paragraph + appended new claim or sentence). Choose when stripped surfaces a new transcript-grounded fact existing didn't have.
    - `replace` = stripped wins. Existing content for this section discarded. Choose when stripped is more accurate / more current AND covers the existing facts. Use sparingly — current kb/about_me.md is already voice-true Joe-prose.

    **Step C — Token budget guidance:**
    - If projected total word count > 1000 for kb/about_me.md, reconsider augments. CHAT-03 caps total kb/ at <50k tokens; about_me is one component.
    - Net-new sections add weight; default to strip-net-new unless the new content is high-signal (e.g., new transcript quote that captures Joe's actual voice better than existing).

    **Step D — Re-validate against the claim matrix.** Spot-check 3-5 random claims in your final merge dispositions: does each surviving claim trace to a `keep` row in 06-01-CLAIM-MATRIX.md? If any traces to a `strip` row, something leaked through Plan 06-02 — back out the disposition or restate without the strip claim.

    **Step E — Commit the resolved decisions file.**
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; — Joe walks every section block, fills in Disposition + Rationale + Apply notes, spot-checks against the claim matrix, commits. ~60-90 min Joe-time (the biggest cost in the phase per CONTEXT workflow table).</action>
  <verify>
    <automated>! grep -E "^\*\*Disposition:\*\* keep \| augment \| replace$" .planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md</automated>
  </verify>
  <done>Every section has an explicit disposition; no placeholder `keep | augment | replace` lines remain; file committed.</done>
  <acceptance_criteria>
    - No line in the file is literally `**Disposition:** keep | augment | replace` (the placeholder template); grep should return 0 matches
    - Every `## Section S` block has a `**Disposition:**` line ending in one of: `keep`, `augment`, `replace`, `keep-as-net-new`, `strip-net-new`
    - Every `**Rationale:**` line has non-empty content after the colon (Joe wrote a sentence)
    - Every `augment` or `replace` disposition has a non-empty `**Apply notes:**` line
    - File is committed
  </acceptance_criteria>
  <resume-signal>"Merge decisions locked: N sections (keep=N, augment=N, replace=N, net-new=N); projected kb/about_me.md word count ~N."</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply Joe-locked dispositions to kb/about_me.md</name>
  <files>kb/about_me.md</files>
  <read_first>
    - .planning/phases/06-kb-enrichment-about-me-hardening/06-03-MERGE-DECISIONS.md (Joe-locked dispositions — Task 2 output; the executable source-of-truth for this task)
    - kb/about_me.md (current target — pre-merge state; this task modifies it)
    - docs/transcripts/06-about-me/llm-about-me.stripped.md (the source of `augment` and `replace` content, gitignored)
    - kb/about_me.md commit history via `git log --oneline -- kb/about_me.md` (so executor can sanity-check the file hasn't drifted since Task 1 baseline)
  </read_first>
  <action>
**Method (mechanical application of Joe's locked decisions — no judgment calls allowed):**

1. **Read 06-03-MERGE-DECISIONS.md** section-by-section.

2. **For each section:**
   - `keep` → no edit to kb/about_me.md for this section. Skip.
   - `augment` → apply the exact merge described in `**Apply notes:**`. Typically: append a sentence/clause from stripped content into the matching existing paragraph. Place it where the apply notes specify (e.g., "end of paragraph" or "after sentence beginning with 'I really like'").
   - `replace` → swap the existing section's content for the stripped content per `**Apply notes:**`. If apply notes say "keep my last sentence X", preserve that sentence verbatim.
   - `keep-as-net-new` → append the stripped section as a new paragraph at end of kb/about_me.md (before any trailing whitespace).
   - `strip-net-new` → no edit. Skip.

3. **CRITICAL guards (the executor must enforce):**
   - **Do NOT voice-rewrite.** Apply stripped content verbatim to the file. Voice rewriting is Plan 06-04 — confusing the two violates separation of concerns and can re-introduce LLM expansion.
   - **Do NOT add or invent any text beyond what is in Apply notes or stripped content.** Filler sentences ("This shaped my approach to..."), transitions, or interpretive bridges are forbidden. If a transition is needed, that's Plan 06-04's voice rewrite job.
   - **Do NOT touch other kb/ files.** Only kb/about_me.md is in this plan's `files_modified`. Adjacent files (resume.md, profile.yml, voice.md, etc.) stay untouched in this plan.
   - **Preserve the file's # heading** at top (`# About Me`).
   - **Preserve trailing newline** (POSIX file convention).

4. **Sanity checks after apply:**
   - Count paragraphs in kb/about_me.md (pre-merge: 5). Post-merge should be 5 (if all dispositions were keep/augment) or 5+N (if N net-new sections were keep-as-net-new).
   - Run `git diff kb/about_me.md` and inspect the diff: every added line should trace to either (a) a `**Apply notes:**` instruction in 06-03-MERGE-DECISIONS.md, or (b) verbatim from stripped source per `replace`.
   - Run `npm test -- --filter=system-prompt` to confirm SAFE-11 determinism test stays green. The KB content edit must not break determinism (which would only happen if a dynamic value snuck in — impossible with static markdown changes, but the test runs as belt-and-suspenders).
   - Run the kb token-count check: `npx tsx scripts/validate-kb-frontmatter.ts` (or whatever the project's kb-validation script is — check scripts/ dir) to confirm kb/ size is still healthy.

5. **Commit:** atomic commit per section is overkill; one commit `kb(06-03): apply section-by-section merge per 06-03-MERGE-DECISIONS.md` is the right granularity. The decisions file IS the audit trail.
  </action>
  <verify>
    <automated>npm test -- --filter=system-prompt && git diff --stat kb/about_me.md</automated>
  </verify>
  <done>kb/about_me.md updated per Joe-locked dispositions; system-prompt determinism test green; no other kb/ files touched.</done>
  <acceptance_criteria>
    - `kb/about_me.md` has been modified (verify via `git diff --stat kb/about_me.md` showing a non-zero line delta)
    - `kb/about_me.md` starts with `# About Me` heading (preserved)
    - `kb/about_me.md` ends with a trailing newline
    - No other kb/* files appear in `git status` (only kb/about_me.md and the decisions file modified)
    - `npm test -- --filter=system-prompt` exit 0 (SAFE-11 determinism test green)
    - `npx tsc --noEmit` exit 0 (no TS impact — this is a pure markdown change)
    - `npm run build` exit 0 (no Next.js build break; the kb-loader bundle must still compile)
    - For every `augment` or `replace` disposition in 06-03-MERGE-DECISIONS.md, the corresponding new content is now present in kb/about_me.md (spot-check 3 random sections)
    - For every `keep` disposition, the corresponding existing paragraph is byte-identical to pre-merge state (spot-check 1-2 sections via `git show HEAD~1:kb/about_me.md | diff -`)
    - Word count of kb/about_me.md stays within projected range from Task 2 Step C (token budget guidance)
  </acceptance_criteria>
  <done>Merged kb/about_me.md ready for Plan 06-04 voice rewrite (Wave 2 next task running in parallel? — No, 06-04 depends on 06-03 strict-sequentially; see depends_on in 06-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Joe's section decisions → kb/about_me.md content | Manual decisions cross into the cached system prompt; SAFE-11 determinism test is the validation barrier |
| Stripped source (gitignored) → committed kb/about_me.md | Content crosses the gitignore boundary; mitigated because stripped content already passed Plan 06-02 review |
| New kb content → SAFE-11 cached-prefix integrity | Static markdown additions are safe by definition; any dynamic content would break the test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Executor in Task 3 voice-rewrites while applying merge (scope creep) | mitigate | Task 3 method step 3 explicit guard: "Do NOT voice-rewrite. Apply stripped content verbatim." Acceptance criteria spot-check via git diff. Joe sees the diff before final commit. |
| T-06-03-02 | Information Disclosure | Stripped content with sensitive transcript-adjacent detail (employer / coworker identifiers) committed into kb/about_me.md | mitigate | Plan 06-02 strip pass + Plan 06-01 claim matrix already filtered. Joe owns final sign-off via Task 2 disposition decisions. Defense-in-depth: review the diff before commit. |
| T-06-03-03 | Determinism break (SAFE-11) | Per-request dynamic content (timestamp, session-id) somehow injected into kb/about_me.md via the merge | accept | Stripped source is static markdown; no template variables can sneak through. CI determinism test (D-B-04) is the catch-all. Task 3 acceptance runs the test explicitly. |
| T-06-03-04 | Repudiation | Joe later disputes a per-section disposition decision | mitigate | 06-03-MERGE-DECISIONS.md is committed; every disposition has Joe's Rationale; git history records timestamp. Same pattern as Plan 05-12 launch checklist. |
| T-06-03-05 | Tampering | Adjacent kb/* files unintentionally modified during merge apply | mitigate | Task 3 acceptance criteria explicit: `git status` must show only kb/about_me.md (plus the decisions doc). Forces clean blast radius. |
| T-06-03-06 | Denial of Service | kb/about_me.md grows past token budget, breaking CHAT-03 (<50k total kb tokens) | mitigate | Task 1 Token Budget Check section forecasts post-merge size; Task 2 Step C Joe guidance flags over-budget cases. Task 3 acceptance criteria includes word-count guard. |
</threat_model>

<verification>
- 06-03-MERGE-DECISIONS.md committed with every section having an explicit Joe-locked Disposition + Rationale
- kb/about_me.md modified per dispositions; git diff visible and traceable to decisions doc
- SAFE-11 determinism test green
- No other kb/* files modified
- Tsc + build green
- kb token-budget check pass (file size within projected range)
</verification>

<success_criteria>
- kb/about_me.md is content-merged: existing voice-true content preserved per Joe's keep decisions; transcript-grounded new content augmented or replaced per Joe's calls
- Voice rewrite explicitly NOT done in this plan — that's Plan 06-04 (separation of concerns enforced via Task 3 guards)
- Per-section audit trail committed; future Joe can see WHY each merge call was made
- SAFE-11 determinism contract preserved (D-E-03 mitigation: CI test green on the merge commit)
- Token budget honored
</success_criteria>

<output>
After completion, create `.planning/phases/06-kb-enrichment-about-me-hardening/06-03-SUMMARY.md` referencing 06-03-MERGE-DECISIONS.md as the durable audit artifact, recording: disposition counts (keep/augment/replace/net-new), pre/post word counts of kb/about_me.md, any token-budget concerns flagged for Plan 06-06, and the SAFE-11 determinism test pass status on the merge commit.
</output>
