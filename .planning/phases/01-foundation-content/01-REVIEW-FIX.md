---
phase: 01-foundation-content
review_source: .planning/phases/01-foundation-content/01-REVIEW.md
review_date: 2026-04-29T00:00:00Z
fix_session_date: 2026-04-29
status: warnings_resolved
totals:
  critical_resolved: 2  # CR-01, CR-02 fixed in 25e1d1e (prior session)
  warnings_resolved: 6  # WR-01..06 (5 fixed, 1 no-action)
  info_resolved: 1      # IN-01
  info_deferred: 6      # IN-02..07 (none in this session's scope)
---

# REVIEW Fix Report — Phase 01

Companion to [01-REVIEW.md](./01-REVIEW.md). Tracks resolution status of every finding.

## Critical (already closed before this session)

- **CR-01** ✅ FIXED — `25e1d1e fix(01): anonymize coworker names in voice/stances/management`. Real coworker names + soft disparagement in `kb/voice.md` Samples 1, 3, 4 anonymized to "a peer on my team"; disparagement softened. Joe-signed guardrails contract restored.
- **CR-02** ✅ FIXED — same commit `25e1d1e`. Former direct-report named in `kb/management_philosophy.md` and `kb/stances.md` anonymized to "an analyst". `kb/brainstorm/case-study-candidates.md` (excluded from production output) renamed for consistency.

## Warnings

- **WR-01** ✅ FIXED — `c990340 fix(01): close REVIEW WR-01 — pin LF + normalize KB bytes for cache stability`. Added `.gitattributes` pinning `eol=lf` and `normalizeKBContent()` in `src/lib/kb-loader.ts` that strips a UTF-8 BOM and converts CRLF → LF at both `readFileSync` sites. Defense in depth. 5 new unit tests added (16/16 pass). The `LF will be replaced by CRLF` warning that appeared on every prior commit no longer fires.

- **WR-02** ✅ FIXED — `80e97a7 fix(01): close REVIEW WR-02 — enforce anonymization invariant on confidential case studies`. `scripts/validate-kb-frontmatter.ts` now flags any case study with `confidential: true` whose `role` string mentions a company from `kb/profile.yml`'s `companies[]` allow-list. Pulls allow-list at script start; tolerates missing/unparseable `profile.yml` (skip check, run rest). Strips trailing non-word chars from each entry so names like "Gap Inc." word-boundary-match correctly. Smoke-tested positive (SEI / Under Armour / Gap Inc. / Gap Inc) and negative (substring matches like "Gappy LLC", "SEIform component") paths.

- **WR-03** ✅ FIXED — `01a5770 fix(01): close REVIEW WR-03 — EmailGate maxLength + ref-based double-submit guard`. `src/components/EmailGate.tsx` adds `maxLength={254}` (RFC 5321 max email length) and a `submittingRef` for synchronous double-submit guarding. The async state-update race that let two rapid Enters both pass the `if (submitting)` check is closed. Type-check clean; 16/16 tests pass.

- **WR-04** ✅ FIXED — `cdb5de6 fix(01): close REVIEW WR-04 — explicit error checks on pre-commit self-test setup steps`. `scripts/test-pre-commit-hook.sh` now wraps `printf` and `git add` in `if !` branches with distinct ERROR (not FAIL) messages. Same pattern applied to the `.env.local` block. Trap commented to explain why it silently swallows cleanup errors. 4/4 hook self-tests still PASS.

- **WR-05** ✅ FIXED — `0d411bd fix(01): close REVIEW WR-05 — exclude package-lock.json from JWT regex check`. `scripts/install-pre-commit-hook.sh` adds `':(exclude)package-lock.json'` to the values-scope diff filter. Re-installed hook contains the new exclude (`grep -c` returned 1). 4/4 self-tests still PASS.

- **WR-06** 🟡 NO-ACTION (resolved by Next 16 defaults). REVIEW concern was "next/font/google can emit preconnect hints to fonts.gstatic.com in rendered HTML." Empirical check after `npm run build`: `grep -ri "fonts.gstatic\|fonts.google\|gstatic" .next/server/app/` returned **zero matches**. Next 16 self-hosts Geist correctly; fonts ship from `/_next/static/media/*.woff2` (preloaded with `crossorigin=""`). The recruiter's browser never talks to Google — the only Google call is at build time inside the Vercel build container, which isn't a recruiter-privacy concern. Vendoring would add ~100 KB to the repo and font-update friction for zero recruiter-facing benefit.

## Info

- **IN-01** ✅ FIXED — `e86b1a5 fix(01): close REVIEW IN-01 — replace create-next-app placeholder metadata`. Caught during the WR-06 build inspection: rendered HTML was leaking `<title>Create Next App</title>` and `<meta name="description" content="Generated by create next app"/>` into recruiter-facing tab titles and link unfurls. Replaced with real title + description + `robots: noindex` (flips to indexable at Phase 5 launch). Verified by rebuild + grep on rendered HTML.

## Deferred to a separate pass

The following info findings were not in scope for this fix session:

- **IN-02** — *(see 01-REVIEW.md for details; not yet reviewed in this session)*
- **IN-03** — *(see 01-REVIEW.md)*
- **IN-04** — shadcn-vendored components use double quotes; project Prettier wants single. Recommend `.prettierignore` for `src/components/ui/**` to keep `npx shadcn add` upgrades clean.
- **IN-05** — `tsconfig.json` target ES2017 conservative for Node 22.11+; bumping to ES2022 saves bundle bytes.
- **IN-06** — `vitest --pool=vmThreads` override comment will go stale when vitest > 4.1.5; add a TODO marker.
- **IN-07** — same Prettier issue as IN-04 (different file scope).

Recommend triaging the remaining 6 info findings in a follow-up pass after Phase 2 plan-phase work begins.
