---
phase: 01-foundation-content
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - docs/interview-protocol-voice.md
  - docs/interview-protocol-selection.md
  - docs/interview-protocol-case-study.md
  - .planning/phases/01-foundation-content/01-CONTENT-STATUS.md
  - scripts/validate-kb-frontmatter.ts
  - kb/voice.md
  - kb/stances.md
  - kb/about_me.md
  - kb/management_philosophy.md
  - kb/faq.md
  - kb/guardrails.md
  - kb/brainstorm/case-study-candidates.md
  - kb/case_studies/
autonomous: false
requirements:
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - VOICE-05
  - VOICE-06
  - VOICE-07
  - VOICE-08
  - VOICE-09
  - VOICE-10

estimated_hours: 12

user_setup:
  - service: voice-recording
    why: "Voice interview (Task 3) requires a recording tool. Joe picks one — no account setup needed for the simplest paths."
    dashboard_config:
      - task: "Pick one: Loom (free tier, browser-based, auto-transcribes), macOS Voice Memos + ChatGPT clean-up, or phone voice memo + Otter.ai free tier. No install blockers — Joe decides on the day of Task 3."
        location: "Joe's laptop / phone — whichever is at hand"

must_haves:
  truths:
    - "Three interview protocol docs exist at docs/interview-protocol-{voice,selection,case-study}.md with concrete prompts Claude can execute in follow-up sessions (VOICE-05 protocol, VOICE-02 protocol, case-study-selection protocol for VOICE-03)"
    - "01-CONTENT-STATUS.md exists with one checkbox per VOICE-02..10 REQ and one checkbox per case-study slug chosen (used by human verification in Task 7 to sign off the plan)"
    - "scripts/validate-kb-frontmatter.ts exists and, when run, exits 0 after Task 6 (verifies every kb/case_studies/*.md has required frontmatter fields: slug, hook, role, timeframe, confidential)"
    - "Voice interview transcript exists (Joe's local file — NOT committed to repo if confidential) and voice.md + stances.md seeded from it (VOICE-05)"
    - "kb/voice.md contains 8-12 sample blocks, each labeled with register + source type (Slack DM / text / voice memo / unfiltered email / unpolished draft) — NOT from LinkedIn, PRDs, or performance-review prose (VOICE-04)"
    - "kb/stances.md contains 8-12 disagreeable-on-purpose positions, each one passing the 'could a senior PM I respect say I disagree?' test (VOICE-06)"
    - "kb/about_me.md wordcount is 400-600, first-person, warm register, voice-first draft (VOICE-08)"
    - "kb/management_philosophy.md wordcount is 600-1000, opinionated and concrete, voice-first draft (VOICE-08)"
    - "kb/faq.md contains 15 Q/A entries covering visa, remote, timezone, comp→redirect, availability, relocation, interests (VOICE-07)"
    - "kb/guardrails.md is Joe-authored and contains a 'Signed: Joe Dollinger, <YYYY-MM-DD>' line committed by Joe's git author identity (VOICE-09)"
    - "kb/brainstorm/case-study-candidates.md contains 8-10 brainstormed candidates, coverage rubric checked, and the final 4-6 slug list at the bottom"
    - "kb/case_studies/ contains 4-6 .md files (excluding _fixture_for_tests.md) — each ≥300 words, all template sections filled (Context / Options / Decision / Outcome / Retrospective / Follow-ups), all frontmatter fields present (VOICE-02)"
    - "Coverage rubric verified: the 4-6 chosen case studies include ≥1 failure, ≥1 leadership-without-authority, ≥1 data-rooted, ≥1 cross-functional conflict, ≥1 recent (<2y), ≥1 long-arc (>12mo) — signed off by Joe on the content-status tracker (VOICE-03)"
    - "Each case study passes voice-pass final read (conversational register from first draft, not LinkedIn register) — Joe confirms on the content-status tracker (VOICE-10)"
  artifacts:
    - path: "docs/interview-protocol-voice.md"
      provides: "30-min voice interview protocol — 8 prompts designed to elicit authentic speech across casual/decisive/annoyed/curious/teaching registers"
      contains: "8 prompts"
    - path: "docs/interview-protocol-selection.md"
      provides: "30-min selection-session protocol — how to brainstorm 8-10 candidates and prune to 4-6 via coverage rubric"
      contains: "coverage rubric"
    - path: "docs/interview-protocol-case-study.md"
      provides: "45-min per-case-study interview protocol — 15-20 probing questions across Context / Options / Decision / Outcome / Retro"
      contains: "Context"
    - path: ".planning/phases/01-foundation-content/01-CONTENT-STATUS.md"
      provides: "Checklist tracker for VOICE-02..10 + case-study-slug-level sign-off + coverage rubric sign-off"
      contains: "VOICE-09"
    - path: "scripts/validate-kb-frontmatter.ts"
      provides: "tsx-runnable script that parses every kb/case_studies/*.md (excluding _-prefixed fixtures), verifies frontmatter schema (slug, hook, role, timeframe, confidential), exits non-zero on any failure"
      contains: "case_studies"
    - path: "kb/voice.md"
      provides: "8-12 voice samples from unfiltered sources only (VOICE-04)"
      min_lines: 40
    - path: "kb/stances.md"
      provides: "8-12 disagreeable positions (VOICE-06)"
      min_lines: 40
    - path: "kb/about_me.md"
      provides: "400-600 word first-person about (VOICE-08)"
      min_lines: 20
    - path: "kb/management_philosophy.md"
      provides: "600-1000 word opinionated management philosophy (VOICE-08)"
      min_lines: 30
    - path: "kb/faq.md"
      provides: "15 canned Q/A entries (VOICE-07)"
      min_lines: 45
    - path: "kb/guardrails.md"
      provides: "Joe-authored and Joe-signed guardrails (VOICE-09)"
      contains: "Signed:"
    - path: "kb/brainstorm/case-study-candidates.md"
      provides: "8-10 candidates + final 4-6 selection with coverage rubric checked"
      contains: "Final 4-6"
    - path: "kb/case_studies/"
      provides: "4-6 case study .md files, each voice-first drafted and frontmatter-valid (VOICE-02, VOICE-10)"
  key_links:
    - from: "docs/interview-protocol-voice.md"
      to: "kb/voice.md, kb/stances.md"
      via: "Protocol output seeds voice.md + 2-3 stances entries per VOICE-05"
      pattern: "voice.md"
    - from: "docs/interview-protocol-case-study.md"
      to: "kb/case_studies/*.md"
      via: "Protocol output becomes a case-study file per Task 6"
      pattern: "case_studies"
    - from: "scripts/validate-kb-frontmatter.ts"
      to: "kb/case_studies/*.md"
      via: "Script reads each file, parses gray-matter frontmatter, asserts required fields"
      pattern: "gray-matter"
    - from: ".planning/phases/01-foundation-content/01-CONTENT-STATUS.md"
      to: "VOICE-02..10"
      via: "One checkbox per REQ + one per case-study slug + coverage rubric block"
      pattern: "VOICE-\\d+"
---

<objective>
Populate the knowledge base with real, voice-true content across the nine remaining VOICE-* requirements. This is the **content acquisition track** — the real critical path of Phase 1 per research SUMMARY.md and the PITFALLS.md Pitfall 3 defense. Engineering shipped the scaffolding in Plans 01-03; this plan closes the loop by replacing every placeholder in `kb/` with authentic Joe content, authored voice-first, drawn from unfiltered sources, drafted under the four-layer voice defense.

The plan is structured as:
- **Task 1 (Claude-automatable, ~30 min):** Create the three interview protocol docs + content-status tracker + frontmatter validation script.
- **Tasks 2-6 (Joe-time, ~10-12 hours total):** Dedicated sessions where Joe sits down with Claude (in new chat windows) following the committed protocols. Voice interview → voice/stances seeding → self-authored prose → selection session → case-study interviews.
- **Task 7 (Joe-time, ~1 hour):** Voice-pass final read + coverage rubric sign-off + guardrails commit with Joe's git identity.

**Critical rule (Pitfall 3 defense):** The executor agent running this plan CANNOT produce the content itself. Attempting to do so fails VOICE-04 (voice samples must be from Joe's actual unfiltered sources, not generated), VOICE-05 (requires a real recorded interview), VOICE-06 (stances must be Joe's actual positions), VOICE-08/09/10 (all Joe-authored). The executor's role in Tasks 2-7 is to **run the protocol sessions with Joe**, transcribe/probe/draft-against-transcript, and update the content-status tracker — not to invent content.

Purpose:
- VOICE-02 — 4-6 case studies following the strict template, frontmatter-valid
- VOICE-03 — Coverage rubric satisfied across chosen case studies
- VOICE-04 — `voice.md` with 8-12 samples from unfiltered sources only
- VOICE-05 — Voice interview recorded + transcribed + seeds voice.md + 2-3 stances
- VOICE-06 — `stances.md` with 8-12 disagreeable positions
- VOICE-07 — `faq.md` with 15 canned answers
- VOICE-08 — `about_me.md` (400-600w) + `management_philosophy.md` (600-1000w)
- VOICE-09 — `guardrails.md` Joe-authored and Joe-signed
- VOICE-10 — Case studies drafted voice-first (conversational register from first draft)

Output:
- Three committed interview protocol docs in `docs/`
- Content-status tracker in `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`
- Frontmatter validation script in `scripts/`
- Fully populated `kb/` replacing every placeholder from Plan 02 with real content
- 4-6 case study files in `kb/case_studies/`
- Joe-signed `guardrails.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-content/01-CONTEXT.md
@.planning/phases/01-foundation-content/01-RESEARCH.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@docs/superpowers/specs/2026-04-21-resume-agent-design.md

<interfaces>
<!-- Interfaces this plan depends on (from Plan 02): -->

From kb/ scaffold (Plan 02 Task 1):
- All 10 top-level kb/*.md + kb/profile.yml files exist with "PLACEHOLDER" markers
- kb/brainstorm/case-study-candidates.md has the coverage rubric template
- kb/case_studies/_fixture_for_tests.md exists (stays — do not touch)

From src/lib/kb-loader.ts (Plan 02 Task 2):
- Will read whatever real content we drop in at cold start; no code changes needed for this plan
- `_`-prefixed case studies are excluded from production listing — real case studies MUST NOT start with `_`

<!-- Interfaces this plan creates: -->

Protocol docs (Task 1):
- `docs/interview-protocol-voice.md` — 8 prompts structured so a voice-interview session can be executed end-to-end in ~30 min
- `docs/interview-protocol-selection.md` — brainstorm template + coverage rubric + pruning heuristic
- `docs/interview-protocol-case-study.md` — 5 question clusters (Context, Options, Decision, Outcome, Retro), 3-4 probing questions each

Content status tracker (Task 1):
- `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` — source of truth for plan-level sign-off

Frontmatter validator (Task 1):
- `scripts/validate-kb-frontmatter.ts` — run via `npx tsx scripts/validate-kb-frontmatter.ts`; verifies every `kb/case_studies/*.md` (excluding `_`-prefixed) has `slug`, `hook`, `role`, `timeframe`, `confidential` frontmatter keys

<!-- Case study template (from design spec §4 — Task 1 protocol doc will restate this).
     NOTE: the YAML frontmatter delimiters in real case-study files are three-hyphen lines;
     we render them here with a leading bullet to avoid confusing YAML parsers that scan
     this plan file for repeated frontmatter blocks. Real files use plain three-hyphen lines. -->

```text
• frontmatter open (three hyphens on their own line)
slug: killing-the-feature      # kebab-case; must match filename
hook: "Killing a feature after 18 months of usage data"
role: "Senior PM, Analytics Platform"
timeframe: "2023 Q2 – 2024 Q1"
confidential: false            # if true, agent swaps names for placeholders
• frontmatter close (three hyphens on their own line)

## Context
## Options considered
## Decision & reasoning
## Outcome
## Retrospective
## Likely recruiter follow-ups (and my answers)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create three interview protocol docs + content-status tracker + frontmatter validation script</name>
  <files>docs/interview-protocol-voice.md, docs/interview-protocol-selection.md, docs/interview-protocol-case-study.md, .planning/phases/01-foundation-content/01-CONTENT-STATUS.md, scripts/validate-kb-frontmatter.ts</files>
  <read_first>
    - .planning/phases/01-foundation-content/01-CONTEXT.md (§ KB Content Acquisition — D-C-01..07; specifically the recommended population sequence)
    - .planning/research/PITFALLS.md (Pitfall 3 — voice defense; unfiltered-source rule; voice-first drafting rule)
    - docs/superpowers/specs/2026-04-21-resume-agent-design.md (§4 — KB structure, case-study template, coverage rubric, voice defense four-layer stack)
    - .planning/REQUIREMENTS.md (VOICE-02 through VOICE-10 acceptance criteria)
    - kb/brainstorm/case-study-candidates.md (confirm coverage rubric already scaffolded by Plan 02)
    - kb/case_studies/_fixture_for_tests.md (confirm frontmatter schema)
  </read_first>
  <action>
    This task is **fully Claude-automatable** — it produces protocol scaffolding, a tracker, and a validation script. No Joe voice required. Write each file with the EXACT content below.

    **Step 1 — Write `docs/interview-protocol-voice.md`:**

    File content (write verbatim):

    # Voice Interview Protocol

    **Duration:** ~30 min recorded + ~30 min transcription cleanup
    **Output:** A transcript file (Joe's local — NOT committed if it contains confidential material) seeding `kb/voice.md` (8-12 samples) + 2-3 `kb/stances.md` entries
    **Facilitator:** Claude (in a fresh chat session with this protocol loaded)
    **Covers:** VOICE-04, VOICE-05, seeds VOICE-06

    ## Why this protocol exists

    The single largest quality risk for this project is voice that reads as generic ChatGPT (see `.planning/research/PITFALLS.md` Pitfall 3). The four-layer defense starts with **authentic voice samples from unfiltered sources** — Slack DMs, texts, voice-memo transcripts, unpolished drafts. LinkedIn posts and PRDs are **banned** as voice sources; they're already in corporate register.

    The most efficient way to generate unfiltered source material is to record Joe talking extemporaneously, because **speech is already voice-first**. Transcripts of Joe talking are strictly better voice samples than anything Joe will type on LinkedIn.

    ## Setup (5 min)

    1. **Pick a recording tool.** Loom (browser, auto-transcribes), macOS Voice Memos (local, paste into ChatGPT for clean-up), or phone voice memo + Otter.ai free tier. No "right answer" — Joe picks whichever is at hand.
    2. **Pick a quiet environment.** Not mission-critical for transcription quality, but matters for Joe's willingness to be unfiltered. Don't do this in a coffee shop.
    3. **Open this protocol on a second screen or phone.** Joe reads the prompt, then speaks.
    4. **Rules of engagement:**
       - Speak extemporaneously; do not write out an answer first.
       - Use contractions. Say "I don't know" if you don't. Say "I think X" not "one might argue X."
       - Don't self-edit mid-sentence. Let the false starts in — they're part of voice.
       - Take positions. If a prompt asks what annoys you, name the thing.
       - Aim for 2-4 minutes per prompt. Skip any prompt that doesn't have a real answer today.

    ## The 8 prompts (answer in any order)

    These are designed to elicit **five different registers**: casual, decisive, annoyed, curious, teaching. Aim for at least one prompt per register across the session.

    ### Prompt 1 — Casual register (warm-up)
    *"Walk me through what you actually did at work last week. Not the résumé version — what did you type, who did you talk to, what was annoying?"*

    (Register target: casual, conversational. Sample harvest: how Joe describes mundane work without polish.)

    ### Prompt 2 — Decisive register
    *"Name one decision you made in the last year where, if someone had pushed back on it at the time, you would've said 'no, I'm doing this anyway' — and tell me why."*

    (Register target: decisive, opinion-dense. Sample harvest: how Joe talks when he's sure. Also seeds `stances.md` — that "why" is usually a stance.)

    ### Prompt 3 — Annoyed register
    *"What's a thing teams in your space do wrong over and over that you find genuinely frustrating? Not a safe pet peeve — the real one."*

    (Register target: annoyed, honest. Sample harvest: how Joe talks when he's venting. Also seeds `stances.md` — the frustration is a stance.)

    ### Prompt 4 — Curious register
    *"Is there a product or team you've been watching from the outside lately where you can't tell if they're actually doing something interesting or just generating LinkedIn posts? Talk me through the ambiguity."*

    (Register target: curious, exploratory. Sample harvest: how Joe reasons out loud when he's uncertain.)

    ### Prompt 5 — Teaching register
    *"If a new PM on your team asked you 'how do you actually measure whether a feature is working,' what would you say — as if you were answering in Slack, not writing a blog post?"*

    (Register target: teaching. Sample harvest: how Joe explains without corporate scaffolding. Note the Slack-vs-blog-post framing — it's explicitly designed to prevent LinkedIn register.)

    ### Prompt 6 — Failure / learning
    *"Tell me about a project that didn't work the way you thought it would. What did you actually do about it, and what does that look like in retrospect?"*

    (Register target: reflective, candid. Sample harvest: how Joe describes failure without performance-review hedging. Also a candidate case study if the project is concrete.)

    ### Prompt 7 — Identity / what energizes you
    *"When you've had a week that felt great — not in the smile-for-the-camera sense, in the actually-felt-great sense — what did that week usually look like? What were you doing?"*

    (Register target: warm, first-person. Sample harvest: about_me.md seed material.)

    ### Prompt 8 — Disagreement seed
    *"Name an opinion you hold about product management that you think most PMs you respect would disagree with — and walk me through why you hold it anyway."*

    (Register target: opinionated. Sample harvest: direct seed for `stances.md`. The "PMs you respect would disagree" framing is the Layer 3 test.)

    ## After the recording (30 min)

    1. **Transcribe.** Otter/Loom auto-transcribe; Voice Memos → paste into ChatGPT with prompt: "Clean up this transcript — preserve contractions, preserve false starts where they carry meaning, remove only um/uh filler and repeated words."
    2. **Save the transcript** to Joe's local notes (Obsidian, Notes.app, plain .md). **DO NOT commit it to the repo if it contains confidential company or coworker details.** The KB only gets the extracted passages, not the full transcript.
    3. **Seed `kb/voice.md`.** Claude (in the same or a new session) reads the transcript + Joe picks 8-12 passages of 2-4 sentences each. For each passage, label the register (casual / decisive / annoyed / curious / teaching / reflective) and the implicit source ("voice interview YYYY-MM-DD, prompt N"). Drop into `kb/voice.md` replacing the placeholder.
    4. **Seed 2-3 `kb/stances.md` entries.** Prompts 2, 3, and 8 usually produce 1-3 raw stances each. Pick the 2-3 strongest, polish into short paragraphs that pass the "could a senior PM I respect say 'I disagree'?" test. These are seed entries; Task 4 rounds out to 8-12.

    ## Done when

    - Transcript exists in Joe's local notes
    - kb/voice.md has 8-12 sample blocks with register + source labels
    - kb/stances.md has 2-3 seed entries (remaining 6-10 written in Task 4)
    - Content-status tracker VOICE-04, VOICE-05 rows checked (partial for VOICE-06)

    (End of file content for `docs/interview-protocol-voice.md`.)

    **Step 2 — Write `docs/interview-protocol-selection.md`:**

    File content (write verbatim):

    # Case-Study Selection Session Protocol

    **Duration:** ~30 min live session (Joe + Claude)
    **Output:** `kb/brainstorm/case-study-candidates.md` filled to 8-10 candidates, pruned to 4-6 with the coverage rubric satisfied
    **Facilitator:** Claude (in a fresh chat session with this protocol + Joe's resume.md loaded)
    **Covers:** Groundwork for VOICE-02, VOICE-03

    ## Why this protocol exists

    Joe has 15+ years of BI + 6 years of PM experience. The first brainstorm will surface 15-25 candidate stories. The job of this session is to **converge fast** — 30 minutes, not 3 hours — and land on 4-6 case studies whose combined coverage satisfies the rubric without overlap.

    Being too precious about case-study selection is a common failure mode. The 4-6 chosen stories are not "Joe's greatest hits" — they're the stories that span the coverage rubric so a recruiter who asks any behavioral question can get a real answer.

    ## Setup (5 min)

    1. Claude loads `kb/resume.md` (real content, landed from Joe-authored resume), `kb/about_me.md`, any previous voice-interview transcripts, and this protocol.
    2. Joe has a blank `kb/brainstorm/case-study-candidates.md` open (scaffold from Plan 02 Task 1).
    3. Joe has 30 focused minutes. No laptop-open-in-parallel multitasking — this is the only open task.

    ## Brainstorm phase (15 min)

    Claude asks, Joe answers. **Target: 8-10 candidates.** Better to have 10 and cut than 6 and stretch.

    Prompts Claude cycles through:

    1. *"Scanning your resume — what's a story from {role at company X} that you tell in interviews that lands?"*
    2. *"What's a project where you had to get 5+ people to do something they initially didn't want to do?"* (targets leadership-without-authority)
    3. *"When's a time you killed something? Killed a feature, killed a project, killed an initiative. What was the decision mechanism?"* (targets failure / killed project)
    4. *"What's a decision you made from data that would've been wrong if you'd gone with intuition — or vice versa?"* (targets data-rooted or intuition-validated)
    5. *"When was there a real conflict across teams where you were the person in the middle?"* (targets cross-functional conflict)
    6. *"Most recent thing you shipped that you'd want to narrate — even if it's still ongoing?"* (targets recent, <2y)
    7. *"What's the longest-arc project you've been involved in — the one that took a year or more to land?"* (targets long-arc, >12mo)
    8. *"What's the story that, when you tell it at dinner with non-work people, actually lands as 'oh that's interesting'?"* (fishing for memorable hooks)

    For each candidate, Claude captures (in the `case-study-candidates.md` file in realtime), using this row template:

    ```
    ## Candidate: <working title>
    - **Role / timeframe:**
    - **One-line hook:**
    - **Why this one? (skill/trait):**
    - **Rough outcome:**
    - **Confidentiality concerns:**
    - **Freshness:**
    - **Rubric bucket(s) it could fill:** failure / leadership-without-authority / data-rooted / cross-functional-conflict / recent / long-arc
    ```

    ## Pruning phase (10 min)

    Coverage rubric (must satisfy with the final 4-6):
    - [ ] ≥1 failure or killed project
    - [ ] ≥1 leadership-without-authority
    - [ ] ≥1 data-rooted decision
    - [ ] ≥1 cross-functional conflict
    - [ ] ≥1 recent (<2y)
    - [ ] ≥1 long-arc (>12mo)

    Pruning rules (apply in order):
    1. **Remove duplicates of coverage.** If two stories both fill "cross-functional conflict" and nothing else, pick the more memorable and cut the other.
    2. **Remove confidentiality-blocked stories** where the anonymization would gut the specifics.
    3. **Remove stories where the outcome is unclear or still-unfolding** unless "still unfolding" is the point (recent, ongoing) and Joe can narrate it cleanly.
    4. **Aim for 4-6.** If rubric coverage forces 7, keep 7 and note to cut one after drafting (weakest draft loses). If rubric can be satisfied with 4, ship 4.

    ## Finalization (5 min)

    At the bottom of `case-study-candidates.md`, add this block (fill in the slugs):

    ```
    ## Final 4-6 (selected YYYY-MM-DD)
    1. <slug-one>             — rubric: failure, long-arc
    2. <slug-two>             — rubric: leadership-without-authority
    3. <slug-three>           — rubric: data-rooted, recent
    4. <slug-four>            — rubric: cross-functional-conflict
    5. <slug-five>  (optional) — rubric: <bucket>
    6. <slug-six>   (optional) — rubric: <bucket>

    Rubric coverage verified: ✅ failure ✅ leadership-w/o-authority ✅ data-rooted ✅ cross-functional ✅ recent ✅ long-arc
    ```

    Slugs are kebab-case, content-descriptive, not cute (see CONTEXT.md specifics). Good: `killing-the-feature`, `bi-migration`, `onboarding-ab-test`. Bad: `project-x`, `story-1`, `my-biggest-win`.

    ## Done when

    - `kb/brainstorm/case-study-candidates.md` has 8-10 candidates listed + final 4-6 block with rubric check
    - Joe has a time-on-calendar block for each of the 4-6 case-study interviews (Task 6)
    - Content-status tracker row "Case-study selection complete" checked

    (End of file content for `docs/interview-protocol-selection.md`.)

    **Step 3 — Write `docs/interview-protocol-case-study.md`:**

    File content (write verbatim):

    # Per-Case-Study Interview Protocol

    **Duration:** ~45 min per case study (4-6 sessions total = 3-5 hours)
    **Output:** One `kb/case_studies/<slug>.md` file per session, drafted voice-first, ≥300 words, all template sections filled, frontmatter complete
    **Facilitator:** Claude (fresh chat session per case study, this protocol + the selected candidate's row from `case-study-candidates.md` loaded)
    **Covers:** VOICE-02, VOICE-10

    ## Why this protocol exists

    Case studies are the #1 behavioral-interview question a recruiter will have the agent answer. If these read as LinkedIn-register slide decks, the whole portfolio pitch dies. The protocol is engineered to:
    1. Extract enough specifics that the agent can narrate without fabricating
    2. Keep Joe in conversational register (voice-first), not performance-review prose
    3. Produce a first draft ready for grammar cleanup, not voice-transplant surgery

    **Voice-passing at the end never works** (Pitfall 3 execution detail). If the first draft is in LinkedIn register, the story is dead — start over.

    ## Setup (5 min)

    1. Claude loads: this protocol, the candidate's row from `case-study-candidates.md`, `kb/voice.md` (for register reference), `docs/superpowers/specs/2026-04-21-resume-agent-design.md` §4 case-study template.
    2. Joe and Claude agree on the slug (kebab-case, content-descriptive) and the file path `kb/case_studies/<slug>.md`.
    3. Joe pulls any supporting material out of memory (or local notes): the actual metrics, dates, the names of internal tools, who pushed back, what the deck looked like. These don't go in the file — they ground Joe's recall.

    ## Interview phase (30 min — the 5 clusters)

    Claude asks the questions below, Joe answers extemporaneously. Claude takes notes but does NOT draft yet. **15-20 probing questions total, distributed across the 5 clusters.**

    ### Cluster 1 — Context (5-7 min, ~4 questions)
    1. *"Set the scene. What was the team, what was the business context, what were you supposed to be doing when this started?"*
    2. *"Who were the users — and be specific. Not 'enterprise customers,' name the archetype or the actual 2-3 companies if you can."*
    3. *"What did everyone on the team believe to be true at the start that turned out to be load-bearing later?"*
    4. *"If you'd described this project to a friend the week it started, what would you have said?"* (voice-forcing — conversational register)

    ### Cluster 2 — Options Considered (5-7 min, ~3 questions)
    5. *"Before you landed on what you did, what were the other options on the table? Even the ones that were dismissed fast — what made them attractive to the people who raised them?"*
    6. *"Who was pushing for each option? Doesn't have to be names — 'the eng lead' is fine — but who wanted what?"*
    7. *"What was the option that was almost chosen? The one that lost by a hair?"*

    ### Cluster 3 — Decision & Reasoning (5-7 min, ~4 questions)
    8. *"What did you actually decide, and what were the 2-3 factors that tipped it?"*
    9. *"Was there a moment or a meeting where the decision crystallized, or did it accrete?"*
    10. *"What did you know at the time that you wouldn't have bet money on?"* (surfaces the gap between what was known and what was believed)
    11. *"What did the skeptics say, and what did you say back?"* (voice-forcing — disagreement)

    ### Cluster 4 — Outcome (5-7 min, ~3 questions)
    12. *"Quantified where possible: what happened? Specific numbers, specific dates."*
    13. *"What was the outcome that the skeptics had predicted, and how close did they get?"*
    14. *"If there was one moment where you knew this was either working or not working — when was it?"*

    ### Cluster 5 — Retrospective (5-7 min, ~4 questions) — this is the "senior" tent pole
    15. *"What would you do differently if you ran it again today?"*
    16. *"What did you learn that you now apply to unrelated projects?"*
    17. *"Is there a thing that happened that you haven't fully metabolized — you still think about it, still aren't sure?"* (the ambiguity surfaces seniority)
    18. *"What's the critique of this story someone could make that you'd have to take seriously?"*

    ### Optional follow-up — Recruiter Q&A (3-5 min, 2-3 questions)
    19. *"If a recruiter asked 'what was your actual contribution here vs. the team's,' what would you say?"*
    20. *"If a recruiter said 'isn't this really a <adjacent discipline> problem, not a PM problem?' — how would you answer?"*

    ## Drafting phase (10 min)

    Claude writes the case-study file in real-time at the end of the interview. **Voice-first rule:** the first draft is in conversational register — use contractions, let sentence length vary, take positions, don't add corporate scaffolding. Grammar cleanup comes later. Target ≥300 words, typically 400-600.

    File template (real files use plain three-hyphen YAML delimiters — shown here with leading bullets to avoid confusing YAML parsers that scan this plan file):

    ```
    • frontmatter open (three hyphens on their own line)
    slug: <kebab-case-slug>           # must match filename
    hook: "<one-line-hook>"
    role: "<title-at-company-or-placeholder-if-confidential>"
    timeframe: "<YYYY QN – YYYY QM>"
    confidential: <false|true>
    • frontmatter close (three hyphens on their own line)

    ## Context
    <~80-120 words, first-person conversational>

    ## Options considered
    - <option 1 — why it looked attractive, why it lost>
    - <option 2>
    - <option 3>

    ## Decision & reasoning
    <~80-120 words — what + 2-3 factors that tipped it>

    ## Outcome
    <quantified where possible; specific where not>

    ## Retrospective
    <the "senior" section — what I'd do differently, what I still don't know>

    ## Likely recruiter follow-ups (and my answers)
    - Q: <common follow-up from Cluster 5 / optional>
      A: <conversational answer>
    - Q: <another>
      A: <answer>
    ```

    If `confidential: true`, Claude proactively swaps company/person names for placeholders during drafting ("[large e-commerce platform]", "the eng lead") — do NOT leave real names in the file pending later scrubbing.

    ## Joe edits (rolling, ~10 min per case study the next day)

    1. Read aloud. If any sentence sounds like LinkedIn, rewrite it.
    2. Check: does the Retrospective section have real ambiguity, or is it "what I learned was the importance of collaboration"? If the latter, rewrite.
    3. Check: are the Options genuinely competing, or is Option 1 obviously the right answer and Options 2-3 strawmen? If strawmen, rewrite or cut.
    4. Do NOT add bullets where there's a natural paragraph. Do NOT add markdown headers within sections.
    5. Run `npx tsx scripts/validate-kb-frontmatter.ts` — frontmatter must validate.

    ## Done when (per case study)

    - File at `kb/case_studies/<slug>.md` exists with all template sections filled
    - Frontmatter has `slug`, `hook`, `role`, `timeframe`, `confidential`
    - File is ≥300 words
    - Read-aloud passes: first-draft conversational register, not LinkedIn
    - `npx tsx scripts/validate-kb-frontmatter.ts` exits 0
    - Content-status tracker row for this slug checked

    (End of file content for `docs/interview-protocol-case-study.md`.)

    **Step 4 — Write `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`:**

    File content (write verbatim):

    # Phase 1 Content-Status Tracker

    **Purpose:** Single source of truth for Plan 04 sign-off. Joe and Claude both update this file as content lands. Plan 04 is complete when every checkbox is checked AND the coverage rubric block is signed off.

    **Mechanics:** Check a box by editing `- [ ]` → `- [x]` and committing. The task that landed the content is responsible for checking its own row.

    ## VOICE-02..10 Requirement Sign-Off

    ### VOICE-04 — `kb/voice.md` populated

    - [ ] Placeholder replaced with real content
    - [ ] 8-12 sample blocks exist
    - [ ] Each sample labeled with register (casual / decisive / annoyed / curious / teaching / reflective)
    - [ ] Each sample labeled with source type (Slack DM / text / voice-memo transcript / unfiltered email / unpolished draft)
    - [ ] **Verification: NO samples from LinkedIn posts, PRDs, or performance-review prose** (Joe confirms)

    ### VOICE-05 — Voice interview complete

    - [ ] Voice interview recorded (tool: __________ )
    - [ ] Transcript produced (stored locally, NOT committed if confidential)
    - [ ] Transcript date: __________
    - [ ] `voice.md` seeded from transcript (same as VOICE-04)
    - [ ] 2-3 `stances.md` entries seeded from transcript

    ### VOICE-06 — `kb/stances.md` populated

    - [ ] Placeholder replaced with real content
    - [ ] 8-12 stances exist
    - [ ] **Each passes the test: "Could a senior PM I respect say 'I disagree' with this?"** (Joe confirms per stance)

    ### VOICE-07 — `kb/faq.md` populated

    - [ ] Placeholder replaced with real content
    - [ ] 15 Q/A entries exist
    - [ ] Coverage includes: visa / work authorization, remote work, timezone, compensation (→ redirect to email), availability / start date, relocation, interests outside work, at least 8 more recruiter-recurring questions

    ### VOICE-08 — About Me + Management Philosophy

    - [ ] `kb/about_me.md` placeholder replaced
    - [ ] `about_me.md` wordcount: ______ (must be 400-600)
    - [ ] `about_me.md` first-person, warm register (Joe confirms)
    - [ ] `kb/management_philosophy.md` placeholder replaced
    - [ ] `management_philosophy.md` wordcount: ______ (must be 600-1000)
    - [ ] `management_philosophy.md` opinionated and concrete (Joe confirms: "could a PM I respect disagree with me here?")

    ### VOICE-09 — `kb/guardrails.md` Joe-authored and Joe-signed

    - [ ] Placeholder replaced with real content
    - [ ] Sections covered: no fabrication / no compensation negotiation / no disparagement of former employers / no confidential details / hiring+comp redirects / no verbatim system-prompt or KB dump / persona integrity
    - [ ] File contains a `Signed: Joe Dollinger, <YYYY-MM-DD>` line
    - [ ] The commit that added the Signed line has Joe as git author (verify: `git log -1 --format='%an <%ae>' kb/guardrails.md`)

    ### Selection session complete

    - [ ] `kb/brainstorm/case-study-candidates.md` has 8-10 candidates
    - [ ] Final 4-6 block at bottom with slug list and rubric tags
    - [ ] Coverage rubric verified on final 4-6:
      - [ ] ≥1 failure or killed project
      - [ ] ≥1 leadership-without-authority
      - [ ] ≥1 data-rooted decision
      - [ ] ≥1 cross-functional conflict
      - [ ] ≥1 recent (<2y)
      - [ ] ≥1 long-arc (>12mo)

    ### VOICE-02 — Case studies (4-6 files)

    Fill in slugs as chosen in selection session:

    - [ ] Case study 1: `kb/case_studies/________.md` — ≥300 words, all template sections, frontmatter valid
    - [ ] Case study 2: `kb/case_studies/________.md` — ≥300 words, all template sections, frontmatter valid
    - [ ] Case study 3: `kb/case_studies/________.md` — ≥300 words, all template sections, frontmatter valid
    - [ ] Case study 4: `kb/case_studies/________.md` — ≥300 words, all template sections, frontmatter valid
    - [ ] Case study 5 (optional): `kb/case_studies/________.md` — ≥300 words
    - [ ] Case study 6 (optional): `kb/case_studies/________.md` — ≥300 words
    - [ ] `npx tsx scripts/validate-kb-frontmatter.ts` exits 0

    ### VOICE-03 — Coverage rubric satisfied across the final case studies

    Mark which slug fills which rubric bucket (a slug may fill multiple):

    - [ ] Failure: ________
    - [ ] Leadership-without-authority: ________
    - [ ] Data-rooted: ________
    - [ ] Cross-functional conflict: ________
    - [ ] Recent (<2y): ________
    - [ ] Long-arc (>12mo): ________
    - [ ] **Final rubric sign-off (Joe):** _______________________ (initials + date)

    ### VOICE-10 — Voice-pass final read

    For each case study, Joe reads aloud and confirms conversational register (not LinkedIn register):

    - [ ] Case study 1 passes voice-pass
    - [ ] Case study 2 passes voice-pass
    - [ ] Case study 3 passes voice-pass
    - [ ] Case study 4 passes voice-pass
    - [ ] Case study 5 (optional) passes voice-pass
    - [ ] Case study 6 (optional) passes voice-pass
    - [ ] **Voice-pass sign-off (Joe):** _______________________ (initials + date)

    ## Plan 04 Exit Criteria

    Plan is complete when:
    - [ ] Every checkbox above is checked
    - [ ] `npx tsx scripts/validate-kb-frontmatter.ts` exits 0
    - [ ] `kb/guardrails.md` git-blame shows Joe as author of the Signed commit
    - [ ] Coverage rubric and voice-pass have Joe's initials + date

    Phase 1 is then content-complete. Plan 02's determinism test (which runs against placeholder content today) will re-run against real content on the next `npm test`; expected behavior: length grows, all other assertions still pass.

    (End of file content for `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`.)

    **Step 5 — Write `scripts/validate-kb-frontmatter.ts`:**

    File content (write verbatim):

    ```typescript
    // scripts/validate-kb-frontmatter.ts
    // Validates every kb/case_studies/*.md (excluding `_`-prefixed fixtures) has required frontmatter.
    // Run: npx tsx scripts/validate-kb-frontmatter.ts
    // Exits 0 on success, 1 on any validation failure.
    // Required fields per design spec §4 case-study template:
    //   slug (string, kebab-case, matches filename), hook (string), role (string),
    //   timeframe (string), confidential (boolean)
    import { readdirSync, readFileSync } from 'node:fs';
    import path from 'node:path';
    import matter from 'gray-matter';

    const CASE_DIR = path.join(process.cwd(), 'kb', 'case_studies');
    const REQUIRED_FIELDS = ['slug', 'hook', 'role', 'timeframe', 'confidential'] as const;

    type Failure = { file: string; issues: string[] };

    function validateFile(file: string): Failure | null {
      const abs = path.join(CASE_DIR, file);
      const raw = readFileSync(abs, 'utf-8');
      const { data } = matter(raw);
      const issues: string[] = [];

      for (const field of REQUIRED_FIELDS) {
        if (!(field in data)) {
          issues.push(`missing required field: ${field}`);
        }
      }

      if (data.slug !== undefined) {
        const expected = path.basename(file, '.md');
        if (data.slug !== expected) {
          issues.push(`slug "${data.slug}" does not match filename "${expected}"`);
        }
        if (typeof data.slug !== 'string' || !/^[a-z0-9-]+$/.test(data.slug)) {
          issues.push(`slug must be kebab-case lowercase ASCII (got: "${data.slug}")`);
        }
      }

      if (data.confidential !== undefined && typeof data.confidential !== 'boolean') {
        issues.push(`confidential must be a boolean (got: ${typeof data.confidential})`);
      }

      for (const field of ['hook', 'role', 'timeframe']) {
        if (data[field] !== undefined && typeof data[field] !== 'string') {
          issues.push(`${field} must be a string`);
        }
      }

      // Minimum word count (≥300 per pre-launch checklist §4). Strip any leading
      // YAML frontmatter block before counting.
      const body = raw.replace(/^\s*---[\s\S]*?---/, '').trim();
      const wordCount = body.split(/\s+/).filter(Boolean).length;
      if (wordCount < 300) {
        issues.push(`content under 300 words (got: ${wordCount})`);
      }

      return issues.length > 0 ? { file, issues } : null;
    }

    function main() {
      let files: string[];
      try {
        files = readdirSync(CASE_DIR)
          .filter((f) => f.endsWith('.md') && !f.startsWith('_'));
      } catch (err) {
        console.error(`Could not read ${CASE_DIR}:`, err);
        process.exit(1);
      }

      if (files.length === 0) {
        console.error('No case-study files found (excluding _-prefixed fixtures).');
        console.error('Phase 1 requires 4-6 case studies per VOICE-02.');
        process.exit(1);
      }

      if (files.length < 4 || files.length > 6) {
        console.error(`Expected 4-6 case studies (excluding fixtures); found ${files.length}.`);
        console.error('See VOICE-02 in .planning/REQUIREMENTS.md');
        process.exit(1);
      }

      const failures: Failure[] = [];
      for (const file of files) {
        const fail = validateFile(file);
        if (fail) failures.push(fail);
      }

      if (failures.length > 0) {
        console.error('Frontmatter validation FAILED:');
        for (const { file, issues } of failures) {
          console.error(`  ${file}:`);
          for (const issue of issues) console.error(`    - ${issue}`);
        }
        process.exit(1);
      }

      console.log(`OK: ${files.length} case studies validated.`);
      for (const f of files) console.log(`  - ${f}`);
    }

    main();
    ```

    **Step 6 — Sanity-check that the script runs (no case studies yet, so expect exit 1 with a clear message about count):**
    ```bash
    npx tsx scripts/validate-kb-frontmatter.ts
    # Expected output today (pre-Task 6): "No case-study files found..." or "Expected 4-6 ... found 0"
    # Exit code: 1 (expected — this will become 0 after Task 6)
    ```

    **Step 7 — Verify all five files written:**
    ```bash
    test -f docs/interview-protocol-voice.md && \
      test -f docs/interview-protocol-selection.md && \
      test -f docs/interview-protocol-case-study.md && \
      test -f .planning/phases/01-foundation-content/01-CONTENT-STATUS.md && \
      test -f scripts/validate-kb-frontmatter.ts && \
      echo "SCAFFOLD_OK"
    ```
  </action>
  <verify>
    <automated>test -f docs/interview-protocol-voice.md && test -f docs/interview-protocol-selection.md && test -f docs/interview-protocol-case-study.md && test -f .planning/phases/01-foundation-content/01-CONTENT-STATUS.md && test -f scripts/validate-kb-frontmatter.ts && grep -q "Prompt 1" docs/interview-protocol-voice.md && grep -q "Prompt 8" docs/interview-protocol-voice.md && grep -q "Coverage rubric" docs/interview-protocol-selection.md && grep -q "Cluster 1 — Context" docs/interview-protocol-case-study.md && grep -q "Cluster 5 — Retrospective" docs/interview-protocol-case-study.md && grep -q "VOICE-09" .planning/phases/01-foundation-content/01-CONTENT-STATUS.md && grep -q "Final rubric sign-off" .planning/phases/01-foundation-content/01-CONTENT-STATUS.md && grep -q "REQUIRED_FIELDS" scripts/validate-kb-frontmatter.ts && npx tsc --noEmit scripts/validate-kb-frontmatter.ts 2>&1 | tail -5 && echo "TASK1_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/interview-protocol-voice.md` exists with all 8 prompts, each labeled with register target
    - `docs/interview-protocol-selection.md` exists with brainstorm prompts, pruning rules, and the coverage rubric
    - `docs/interview-protocol-case-study.md` exists with 5 clusters of probing questions (Context / Options / Decision / Outcome / Retro) totaling 15-20 questions
    - `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` exists with a checkbox row per VOICE-02..10 REQ and per-case-study slug slots
    - `scripts/validate-kb-frontmatter.ts` exists and typechecks clean
    - The validate script returns non-zero today (no real case studies yet) — this is expected and correct; it flips to 0 after Task 6
    - No placeholder-replacement work on `kb/*.md` files — that's Tasks 2-6
  </acceptance_criteria>
  <done>Scaffolding complete. Three protocol docs committed and usable as drop-in prompts for Tasks 3, 5, 6. Content-status tracker established. Frontmatter validator wired. Nothing in `kb/` has been touched (placeholders still in place until Joe-time tasks run).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [JOE-TIME, ~1 hour] Voice interview — record + transcribe (VOICE-05)</name>
  <files>(Joe's local transcript — NOT committed to repo if confidential)</files>
  <action>
    Joe records and transcribes a voice interview per `docs/interview-protocol-voice.md`. See `<how-to-verify>` below for the step-by-step. This task is `checkpoint:human-action` because no executor agent can speak Joe's voice — the deliverable is Joe's actual recorded speech. Executor agent's role during this task is: (a) answer Joe's questions about the protocol, (b) help with transcript cleanup if pasted in, (c) block until Joe types "voice-interview-done".
  </action>
  <verify>
    <automated>MISSING — this task produces a local transcript file outside the repo; verification is Joe confirming via the resume-signal.</automated>
  </verify>
  <done>Joe has a transcript file saved locally, the content-status tracker VOICE-05 "recorded + transcribed" rows are checked, and Joe typed "voice-interview-done".</done>
  <what-built>
    - `docs/interview-protocol-voice.md` ready to follow (Task 1)
    - `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` ready to update (Task 1)
  </what-built>
  <how-to-verify>
    **Why checkpoint:human-action:** This is Joe's voice being recorded. No executor agent can do this. The deliverable is a transcript of Joe speaking extemporaneously across 8 prompts designed to elicit five distinct registers. Pitfall 3 (generic ChatGPT voice) is mitigated here or not at all.

    **Estimated Joe time:** 30 min recording + 20-30 min transcription cleanup = ~1 hour.

    **Step 1 — Pick your recording tool (2 min):**
    One of:
    - **Loom** (browser, free tier, auto-transcribes) — https://www.loom.com — click "New video" → "Screen + cam" or "Cam only" → Record.
    - **macOS Voice Memos** (built-in; Joe has a Mac per design spec) — open Voice Memos → red button → speak → stop. Export as .m4a. Later paste transcript into ChatGPT (prompt below) for clean-up.
    - **Phone voice memo + Otter.ai free tier** (https://otter.ai — 300 min/mo free). Record on phone, email the file to Otter, get a transcript.

    No account signup needed if using Voice Memos. Pick whichever gets you recording fastest; tool quality is not the bottleneck.

    **Step 2 — Open the protocol on a second screen or phone (1 min):**
    `docs/interview-protocol-voice.md` is now committed in the repo. Open it in a browser (GitHub view works if pushed; or local preview). Or just open it in VS Code/Cursor on a second window.

    **Step 3 — Record the interview (~30 min):**
    Follow the protocol's 8 prompts. Rules of engagement (copy-paste from protocol):
    - Speak extemporaneously; do not write out an answer first.
    - Use contractions. Say "I don't know" if you don't. Say "I think X" not "one might argue X."
    - Don't self-edit mid-sentence. Let the false starts in — they're part of voice.
    - Take positions. If a prompt asks what annoys you, name the thing.
    - 2-4 minutes per prompt. Skip any that doesn't have a real answer today.

    Target: all 8 prompts in ~30 minutes. If you hit prompt 6 at 25 minutes and you're fatigued, stop there — 6 strong prompts beats 8 tired ones.

    **Step 4 — Transcribe (~20-30 min):**

    **If Loom or Otter:** transcript auto-generated. Copy into a local file `transcripts/voice-interview-<YYYY-MM-DD>.md` (or anywhere on your laptop — **do NOT commit to this repo if it contains any confidential material**).

    **If Voice Memos:** Paste this ChatGPT prompt into a new chat with the .m4a uploaded (Whisper-based transcription is built into ChatGPT now):
    > "Transcribe this audio. Clean up the transcript: preserve contractions, preserve false starts where they carry meaning, remove only um/uh filler and repeated words. Label each answer with 'Prompt N:' based on which of the 8 prompts from my voice interview protocol it's answering."

    **Step 5 — Save transcript locally (1 min):**
    - Path: (your local notes — Obsidian, Notes.app, ~/Documents/, wherever)
    - Filename: voice-interview-<YYYY-MM-DD>.md
    - Content: the cleaned transcript + date + which tool was used

    **DO NOT commit the transcript to the repo if it contains confidential company or coworker details.** Task 3 extracts the 8-12 passages for `kb/voice.md` and 2-3 for `kb/stances.md`; the full transcript stays private.

    **Step 6 — Update the content-status tracker (2 min):**
    Edit `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` — under VOICE-05:
    - Check: `- [x] Voice interview recorded (tool: Loom)` (or whichever tool)
    - Check: `- [x] Transcript produced (stored locally, NOT committed if confidential)`
    - Fill in: `Transcript date: <today's date>`

    Commit the tracker update:
    ```bash
    git add .planning/phases/01-foundation-content/01-CONTENT-STATUS.md
    git commit -m "content(01-04): record voice interview (VOICE-05 part 1)"
    ```

    **Step 7 — Report back:**
    Type "voice-interview-done" with: (a) which tool you used, (b) transcript date, (c) how many of the 8 prompts you actually answered (if not all 8, which ones you skipped and why).
  </how-to-verify>
  <resume-signal>Type "voice-interview-done" + tool used + date + prompt count. If you hit a blocker (tool failed, background too loud, prompt that doesn't land), paste what went wrong and we'll adjust.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [JOE-TIME, ~1 hour] Seed voice.md (8-12 samples) + 2-3 stances.md entries from transcript (VOICE-04, VOICE-05 finish, VOICE-06 partial)</name>
  <files>kb/voice.md, kb/stances.md, .planning/phases/01-foundation-content/01-CONTENT-STATUS.md</files>
  <action>
    Joe + Claude (in a fresh chat session) extract 8-12 passages from the Task 2 transcript into `kb/voice.md` and 2-3 stance seeds into `kb/stances.md`. See `<how-to-verify>` below for the step-by-step. This task is `checkpoint:human-action` because the passage-selection decisions are Joe's — "is this me or not?" — not something an executor agent can decide.
  </action>
  <verify>
    <automated>grep -c "^## Sample" kb/voice.md 2>/dev/null | awk '$1 >= 8 && $1 <= 12 { print "OK"; exit 0 } { print "FAIL voice sample count:", $1; exit 1 }' && grep -c "^## Stance" kb/stances.md 2>/dev/null | awk '$1 >= 2 && $1 <= 3 { print "OK (seed count)"; exit 0 } { print "seed count out of range (expected 2-3):", $1; exit 1 }' && npm test -- tests/lib/system-prompt.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>kb/voice.md has 8-12 labeled samples, kb/stances.md has 2-3 seed stances, tracker VOICE-04 + VOICE-05 finish rows checked, determinism test passes, Joe typed "voice-seeded".</done>
  <what-built>
    - Voice interview transcript exists locally (Task 2)
    - `docs/interview-protocol-voice.md` "After the recording" section is the script for this task
    - `kb/voice.md` and `kb/stances.md` still contain Plan 02 placeholders waiting to be replaced
  </what-built>
  <how-to-verify>
    **Why checkpoint:human-action:** Joe reads the transcript and picks which passages are "him" and which aren't. Claude can help draft labels and cut/paste, but the selection is Joe's. This is also the first Joe-Claude collaborative working session — spawn a **new chat window** so this task runs with fresh context.

    **Estimated Joe time:** ~1 hour (Joe reviewing + editing; Claude does the cut-and-label mechanics).

    **Step 1 — Open a fresh chat session (1 min):**
    In a new Claude Code window, paste this as the opening message:
    > "I'm running Task 3 of `.planning/phases/01-foundation-content/01-04-PLAN.md`. Read that file + `docs/interview-protocol-voice.md` (the 'After the recording' section) + the transcript I'm about to paste. Then help me seed `kb/voice.md` with 8-12 passages and `kb/stances.md` with 2-3 stances. Rules from Pitfall 3: voice.md passages must read as unfiltered, conversational, in one of five registers (casual / decisive / annoyed / curious / teaching / reflective). Stances must pass the 'could a senior PM I respect disagree with this?' test. Voice-first always — do not polish into LinkedIn register."

    Paste the transcript next.

    **Step 2 — Joe + Claude extract voice.md passages (~30 min):**
    Claude suggests candidate passages with register labels; Joe approves / rejects / edits. Target 8-12 final passages.

    Format for each passage in `kb/voice.md`:
    ```
    ## Sample N — [register] ([source])

    > [2-4 sentence passage, conversational, preserving contractions and natural cadence]
    ```

    Where:
    - `[register]` is one of: casual, decisive, annoyed, curious, teaching, reflective
    - `[source]` is: `voice interview YYYY-MM-DD, prompt N` (or a non-interview source like "Slack DM to [name], 2025-XX" if Joe has pulled one in)
    - The passage is **verbatim or near-verbatim from the transcript**, not a rewrite

    **Do NOT** include passages drawn from LinkedIn posts, PRDs, performance-review writing, or anything already in corporate register. If the transcript has 4 casual passages and 1 curious passage, that's fine — uneven register distribution is preferable to polished-but-wrong samples. See CONTEXT.md D-C-04 (banned sources).

    Replace the entire placeholder content of `kb/voice.md` (the Plan 02 scaffold) with the new samples block. Keep the file's opening `# Voice Samples` heading.

    **Step 3 — Joe + Claude extract 2-3 stances.md seeds (~15 min):**
    Prompts 2, 3, and 8 from the voice interview are stance-producing by design. Pull 2-3 genuine opinion passages and polish them into `stances.md` format:

    ```
    ## Stance N — [topic]

    [1-3 sentence position statement — must be disagreeable-on-purpose]

    [1 paragraph elaborating the reasoning — what most PMs believe, why Joe thinks they're wrong or at least missing something]
    ```

    Test for each stance: "Could a senior PM I respect read this and say 'I disagree'?" If no → rewrite or cut. If yes → keep.

    Replace the first 2-3 stance blocks in `kb/stances.md` (Plan 02 placeholder). Leave the remaining slots for Task 4.

    **Step 4 — Update the content-status tracker (2 min):**
    Edit `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`:
    - Under VOICE-04: check all 5 sub-items (placeholder replaced, 8-12 samples, register labels, source labels, no LinkedIn/PRD sources).
    - Under VOICE-05: check "`voice.md` seeded from transcript" and "2-3 `stances.md` entries seeded from transcript".
    - VOICE-06 stays unchecked (3 of 8-12 stances ≠ done).

    **Step 5 — Commit (2 min):**
    ```bash
    git add kb/voice.md kb/stances.md .planning/phases/01-foundation-content/01-CONTENT-STATUS.md
    git commit -m "content(01-04): seed voice.md + partial stances.md from voice interview (VOICE-04, VOICE-05)"
    ```

    **Step 6 — Run the determinism test (sanity, 1 min):**
    ```bash
    npm test -- tests/lib/system-prompt.test.ts
    ```
    All 5 tests should still pass. Byte count will grow substantially (placeholder text → real content) — this is expected. If any test fails, paste the output.

    **Step 7 — Report back:**
    Type "voice-seeded" with: (a) number of voice samples, (b) number of stance seeds, (c) confirmation that determinism test still passes, (d) optional: which register was hardest to find a passage for.
  </how-to-verify>
  <resume-signal>Type "voice-seeded" + sample count + stance seed count + determinism-test result. If any voice.md sample feels like LinkedIn register, call it out now — easier to rewrite while the transcript is fresh.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [JOE-TIME, ~4-6 hours over 2-3 sittings] Write self-authored prose pack — about_me, management_philosophy, faq, stances completion, guardrails Joe-signed (VOICE-06, VOICE-07, VOICE-08, VOICE-09)</name>
  <files>kb/about_me.md, kb/management_philosophy.md, kb/stances.md, kb/faq.md, kb/guardrails.md, .planning/phases/01-foundation-content/01-CONTENT-STATUS.md</files>
  <action>
    Joe authors 4 prose files + completes stances. See `<how-to-verify>` below for sitting-by-sitting breakdown. This task is `checkpoint:human-action` because VOICE-08 and VOICE-09 require Joe-authored content — Claude can tighten grammar without touching register, but the substance is Joe's. Critical: `kb/guardrails.md` must be committed with Joe as git author (VOICE-09 load-bearing).
  </action>
  <verify>
    <automated>wc -w kb/about_me.md kb/management_philosophy.md 2>&1 | awk 'NR==1 && $1<400 { print "about_me too short:", $1; err=1 } NR==1 && $1>600 { print "about_me too long:", $1; err=1 } NR==2 && $1<600 { print "mgmt_philosophy too short:", $1; err=1 } NR==2 && $1>1000 { print "mgmt_philosophy too long:", $1; err=1 } END { exit err ? 1 : 0 }' && grep -c "^## Stance" kb/stances.md | awk '$1 < 8 || $1 > 12 { print "stance count out of range:", $1; exit 1 } { print "stance OK:", $1 }' && grep -c "^## Q:" kb/faq.md | awk '$1 != 15 { print "faq count wrong (expected 15):", $1; exit 1 } { print "faq OK: 15" }' && grep -q "^Signed:" kb/guardrails.md || grep -q "\*Signed:" kb/guardrails.md && git log -1 --format='%ae' kb/guardrails.md</automated>
  </verify>
  <done>All wordcounts in range, stance count 8-12, FAQ count 15, guardrails.md Joe-signed with Joe as git author, tracker VOICE-06/07/08/09 all checked, Joe typed "prose-pack-done".</done>
  <what-built>
    - `voice.md` populated (Task 3) — available as register reference
    - `stances.md` partially seeded (Task 3) — Joe finishes to 8-12 total
    - Plan 02 placeholder content still in `about_me.md`, `management_philosophy.md`, `faq.md`, `guardrails.md`
  </what-built>
  <how-to-verify>
    **Why checkpoint:human-action:** This is Joe-authored prose. Four files, spanning 2000-3000 words of original content, all drafted voice-first. Claude can pair-write (Joe dictates / types in conversational register, Claude tightens grammar without touching register). An executor agent cannot produce any of this content without violating VOICE-08, VOICE-09's author requirement.

    **Estimated Joe time:** ~4-6 hours total, typically spread over 2-3 sittings across a few days. This is the largest Joe-time block in Plan 04.

    **Recommended order (dependencies, not rules):**
    1. `about_me.md` first (warms Joe back up into personal register after the prose break from Task 3)
    2. `management_philosophy.md` next (opinion-dense; uses stances as seed material)
    3. `stances.md` completion (round out to 8-12 from the 2-3 seeds)
    4. `faq.md` (tightest register; recruiter-facing Q/A; straightforward)
    5. `guardrails.md` last (Joe authors with care because this is the load-bearing safety artifact; signs when done)

    ### Sitting 1 — about_me.md + management_philosophy.md (~2-3 hours)

    **Step 1a — `about_me.md` (400-600 words, ~60 min):**

    Open a new Claude Code session. Paste:
    > "I'm writing `kb/about_me.md` per VOICE-08 of `.planning/phases/01-foundation-content/01-04-PLAN.md`. Target: 400-600 words, first-person, warm register. Topics per spec §4: why PM, what energizes me, what I'm looking for, interests outside work. Use `kb/voice.md` as register reference. Rules: voice-first draft, no LinkedIn register, contractions on, take positions. I'll dictate; tighten grammar without touching register."

    Joe drafts extemporaneously (speaks if faster; pastes dictation if typing is faster). Claude suggests structure and tightens. Target 4 rough paragraphs:
    - Why PM (origin — what got you into it)
    - What energizes (the day-to-day work that makes a week feel great)
    - What you're looking for now
    - Who you are outside work (specific — hobbies with texture, not "I like hiking")

    Replace the entire placeholder of `kb/about_me.md`. Run `wc -w kb/about_me.md` — should be 400-600. If under, expand the weakest paragraph. If over, cut.

    **Step 1b — `management_philosophy.md` (600-1000 words, ~90 min):**

    Same session or new one. Paste:
    > "Writing `kb/management_philosophy.md` per VOICE-08. Target: 600-1000 words, opinionated and concrete. Per spec §4: building teams, coaching, feedback, hiring, disagreement, org design. Each section should pass the 'could a senior PM I respect disagree with me here?' test. Use `kb/stances.md` as seed material — any stance that's really a management position, expand it here."

    Structure suggestion (Joe can vary):
    - How I build teams (2-3 paragraphs — hiring, onboarding, first-90-days philosophy)
    - How I coach and give feedback (2-3 paragraphs — directness vs. diplomacy, 1:1 style, written feedback vs. verbal)
    - How I handle disagreement (2 paragraphs — with engineers, with designers, with leadership above me)
    - Org design / scope (1-2 paragraphs — when to split teams, when to keep things single-threaded)

    Replace the placeholder. `wc -w` in 600-1000.

    ### Sitting 2 — stances.md completion + faq.md (~1-2 hours)

    **Step 2a — `stances.md` to 8-12 total (~30-45 min):**

    Already 2-3 seeds from Task 3. Write 6-9 more to hit 8-12 total. Sources:
    - Strong claims in `management_philosophy.md` you just wrote — promote the sharpest 2-3 into stances (abbreviated to stance format)
    - Anything from the voice-interview transcript that wasn't captured in the initial 2-3 seeds
    - Joe's opinions about tools, processes, frameworks (OKRs? agile? north-star metrics? A/B testing's overuse?)

    Each stance:
    ```
    ## Stance N — [topic]

    [1-3 sentence position — disagreeable-on-purpose]

    [1 short paragraph — why you hold this position, with at least one concrete example]
    ```

    Test each one: "Could a senior PM I respect disagree?" If no → rewrite or cut. Target 8-12 strong positions.

    **Step 2b — `faq.md` (15 entries, ~30-45 min):**

    Claude drafts first pass — 15 Q/A entries covering the required topics:
    1. Visa / work authorization
    2. Open to remote?
    3. Timezone / working hours?
    4. Compensation expectations → redirect to email
    5. Availability / start date
    6. Open to relocation?
    7. Current role status (employed / open / gardening leave)
    8. Interests outside work
    9. What kind of role are you looking for?
    10. Do you prefer B2B or B2C?
    11. Data/analytics PM vs. general PM preference?
    12. Company stage preference (seed / Series A-C / public)?
    13. Industry interests
    14. Why are you looking?
    15. How did you build this agent? (meta — about the project itself)

    Each entry uses this format (exact header format `## Q: ...` is what the automated verify checks for):
    ```
    ## Q: <question text>

    A: <answer text — 1-3 sentences, voice-first>
    ```

    Joe edits each one for voice — FAQ is the tightest register; keep answers 1-3 sentences. For Q4 (compensation), the answer should be something like: "I'd rather not anchor in a chatbot — happy to talk numbers once we're both interested. Shoot Joe an email."

    Replace placeholder. All 15 present.

    ### Sitting 3 — guardrails.md (Joe-authored, Joe-signed, ~30-45 min)

    **Step 3 — `guardrails.md`:**

    Per CONTEXT.md D-C-06, this file MUST cover all 7 topics. Joe authors each section personally — Claude can suggest structure but not content. This is the single most load-bearing safety artifact in the project.

    Sections (from Plan 02 placeholder — keep the structure, rewrite each body):
    - Fabrication
    - Compensation & negotiation
    - Former-employer discussion
    - Confidential details
    - Hiring & comp redirects
    - No verbatim dumping
    - Persona integrity

    Write ~2-4 sentences per section in Joe's voice. End the file with:

    ```
    Signed: Joe Dollinger, <YYYY-MM-DD>
    ```

    The `Signed:` line must appear exactly (with a `*Signed:` italic variant also accepted by the automated verify). The verifier also checks `git log -1 --format='%ae' kb/guardrails.md` — the committing author's email must be Joe's.

    **CRITICAL — commit as Joe:**
    ```bash
    git add kb/guardrails.md
    git commit -m "content(01-04): Joe-author and Joe-sign guardrails.md (VOICE-09)"
    # Verify git author is Joe:
    git log -1 --format='%an <%ae>' kb/guardrails.md
    # Expected: something like "Joe Dollinger <joe.dollinger@gmail.com>"
    ```
    If git author is not Joe, fix git config and re-commit. The commit being Joe-authored is how VOICE-09 "Joe-signed" is verified.

    ### After all three sittings

    **Step 4 — Update content-status tracker:**
    Edit `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`:
    - VOICE-06 → check all 3 sub-items (placeholder replaced, 8-12 stances, each passes disagreement test)
    - VOICE-07 → check all 3 sub-items (placeholder replaced, 15 entries, coverage topics)
    - VOICE-08 → check all 6 sub-items (wordcounts filled in; Joe confirms register)
    - VOICE-09 → check all 4 sub-items (including git-author verification)

    **Step 5 — Run determinism test:**
    ```bash
    npm test -- tests/lib/system-prompt.test.ts
    # Expected: all 5 tests pass. Byte count grew again.
    ```

    **Step 6 — Commit (after each sitting; consolidate if you prefer):**
    ```bash
    git add kb/about_me.md kb/management_philosophy.md kb/stances.md kb/faq.md
    git commit -m "content(01-04): Joe-authored prose — about, philosophy, stances, faq (VOICE-06, 07, 08)"
    # guardrails.md committed separately per Step 3 above so Joe is sole author
    ```

    **Step 7 — Report back:**
    Type "prose-pack-done" with: (a) wordcounts for about_me and management_philosophy, (b) stance count, (c) FAQ count (should be exactly 15), (d) guardrails commit hash and git-author line, (e) determinism-test status.
  </how-to-verify>
  <resume-signal>Type "prose-pack-done" + metrics listed above. If any one section stalled (e.g., management_philosophy felt repetitive with stances), call it out — can rework before moving to Task 5.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: [JOE-TIME, ~30 min] Case-study selection session (pre-work for VOICE-02, VOICE-03)</name>
  <files>kb/brainstorm/case-study-candidates.md, .planning/phases/01-foundation-content/01-CONTENT-STATUS.md</files>
  <action>
    Joe + Claude run the 30-min selection session per `docs/interview-protocol-selection.md`, landing 8-10 candidate stories and pruning to 4-6 with rubric coverage verified. See `<how-to-verify>` below. This task is `checkpoint:human-action` because the story-selection decisions are Joe's memory + judgment.
  </action>
  <verify>
    <automated>grep -c "^## Candidate:" kb/brainstorm/case-study-candidates.md | awk '$1 < 8 { print "candidate count too low:", $1; exit 1 } { print "candidate count OK:", $1 }' && grep -q "Final 4-6" kb/brainstorm/case-study-candidates.md && grep -q "Rubric coverage verified" kb/brainstorm/case-study-candidates.md</automated>
  </verify>
  <done>kb/brainstorm/case-study-candidates.md has 8-10 candidates + Final 4-6 block + rubric coverage line, tracker Selection-session rows checked, interviews scheduled, Joe typed "selection-done".</done>
  <what-built>
    - `docs/interview-protocol-selection.md` is the script (Task 1)
    - `kb/voice.md`, `kb/about_me.md`, `kb/resume.md` (assuming real resume content landed) available as context
    - `kb/brainstorm/case-study-candidates.md` scaffold exists (Plan 02) — currently placeholder
  </what-built>
  <how-to-verify>
    **Why checkpoint:human-action:** Joe's stories, Joe's memory, Joe's decision on which 4-6 to invest ~45 min each interviewing. Claude facilitates; Joe decides.

    **Estimated Joe time:** ~30 min focused session.

    **Step 1 — Spawn a fresh chat session (1 min):**
    In a new Claude Code window, paste:
    > "I'm running Task 5 of `.planning/phases/01-foundation-content/01-04-PLAN.md`. Execute the selection-session protocol at `docs/interview-protocol-selection.md`. Load: that protocol + `kb/resume.md` + `kb/about_me.md` + `kb/voice.md`. Output: `kb/brainstorm/case-study-candidates.md` filled to 8-10 candidates with the final 4-6 block at the bottom + coverage rubric verified."

    **Step 2 — Follow the protocol (~25 min):**
    Claude cycles through the 8 brainstorm prompts from the protocol. Joe answers each with 1-2 candidate stories. Claude captures each in the file template (Role/timeframe, one-line hook, why this one, rough outcome, confidentiality concerns, freshness, rubric bucket).

    Target: 8-10 candidates. If you hit 6 and are clearly out of stories, 6 is fine — the pruning phase doesn't require a big starting pool.

    **Step 3 — Prune to 4-6 (~5 min):**
    Apply the pruning rules from the protocol in order. End with the "Final 4-6" block at the bottom of the file:
    ```
    ## Final 4-6 (selected YYYY-MM-DD)
    1. <slug-one>             — rubric: failure, long-arc
    2. <slug-two>             — rubric: leadership-without-authority
    3. <slug-three>           — rubric: data-rooted, recent
    4. <slug-four>            — rubric: cross-functional-conflict

    Rubric coverage verified: ✅ failure ✅ leadership-w/o-authority ✅ data-rooted ✅ cross-functional ✅ recent ✅ long-arc
    ```

    Slugs kebab-case, content-descriptive. Good: `killing-the-feature`, `bi-migration`. Bad: `story-1`, `project-x`.

    **Step 4 — Update content-status tracker:**
    Edit `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`:
    - Under "Selection session complete" → check all sub-items, including all 6 rubric lines.
    - Under VOICE-02 "Case studies (4-6 files)" → write each chosen slug into the 4-6 slots (leave the checkboxes unchecked; they check as each case study is written in Task 6).
    - Under VOICE-03 → fill in which slug fills which rubric bucket. Do NOT sign off yet — that's Task 7.

    **Step 5 — Schedule the case-study interviews:**
    Block time on Joe's calendar for each of the 4-6 interviews (~45 min each + ~15 min edit pass per study = ~1 hour each, totaling 4-6 hours). These become Task 6. Spacing them over a few days keeps energy up (fatigue kills voice).

    **Step 6 — Commit:**
    ```bash
    git add kb/brainstorm/case-study-candidates.md .planning/phases/01-foundation-content/01-CONTENT-STATUS.md
    git commit -m "content(01-04): selection session — 8-10 candidates, final 4-6 chosen, rubric verified"
    ```

    **Step 7 — Report back:**
    Type "selection-done" with: (a) the 4-6 chosen slugs, (b) the rubric mapping (which slug → which bucket), (c) when the interviews are scheduled (rough — "next 3 days" is fine).
  </how-to-verify>
  <resume-signal>Type "selection-done" + slug list + rubric mapping + rough interview schedule. If the rubric isn't satisfied with 4-6 candidates (common: no "long-arc" story surfaced), extend the brainstorm before proceeding.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: [JOE-TIME, ~4-6 hours across 4-6 sessions] Case-study interviews — one per chosen slug, voice-first drafting (VOICE-02, VOICE-10)</name>
  <files>kb/case_studies/, .planning/phases/01-foundation-content/01-CONTENT-STATUS.md</files>
  <action>
    Joe + Claude run 4-6 case-study interview sessions per `docs/interview-protocol-case-study.md`, one per chosen slug. Each session produces one `kb/case_studies/<slug>.md` file drafted voice-first. See `<how-to-verify>` below. This task is `checkpoint:human-action` because the case-study content is Joe's real project memories — executor cannot invent.
  </action>
  <verify>
    <automated>ls kb/case_studies/*.md 2>/dev/null | grep -v "_fixture_for_tests" | wc -l | awk '$1 < 4 || $1 > 6 { print "case study count out of range (expected 4-6):", $1; exit 1 } { print "case study count OK:", $1 }' && npx tsx scripts/validate-kb-frontmatter.ts && npm test -- tests/lib/system-prompt.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>4-6 case study files exist in kb/case_studies/ (excluding fixture), frontmatter validator exits 0, determinism test passes, tracker VOICE-02 per-slug rows all checked, Joe typed "case-studies-done".</done>
  <what-built>
    - `docs/interview-protocol-case-study.md` is the script (Task 1)
    - 4-6 slugs chosen with rubric mapping (Task 5)
    - `kb/voice.md` populated (Task 3) — available as register reference
    - `scripts/validate-kb-frontmatter.ts` ready to run (Task 1)
  </what-built>
  <how-to-verify>
    **Why checkpoint:human-action:** Joe's stories, dictated voice-first. Per Pitfall 3 execution rule: voice-passing at the end never works — first draft is conversational register or the story is dead. Executor cannot invent case studies about Joe's real projects.

    **Estimated Joe time:** ~1 hour per case study (45-min interview + 15-min next-day edit pass) × 4-6 = **4-6 hours total**. Spread over 3-5 days. Do not batch more than 2 in a day (fatigue kills voice).

    **For each of the 4-6 chosen slugs, run one session following this template:**

    ### Per-session flow

    **Step 1 — Spawn a fresh chat session (1 min):**
    One session per case study. New chat each time. Paste:
    > "I'm running Task 6 of `.planning/phases/01-foundation-content/01-04-PLAN.md` for slug `<slug>`. Execute the per-case-study interview protocol at `docs/interview-protocol-case-study.md`. Load: that protocol + `kb/voice.md` + `kb/brainstorm/case-study-candidates.md` (find the row for `<slug>`) + `kb/case_studies/_fixture_for_tests.md` as template reference. Output: `kb/case_studies/<slug>.md` drafted voice-first, ≥300 words, all template sections filled, frontmatter complete."

    **Step 2 — Interview (~30 min):**
    Claude runs the 5 clusters (Context / Options / Decision / Outcome / Retrospective) with 3-4 probing questions each. Joe answers extemporaneously. Claude takes notes; does NOT draft yet.

    Voice-first rules (repeat each session):
    - Conversational register from the first draft
    - Contractions on
    - Take positions
    - Let the Retrospective section show real ambiguity ("I still don't know if X was the right call") — don't settle for "lessons learned"

    **Step 3 — Draft (~10 min, at end of interview):**
    Claude writes the file in realtime using the template at the bottom of `docs/interview-protocol-case-study.md`. Real files use standard three-hyphen YAML frontmatter delimiters:
    - Opens with `---` on its own line
    - YAML key/value pairs for slug, hook, role, timeframe, confidential
    - Closes with `---` on its own line
    - Then the body sections: Context / Options considered / Decision & reasoning / Outcome / Retrospective / Likely recruiter follow-ups (and my answers)

    If `confidential: true`, Claude swaps company/person names for placeholders during drafting — do NOT leave real names pending later scrubbing.

    Target: ≥300 words, typically 400-600.

    **Step 4 — Next-day Joe edit pass (~15 min):**
    Don't edit same-day — freshness kills voice. Next day:
    1. Read aloud. Any sentence that sounds like LinkedIn → rewrite.
    2. Retrospective: does it have real ambiguity? If it's "I learned the importance of X" → rewrite with a specific unresolved thing.
    3. Options: are they genuinely competing? If they're strawmen → rewrite or cut.
    4. Do NOT add bullets within paragraph sections. Do NOT add markdown headers inside sections.

    **Step 5 — Validate frontmatter (~1 min):**
    ```bash
    npx tsx scripts/validate-kb-frontmatter.ts
    ```
    If it complains about this file's frontmatter or word count, fix and re-run. Once 4-6 studies exist and all validate, exits 0.

    **Step 6 — Update tracker + commit:**
    Edit `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`:
    - Under VOICE-02 → check the row for this slug.
    (VOICE-10 voice-pass is Task 7 — don't check voice-pass rows here.)

    ```bash
    git add kb/case_studies/<slug>.md .planning/phases/01-foundation-content/01-CONTENT-STATUS.md
    git commit -m "content(01-04): draft case study <slug> (VOICE-02 part)"
    ```

    ### Repeat for each of the 4-6 slugs

    **After all 4-6 case studies exist:**

    **Step 7 — Run determinism test:**
    ```bash
    npm test -- tests/lib/system-prompt.test.ts
    # All 5 tests pass. Byte count grew substantially with real case studies.
    ```

    **Step 8 — Run frontmatter validator:**
    ```bash
    npx tsx scripts/validate-kb-frontmatter.ts
    # Exits 0 with "OK: N case studies validated."
    ```

    If frontmatter validation fails on any file, fix before moving to Task 7.

    **Step 9 — Report back:**
    Type "case-studies-done" with: (a) list of slugs, (b) wordcount per slug, (c) frontmatter validator output, (d) determinism-test status, (e) optional: which case study you're least satisfied with (gets extra scrutiny in Task 7 voice-pass).
  </how-to-verify>
  <resume-signal>Type "case-studies-done" + slug list + wordcounts + validator output. If any case study's first draft drifted into LinkedIn register and you can tell, flag it now — easier to re-interview while memory is fresh than to voice-pass-at-the-end in Task 7.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: [JOE-TIME, ~1 hour] Voice-pass final read + coverage rubric sign-off + Plan 04 exit (VOICE-03, VOICE-10, plan close)</name>
  <files>kb/case_studies/, .planning/phases/01-foundation-content/01-CONTENT-STATUS.md</files>
  <action>
    Joe reads each case study aloud to verify voice-first register (VOICE-10), re-verifies the coverage rubric against the final 4-6 (VOICE-03), and signs off the content-status tracker. This closes Plan 04. See `<how-to-verify>` below. Task is `checkpoint:human-verify` because Joe's read-aloud judgment is the gate.
  </action>
  <verify>
    <automated>grep -q "Final rubric sign-off.*(Joe)" .planning/phases/01-foundation-content/01-CONTENT-STATUS.md && grep -q "Voice-pass sign-off.*(Joe)" .planning/phases/01-foundation-content/01-CONTENT-STATUS.md && npm test 2>&1 | tail -5 && npx tsx scripts/validate-kb-frontmatter.ts</automated>
  </verify>
  <done>All tracker checkboxes checked, rubric + voice-pass signed by Joe (initials + date), all tests pass, validator exits 0, guardrails git-author is Joe, Joe typed "plan-04-approved".</done>
  <what-built>
    - All VOICE-02..10 content populated (Tasks 2-6)
    - `scripts/validate-kb-frontmatter.ts` exits 0 (Task 6)
    - `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` mostly checked; voice-pass rows + rubric sign-off still open
  </what-built>
  <how-to-verify>
    **Why checkpoint:human-verify:** This is the plan-close verification. Joe reads each case study aloud and confirms conversational register (VOICE-10), confirms the rubric is satisfied across the final 4-6 (VOICE-03), then signs off.

    **Estimated Joe time:** ~1 hour.

    **Step 1 — Voice-pass each case study (~30 min total):**

    For each of the 4-6 `kb/case_studies/*.md` files, read aloud. Ask yourself:
    - Does this sound like a story I'd tell a trusted former coworker at dinner?
    - Or does it sound like a PRD / performance-review prose / LinkedIn post?

    **Failing voice-pass signs:**
    - Opens with "In my role as..." or "This project demonstrates..."
    - Uses banned vocab (leverage / robust / holistic / align (verb) / drive (verb))
    - Retrospective reads as "key learnings" / "takeaways" list
    - No contractions anywhere
    - Any sentence feels like it was written for a slide, not said to a human

    If a study fails voice-pass, rewrite the offending sections in-place (don't re-interview — Joe knows the content now, just rephrase). Commit: `content(01-04): voice-pass edits on <slug>`.

    Check each row in `01-CONTENT-STATUS.md` → VOICE-10 "Voice-pass final read" as each case study passes.

    **Step 2 — Coverage rubric sign-off (~5 min):**

    Re-open `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` → VOICE-03 section. For each rubric bucket, verify:
    - Is there actually a slug filling this bucket?
    - Does the slug's content genuinely satisfy the bucket (not just label-compatible)?

    Specifically challenge yourself:
    - "Failure or killed project" — does the study actually contain a failure outcome, or is it a success story with one bullet about something that went wrong?
    - "Leadership without authority" — did Joe genuinely lead people outside his reporting line, or was it a normal team-lead situation?
    - "Data-rooted" — is the decision actually grounded in specific data/metrics, or is "data" just mentioned as context?
    - "Cross-functional conflict" — was there real disagreement, or just normal async coordination?
    - "Recent (<2y)" — is the timeframe actually within the last 2 years from today?
    - "Long-arc (>12mo)" — did the project actually span more than a year, or is "timeframe" generous?

    If any bucket is weak, either:
    - Edit the relevant case study to surface the missing dimension, OR
    - Swap in another candidate from `case-study-candidates.md` (run one more Task 6 session for the new slug)

    Once all 6 buckets are genuinely satisfied, write your initials + date on the sign-off line:
    ```
    - [x] **Final rubric sign-off (Joe):** JD, <YYYY-MM-DD>
    ```

    Same for voice-pass sign-off.

    **Step 3 — Plan exit criteria check (~5 min):**

    Scroll to "Plan 04 Exit Criteria" in `01-CONTENT-STATUS.md`. Verify each line:

    - Every checkbox above is checked — skim the whole file. If any VOICE-XX checkbox is unchecked, fix (or explain why and leave unchecked — but the plan does not close unless ALL are checked).
    - `npx tsx scripts/validate-kb-frontmatter.ts` exits 0 — re-run it.
    - `kb/guardrails.md` git-blame shows Joe as author of the Signed commit — run `git log -1 --format='%an <%ae>' kb/guardrails.md`. Confirm Joe.
    - Coverage rubric and voice-pass have Joe's initials + date — confirm Step 2 above.

    Check all four boxes.

    **Step 4 — Final determinism test (~1 min):**
    ```bash
    npm test
    ```
    Every test in the Phase 1 suite (kb-loader.test.ts + system-prompt.test.ts + anything else) should pass. The system prompt byte count will be dramatically larger than with placeholder content — this is the point.

    Note the final byte count: `node -e "console.log(require('./src/lib/system-prompt').buildSystemPrompt().length)"` — record it for the Plan 04 SUMMARY (useful baseline for Phase 2 cost modeling).

    **Step 5 — Final commit:**
    ```bash
    git add kb/case_studies/*.md .planning/phases/01-foundation-content/01-CONTENT-STATUS.md
    git commit -m "content(01-04): voice-pass edits + VOICE-03 rubric sign-off + VOICE-10 sign-off (plan close)"
    ```

    **Step 6 — Report back:**
    Approve when:
    - All 4-6 case studies passed voice-pass (VOICE-10 ✅)
    - Coverage rubric satisfied with Joe's initials (VOICE-03 ✅)
    - Plan 04 exit criteria all checked
    - All tests pass
    - guardrails.md git-author is Joe

    Type "plan-04-approved" with: (a) final system-prompt byte count, (b) any case study that required heavy voice-pass rework (for Plan 04 SUMMARY), (c) rubric mapping (slug → bucket) for the SUMMARY.

    If ANY criterion is unmet, paste what's missing and we'll fix before sign-off.
  </how-to-verify>
  <resume-signal>Type "plan-04-approved" + final byte count + rubric mapping + any voice-pass rework notes. If a case study failed voice-pass and can't be salvaged, we may need one more Task 6 session for a replacement slug — call it out and we'll schedule.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Joe's voice-interview transcript → kb/voice.md | Unfiltered, potentially confidential speech filtered down to committed samples; confidential content must stay out of git |
| Case-study interviews → kb/case_studies/*.md | Joe's memory of real projects, potentially containing company/employee names under NDA; `confidential: true` frontmatter triggers name anonymization |
| Joe-authored guardrails → system prompt (Phase 2) | The file that defines agent refusal behavior; if compromised/incomplete, every safety defense downstream is weaker |
| Executor agent running Plan 04 → kb/*.md | Risk: agent invents content to appear helpful, silently violating VOICE-04/06/08/09 authenticity rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Voice-interview transcript committed to repo with confidential details (company internals, coworker names, NDA'd metrics) | mitigate | Task 2 Step 5 explicitly instructs "DO NOT commit to the repo if it contains confidential material." Transcript stays on Joe's laptop; only 8-12 extracted, cleaned passages land in `kb/voice.md`. Task 3 Step 2 "passage is verbatim or near-verbatim — not a rewrite" means Joe reads each extracted passage before commit. |
| T-04-02 | Information Disclosure | Case study written with real confidential company/metric details, `confidential: false` by mistake | mitigate | Task 6 Step 3 requires Claude to proactively swap names when `confidential: true` during drafting. Task 7 voice-pass read catches any leaked internal detail. `.gitignore` does NOT exclude `kb/case_studies/`, so any mistake is publicly committed — the Plan 02 fixture file's `confidential: false` pattern is a reminder to explicitly set this per-study. Long-term: Phase 2's Haiku classifier + Phase 5 eval cat 1 catch agent behaviors violating confidentiality. |
| T-04-03 | Tampering | Executor agent invents voice samples to "be helpful," silently violating VOICE-04's unfiltered-source requirement | mitigate | Every Joe-time task is `type="checkpoint:human-action"` — the agent cannot pass the checkpoint without Joe's explicit "<signal>-done" reply. Content-status tracker requires Joe confirmation per sub-item. Layer of defense: Plan 04's plan-level `autonomous: false` flag makes execute-phase pause for Joe every time. |
| T-04-04 | Tampering | Executor agent rewrites kb/guardrails.md after Joe signs it (breaking VOICE-09 Joe-authored requirement) | mitigate | Task 4 Step 3 explicitly instructs committing guardrails.md with Joe's git identity (`git log -1 --format='%an <%ae>'`). Content-status tracker requires explicit "git author is Joe" check. Any later modification to guardrails.md will show Claude/other as author in git blame — detectable. Phase 5 pre-launch checklist re-verifies. |
| T-04-05 | Elevation of Privilege | `scripts/validate-kb-frontmatter.ts` or `gray-matter` parser exploited via malicious frontmatter in a committed case study | accept | Joe authors every case study; no external input. Script runs locally / in CI (Phase 5), not in production. `gray-matter` parses YAML; malformed YAML throws, not exploitable at scale relevant here. Phase 5 CI lint would catch if ever a generated input path appears. |
| T-04-06 | Repudiation | "Joe signed the guardrails" claim cannot be verified after the fact | mitigate | `git log` is the signature mechanism. Task 4 Step 3's explicit commit-by-Joe + the content-status tracker's "git author verification" row makes the signature cryptographically traceable. If git config is later changed, historical commits remain verifiable. |
| T-04-07 | Denial of Service | Joe-time block stalls indefinitely (life happens); Phase 1 can't close | accept | Plan 04 has no external deadline. Engineering Plans 01-03 already landed; Phase 1 exit criteria require this plan's sign-off but nothing downstream blocks today. Joe controls pacing. If stalled >2 weeks, Phase 1 retrospective would surface it — fine. |
| T-04-08 | Spoofing | Voice-interview transcript fabricated (not a real recording) | accept | Solo developer workflow; Joe is the only author AND reviewer. No adversarial spoofer. Self-discipline is the control. Phase 5 friend-test (non-PM reader says "sounds like Joe") is the ultimate catch — fabricated transcript would fail voice-pass. |
</threat_model>

<verification>
- `docs/interview-protocol-{voice,selection,case-study}.md` all exist, each with the structured content from Task 1
- `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md` exists, every VOICE-02..10 row checked after Task 7
- `scripts/validate-kb-frontmatter.ts` exists and exits 0 after Task 6
- `kb/voice.md` has 8-12 sample blocks with register + source labels; no LinkedIn/PRD sources (Joe confirms)
- `kb/stances.md` has 8-12 disagreeable-position blocks
- `kb/about_me.md` wordcount 400-600
- `kb/management_philosophy.md` wordcount 600-1000
- `kb/faq.md` has exactly 15 Q/A entries
- `kb/guardrails.md` contains `Signed: Joe Dollinger, <date>` and `git log -1 --format='%an <%ae>' kb/guardrails.md` shows Joe
- `kb/brainstorm/case-study-candidates.md` has 8-10 candidates + final 4-6 block with rubric check
- `kb/case_studies/` has 4-6 `.md` files (excluding `_fixture_for_tests.md`), each ≥300 words with valid frontmatter
- `npx tsx scripts/validate-kb-frontmatter.ts` exits 0 with "OK: N case studies validated"
- `npm test` still passes (kb-loader + system-prompt determinism tests)
- Coverage rubric sign-off has Joe's initials + date on the tracker
- Voice-pass sign-off has Joe's initials + date on the tracker
</verification>

<success_criteria>
1. All 9 VOICE-* requirements (VOICE-02 through VOICE-10) satisfied with sign-off on `.planning/phases/01-foundation-content/01-CONTENT-STATUS.md`
2. Three interview protocol docs committed under `docs/`, reusable in future phases and post-launch KB refreshes
3. Content-status tracker established as the durable record of which content landed when
4. `scripts/validate-kb-frontmatter.ts` wired and exits 0 — catches frontmatter drift in any future KB edit
5. `kb/` fully populated with real, voice-first, Joe-authored content replacing every Plan 02 placeholder
6. `kb/guardrails.md` is Joe-signed with git-author evidence (VOICE-09's load-bearing safety artifact)
7. 4-6 case studies drafted voice-first (VOICE-10 verified by voice-pass read) covering the full coverage rubric (VOICE-03 verified by explicit sign-off)
8. System-prompt byte count grew substantially from placeholder baseline (Plan 02 SUMMARY recorded baseline; Plan 04 SUMMARY records post-population count for Phase 2 cost modeling)
9. Every determinism test continues to pass (placeholder → real content is a byte-count change, not a structural change; determinism test only cares about byte-identity across calls, not size)
10. Phase 1 content-complete: the only remaining Phase 1 work is any outstanding engineering from Plans 01-03 that slipped — Plan 04 itself is done.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-content/01-04-SUMMARY.md` documenting:
- **System-prompt byte count** after content population (compare to Plan 02 placeholder baseline) — critical input for Phase 2 Anthropic cost modeling
- **Voice-interview date + tool used** (for VOICE-05 provenance; transcript stays private)
- **Final 4-6 case study slugs** with rubric bucket mapping
- **Wordcounts**: about_me, management_philosophy, case studies (per slug)
- **Guardrails commit hash** + `git log` author output proving Joe-signed
- **Voice samples source mix** (e.g., "6 from voice interview, 2 from Slack DMs with <redacted>, 2 from unfiltered emails") — for Phase 5 eval cat 4 LLM-judge rubric
- **Any case study that required heavy voice-pass rework** — note for future retrospective (Pitfall 3 lesson on voice-first drafting discipline)
- **Content-status tracker location** (for future reference if KB needs refresh later)
- **Phase 2 handoff note:** the populated `kb/` is now the source of truth for the cached system prompt in `/api/chat`. Plan 02's determinism test already guards against KB drift breaking cache integrity (Pitfall 2); re-run after every KB edit.
</output>
