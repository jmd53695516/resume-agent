// tests/setup.ts
// Plan 03-03 Task 0: enable @testing-library/jest-dom matchers (toBeInTheDocument,
// toHaveAttribute, etc.) for component tests that opt into jsdom via the per-file
// `// @vitest-environment jsdom` directive (W3 — global env stays 'node').
import '@testing-library/jest-dom/vitest';
